---
phase: 99-quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/page.tsx
  - web/src/app/layout.tsx
  - api/agents/orchestrator.py
autonomous: false
requirements: []
must_haves:
  truths:
    - "Chat interface has modern themed UI with dark/light mode support"
    - "Backend starts without GCP credentials in dev mode"
    - ".env.example documents required environment variables"
  artifacts:
    - path: "web/src/app/page.tsx"
      provides: "Themed chat UI with shadcn components"
    - path: "api/agents/orchestrator.py"
      provides: "Lazy LLM init with mock fallback"
  key_links:
    - from: "web/src/app/page.tsx"
      to: "api/agents/orchestrator.py"
      via: "fetch to /chat endpoint"
---

<objective>
Quick win UI overhaul and GCP lazy init fix. Deliver modern chat UI with shadcn theming and dark/light mode toggle, plus backend that starts without GCP credentials.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/01-project-skeleton-core-orchestrator/01-UAT.md (gaps to close)
@.planning/phases/01-project-skeleton-core-orchestrator/01-CONTEXT.md
@.agents/skills/frontend-design/SKILL.md

**Gaps from UAT:**
1. UI too basic — need shadcn components, theme.css, dark/light mode, modern themed UI
2. Backend crashes on startup — ChatVertexAI initializes at module load without GCP credentials
</context>

<tasks>

<task type="auto">
  <name>Task 1: UI Overhaul with shadcn theming and dark/light mode</name>
  <files>web/src/app/page.tsx, web/src/app/layout.tsx</files>
  <action>
    Overhaul the chat UI using shadcn/ui components and theming system:

    1. Setup shadcn/ui if not configured: Run `npx shadcn@latest init` in web directory with default settings (slate base color, CSS variables)

    2. Add shadcn components needed:
       - `npx shadcn@latest add button card input textarea avatar dropdown-menu`
       - These provide the component primitives

    3. Create theme.css with shadcn's CSS variable system for dark/light theming. The theme should:
       - Use shadcn's semantic color tokens (--background, --foreground, --primary, --secondary, etc.)
       - Support `data-theme="dark"` and `data-theme="light"` toggling
       - Apply distinctive typography (avoid Inter/Roboto — pick something from Google Fonts like "Outfit" or "Sora" for headings, "DM Sans" for body)
       - Add subtle animations: fade-in for messages, smooth theme transitions

    4. Update page.tsx with modern chat UI:
       - Use Card component for chat container
       - Button with variants for send action
       - Avatar for user/bot distinction
       - Clean message bubbles with proper spacing
       - Theme toggle button (sun/moon icon) in header
       - Loading skeleton for AI responses

    5. Update layout.tsx to:
       - Import theme.css
       - Add theme script for preventing flash of unstyled content
       - Set up next-themes provider for dark/light mode toggle

    Design direction: Minimal, refined aesthetic with generous whitespace. Dark mode as default (matches developer tools aesthetic), but light mode fully supported. Subtle gradient accents, not purple/blue gradients.
  </action>
  <verify>
    <automated>npm run build --prefix web 2>&1 | head -50</automated>
  </verify>
  <done>Modern chat UI with shadcn components, dark/light mode toggle, builds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Backend GCP lazy init with mock fallback</name>
  <files>api/agents/orchestrator.py</files>
  <action>
    Fix the GCP credential crash by implementing lazy initialization with mock fallback:

    1. Modify orchestrator.py to NOT initialize ChatVertexAI at module import time

    2. Create a function `get_llm()` that:
       - Checks for GCP credentials (GOOGLE_APPLICATION_CREDENTIALS env var or ADC)
       - If credentials present: initialize and return real ChatVertexAI
       - If no credentials: return a mock LLM that returns placeholder responses

    3. Mock LLM should implement the same interface (invoke, stream) and return something like:
       `"[Mock] GCP not configured. Set GOOGLE_APPLICATION_CREDENTIALS or run `gcloud auth application-default login`"

    4. Replace direct `llm = ChatVertexAI(...)` with `llm = get_llm()` called inside the graph execution, not at module load

    5. Add a health check at startup that verifies LLM availability and logs warning if using mock

    The key principle: No network calls or credential checks at import time. Everything lazy.
  </action>
  <verify>
    <automated>cd api && python -c "from agents.orchestrator import Orchestrator; print('Import successful, no crash')"</automated>
  </verify>
  <done>Backend imports without crashing, starts with mock LLM when GCP unavailable</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify UI and Backend</name>
  <files></files>
  <action>
    Automated verification of both fixes:
    - npm run build passes in web/
    - python import succeeds in api/
  </action>
  <verify>
    <automated>npm run build --prefix web 2>&1 | tail -10 && cd api && python -c "from agents.orchestrator import Orchestrator; print('Backend import OK')"</automated>
  </verify>
  <done>Both UI builds and backend imports successfully</done>
</task>

</tasks>

<verification>
- `npm run build --prefix web` succeeds
- `python -c "from agents.orchestrator import Orchestrator"` succeeds without GCP credentials
</verification>

<success_criteria>
1. Chat UI uses shadcn components with theme.css dark/light mode
2. Backend starts without GCP credentials (uses mock fallback)
3. Both builds/imports pass automated verification
</success_criteria>

<output>
After completion, create `.planning/quick/260416-wt6-ui-overhaul-with-shadcn-theming-dark-lig/260416-wt6-SUMMARY.md`
</output>
