---
phase: 23-Onboarding-UX-Improvements
plan: quick-update
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/23-Onboarding-UX-Improvements/23-01-PLAN.md, .planning/phases/23-Onboarding-UX-Improvements/23-02-PLAN.md]
autonomous: true
requirements: [PHASE-23-FORMAT]
---

<objective>
Update the Phase 23 plan files to follow the systematic GSD format, specifically fixing the file reference paths in <execution_context> and ensuring internal consistency with the project's state.
</objective>

<execution_context>
@.planning/quick/260510-x3h-update-the-23rd-phase-plan-files-in-a-sy/260510-x3h-PLAN.md
</execution_context>

<context>
@.planning/phases/23-Onboarding-UX-Improvements/23-01-PLAN.md
@.planning/phases/23-Onboarding-UX-Improvements/23-02-PLAN.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize Plan File Formatting</name>
  <files>.planning/phases/23-Onboarding-UX-Improvements/23-01-PLAN.md, .planning/phases/23-Onboarding-UX-Improvements/23-02-PLAN.md</files>
  <action>
    Update both plan files:
    1. Replace `$HOME/.gemini/get-shit-done/` paths in `<execution_context>` with `.planning/` equivalents if applicable, or remove local machine absolute paths. Specifically:
       - `@$HOME/.gemini/get-shit-done/workflows/execute-plan.md` -> `@.planning/workflows/execute-plan.md` (or similar project-relative path).
       - `@$HOME/.gemini/get-shit-done/templates/summary.md` -> `@.planning/templates/summary.md`.
    2. Ensure `phase` frontmatter exactly matches the directory name: `23-Onboarding-UX-Improvements`.
    3. Verify all `<files>` in tasks exist or are intended for creation.
  </action>
  <verify>
    <automated>grep -c "$HOME" .planning/phases/23-Onboarding-UX-Improvements/*.md == 0</automated>
  </verify>
  <done>Plan files normalized to project-relative paths.</done>
</task>

<task type="auto">
  <name>Task 2: Commit Formatted Plans</name>
  <files>.planning/phases/23-Onboarding-UX-Improvements/23-01-PLAN.md, .planning/phases/23-Onboarding-UX-Improvements/23-02-PLAN.md</files>
  <action>
    Stage and commit the changes to the plan files with a clean message.
  </action>
  <verify>
    <automated>git diff --exit-code .planning/phases/23-Onboarding-UX-Improvements/</automated>
  </verify>
  <done>Changes committed to git.</done>
</task>

</tasks>

<success_criteria>
1. Phase 23 PLAN files no longer contain absolute `$HOME` paths.
2. All paths in `<execution_context>` are project-relative or reference correct global agents if needed.
</success_criteria>
