---
phase: 23-Onboarding-UX-Improvements
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [web/package.json, web/src/hooks/useOnboarding.ts, web/src/hooks/useOnboarding.test.ts, web/src/components/onboarding/OnboardingGuide.tsx, web/src/components/onboarding/OnboardingStepCard.tsx, web/src/components/onboarding/OnboardingTrigger.tsx, web/src/components/onboarding/StatusStream.tsx, web/src/components/onboarding/ChoicePoll.tsx, web/src/components/onboarding/SetupGuide.tsx, web/src/app/repo/[id]/page.tsx]
autonomous: true
requirements: [FR-03]
must_haves:
  truths:
    - "User is presented with a Choice Poll (Backend, Frontend, Fullstack, Exploring) before onboarding starts."
    - "SSE request includes the selected 'focus' parameter: GET /repo/{id}/start-here?focus={role}."
    - "Onboarding guide uses 'cult-ui' Onboarding stepper and IntroDisclosure components."
    - "File structures and code changes are visualized using @pierre/trees and @pierre/diffs."
  artifacts:
    - path: "web/src/hooks/useOnboarding.ts"
      provides: "SSE hook supporting focus-tailored requests"
    - path: "web/src/hooks/useOnboarding.test.ts"
      provides: "Unit tests for the onboarding SSE hook"
    - path: "web/src/components/onboarding/OnboardingGuide.tsx"
      provides: "Main walkthrough component using cult-ui and @pierre libs"
    - path: "web/src/components/onboarding/OnboardingTrigger.tsx"
      provides: "Glow-pulsing trigger button for onboarding"
    - path: "web/src/components/onboarding/StatusStream.tsx"
      provides: "Real-time SSE status message list"
    - path: "web/src/components/onboarding/ChoicePoll.tsx"
      provides: "Initial user intent capture component"
    - path: "web/src/components/onboarding/SetupGuide.tsx"
      provides: "Component for copyable setup commands"
  key_links:
    - from: "web/src/app/repo/[id]/page.tsx"
      to: "web/src/components/onboarding/OnboardingGuide.tsx"
      via: "component inclusion with focus state"
    - from: "web/src/hooks/useOnboarding.ts"
      to: "/api/repo/{repo_id}/start-here"
      via: "EventSource with focus query param"
---

<objective>
Implement the personalized frontend UI for repository onboarding.
This involves setting up high-fidelity visualization libraries (@pierre), adding interactive intent-gathering components (cult-ui ChoicePoll), and building a multi-step walkthrough that displays the tailored onboarding plan, fully verified with unit tests and graph updates.
</objective>

<execution_context>
@.planning/workflows/execute-plan.md
@.planning/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-Onboarding-UX-Improvements/23-01-PLAN.md
@.planning/phases/23-Onboarding-UX-Improvements/23-UI-SPEC.md
@.planning/phases/23-Onboarding-UX-Improvements/23-VALIDATION.md
@web/src/app/repo/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup & SSE Integration Hook with Tests</name>
  <files>web/package.json, web/src/hooks/useOnboarding.ts, web/src/hooks/useOnboarding.test.ts</files>
  <action>
    1. Install libraries: `pnpm i @pierre/diffs @pierre/trees motion`.
    2. Add cult-ui components via registry:
       - `npx shadcn@latest add "https://www.cult-ui.com/r/onboarding.json"`
       - `npx shadcn@latest add "https://www.cult-ui.com/r/intro-disclosure.json"`
       - `npx shadcn@latest add "https://www.cult-ui.com/r/choice-poll.json"`
    3. Create `web/src/hooks/useOnboarding.test.ts` as specified in 23-VALIDATION.md to test SSE connection, focus parameter, and state updates.
    4. Implement `web/src/hooks/useOnboarding.ts`:
       - Function: `startGeneration(focus: string)` that initializes `EventSource` to `/api/repo/{repoId}/start-here?focus={focus}`.
       - Manage `status`, `plan`, `loading`, and `error` states.
       - Ensure proper cleanup of `EventSource` on unmount.
  </action>
  <verify>
    <automated>cd web && npm test src/hooks/useOnboarding.test.ts</automated>
  </verify>
  <done>Dependencies installed and SSE hook supports focus-tailored generation with unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: High-Fidelity Onboarding Components</name>
  <files>web/src/components/onboarding/OnboardingGuide.tsx, web/src/components/onboarding/OnboardingStepCard.tsx, web/src/components/onboarding/OnboardingTrigger.tsx, web/src/components/onboarding/StatusStream.tsx, web/src/components/onboarding/ChoicePoll.tsx, web/src/components/onboarding/SetupGuide.tsx</files>
  <action>
    1. Implement specialized components in `web/src/components/onboarding/` per 23-UI-SPEC.md:
       - `OnboardingTrigger.tsx`: Center-aligned button with `animate-glow-pulse`.
       - `StatusStream.tsx`: Renders vertical list of SSE status messages with `animate-fade-up`.
       - `ChoicePoll.tsx`: Captures user focus (Backend, Frontend, Fullstack, Exploring).
       - `SetupGuide.tsx`: Block with copyable commands and "Copy Command" button.
    2. Update `OnboardingGuide.tsx` to orchestrate the flow:
       - Show `OnboardingTrigger` initially.
       - Show `ChoicePoll` to capture focus.
       - Use `StatusStream` during generation.
       - Finally, show the `Onboarding` walkthrough stepper.
    3. Use `@pierre/trees` and `@pierre/diffs` within walkthrough steps for visualization.
    4. Ensure all components follow design tokens in 23-UI-SPEC.md (Orange Flame brand).
  </action>
  <verify>
    <automated>cd web && npm run lint src/components/onboarding/</automated>
  </verify>
  <done>Interactive walkthrough and high-fidelity visualizations implemented with all sub-components.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Integration and Visual Verification</name>
  <files>web/src/app/repo/[id]/page.tsx</files>
  <action>
    1. Integrate `<OnboardingGuide repoId={params.id} />` into the main repo view.
    2. Verify the end-to-end flow:
       - Click "Start Onboarding" (glow pulse).
       - Select focus in `ChoicePoll`.
       - Observe SSE status updates (e.g., "Analyzing [focus] entry points...").
       - Navigate the multi-step `Onboarding` guide.
       - Inspect `@pierre/trees` and `@pierre/diffs` for correct visualization.
    3. Finalize codebase knowledge graph.
  </action>
  <what-built>Full personalized E2E onboarding experience with high-fidelity UI.</what-built>
  <how-to-verify>
    1. Navigate to a repository.
    2. Select "Backend" in the poll.
    3. Verify the generated steps focus on controllers/models.
    4. Repeat with "Frontend" and verify focus on components/hooks.
  </how-to-verify>
  <verify>
    <automated>graphify update .</automated>
  </verify>
  <resume-signal>approved</resume-signal>
  <done>Personalized onboarding UX integrated, visually verified, and knowledge graph updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → cult-ui Registry | Fetching component definitions |
| Browser → EventSource | Incoming tailored SSE stream |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Cross-Site Scripting (XSS) | OnboardingGuide | mitigate | Sanitize all content rendered from the LLM-generated JSON, especially in Diffs. |
| T-23-04 | Supply Chain | cult-ui / @pierre | accept | Use trusted sources for UI libraries; standard project risk. |
</threat_model>

<success_criteria>
1. User can personalize their onboarding guide via a Choice Poll.
2. The walkthrough uses high-fidelity components (stepper, trees, diffs).
3. The UI provides real-time feedback and handles errors gracefully.
4. All frontend logic is verified by unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/23-Onboarding-UX-Improvements/23-02-SUMMARY.md`
</output>
