---
phase: 23-Onboarding-UX-Improvements
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [web/src/hooks/useOnboarding.ts, web/src/components/onboarding/OnboardingGuide.tsx, web/src/components/onboarding/OnboardingStepCard.tsx, web/src/app/repo/[id]/page.tsx]
autonomous: true
requirements: [FR-03]
must_haves:
  truths:
    - "User sees a 'Start Here' or 'Onboarding' section on the repository detail page."
    - "Clicking 'Generate Plan' initiates the SSE stream and shows real-time progress updates."
    - "The final onboarding plan is displayed with guided steps, key files, and architecture overview."
    - "Loading and error states are handled gracefully during the streaming process."
  artifacts:
    - path: "web/src/hooks/useOnboarding.ts"
      provides: "Custom hook for managing SSE connection and state"
    - path: "web/src/components/onboarding/OnboardingGuide.tsx"
      provides: "Main component to display the onboarding plan"
    - path: "web/src/components/onboarding/OnboardingStepCard.tsx"
      provides: "Sub-component for individual onboarding steps"
  key_links:
    - from: "web/src/app/repo/[id]/page.tsx"
      to: "web/src/components/onboarding/OnboardingGuide.tsx"
      via: "component inclusion"
    - from: "web/src/hooks/useOnboarding.ts"
      to: "/api/repo/{repo_id}/start-here"
      via: "EventSource connection"
---

<objective>
Implement the frontend UI for repo onboarding plans.
This involves creating a custom React hook to manage the SSE stream, developing components to visualize the structured plan, and integrating the onboarding guide into the repository detail page.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-Onboarding-UX-Improvements/23-01-PLAN.md
@web/src/app/repo/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: SSE Integration Hook</name>
  <files>web/src/hooks/useOnboarding.ts</files>
  <action>
    Create `web/src/hooks/useOnboarding.ts`:
    - Implement `useOnboarding(repoId: string)` hook.
    - State: `status` (string), `plan` (OnboardingPlan | null), `loading` (boolean), `error` (string | null).
    - Function: `startGeneration()` that initializes `EventSource` to `/api/repo/{repoId}/start-here`.
    - Event handlers:
      - `onmessage`: Parse `data`. If `type === 'status'`, update `status`. If `type === 'plan'`, set `plan`. If `type === 'done'`, close connection.
      - `onerror`: Handle connection issues and set `error`.
    - Cleanup: Ensure `eventSource.close()` is called on unmount.
  </action>
  <verify>
    <automated>npm test web/src/hooks/useOnboarding.ts</automated>
  </verify>
  <done>Hook correctly manages SSE lifecycle and state updates.</done>
</task>

<task type="auto">
  <name>Task 2: Onboarding UI Components</name>
  <files>web/src/components/onboarding/OnboardingGuide.tsx, web/src/components/onboarding/OnboardingStepCard.tsx</files>
  <action>
    1. Create `web/src/components/onboarding/OnboardingStepCard.tsx`:
       - Display step title, description, complexity badge, and related files.
    2. Create `web/src/components/onboarding/OnboardingGuide.tsx`:
       - Props: `repoId`.
       - Uses `useOnboarding(repoId)`.
       - Render states:
         - Idle: Show "Generate Onboarding Guide" button.
         - Loading: Show progress bar/spinner and current status message.
         - Success: Display Architecture Overview, Setup Guide, Key Files, and list of `OnboardingStepCard`s.
         - Error: Display error message with a "Retry" button.
    - Use shadcn/ui components (Card, Button, Badge, Skeleton) for styling.
  </action>
  <verify>
    <automated>npm run lint web/src/components/onboarding/</automated>
  </verify>
  <done>UI components implemented and styled according to project conventions.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Integration and Visual Verification</name>
  <files>web/src/app/repo/[id]/page.tsx</files>
  <action>
    1. Open `web/src/app/repo/[id]/page.tsx`.
    2. Import and add `<OnboardingGuide repoId={params.id} />` to the main repository view (e.g., in a new tab or as a section above/below the file tree).
    3. Run the development server and verify the flow: Trigger generation -> See status updates -> View final plan.
  </action>
  <what-built>Full E2E onboarding plan generation flow from Frontend to Backend.</what-built>
  <how-to-verify>
    1. Navigate to a repository detail page.
    2. Click the onboarding trigger.
    3. Observe the loading states and final plan display.
    4. Check that the plan persists on page refresh (if already generated).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
  <done>Onboarding UX integrated and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → EventSource | Incoming SSE stream |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Cross-Site Scripting (XSS) | OnboardingGuide | mitigate | Sanitize/escape all content rendered from the LLM-generated JSON. |
</threat_model>

<success_criteria>
1. User can successfully generate and view an onboarding plan.
2. Real-time feedback is provided during the generation process.
3. UI is responsive and follows existing design patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/23-Onboarding-UX-Improvements/23-02-SUMMARY.md`
</output>
