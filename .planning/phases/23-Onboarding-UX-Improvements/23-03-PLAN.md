---
phase: 23-Onboarding-UX-Improvements
plan: 03
type: execute
wave: 1
depends_on: [02]
files_modified: [api/db/onboarding_models.py, api/agents/onboarding.py, api/routes/repo.py, web/src/hooks/useOnboarding.ts, web/src/components/onboarding/OnboardingGuide.tsx, web/src/components/onboarding/OnboardingStepCard.tsx]
autonomous: true
requirements: [FR-02, FR-03]
gap_closure: true
must_haves:
  truths:
    - "Backend and Frontend share a unified contract for OnboardingPlan (setup_commands, key_files description)."
    - "Previously generated onboarding plans are retrievable via GET /api/backend/repo/{id}/onboarding-plan."
    - "SSE error events emit 'message' key to match frontend expectation."
    - "OnboardingGuide handles state transitions within useEffect to avoid render-phase updates."
  artifacts:
    - path: "api/routes/repo.py"
      provides: "Retrieval endpoint for cached onboarding plans"
    - path: "web/src/components/onboarding/OnboardingStepCard.tsx"
      provides: "Reusable wrapper for onboarding walkthrough steps"
  key_links:
    - from: "web/src/hooks/useOnboarding.ts"
      to: "/api/backend/repo/{id}/onboarding-plan"
      via: "fetch in startGeneration (check for existing plan)"
---

<objective>
Close critical gaps in the Phase 23 implementation: align the frontend/backend contract, implement the missing retrieval path for cached plans, fix React state update issues, and refactor the onboarding walkthrough into reusable components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-Onboarding-UX-Improvements/23-02-SUMMARY.md
@api/db/onboarding_models.py
@api/agents/onboarding.py
@api/routes/repo.py
@web/src/hooks/useOnboarding.ts
@web/src/components/onboarding/OnboardingGuide.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align Backend Contract and SSE Error Payload</name>
  <files>api/db/onboarding_models.py, api/agents/onboarding.py</files>
  <action>
    1. Update `api/db/onboarding_models.py`:
       - `KeyFile`: Change field `why` to `description`.
       - `OnboardingPlan`: Change field `setup: str` to `setup_commands: list[str]`.
    2. Update `api/agents/onboarding.py`:
       - Update `_build_system_prompt` JSON schema to use `setup_commands` (array) and `description` (within key_files).
       - Update `generate_onboarding_plan` error yield: Change `{"type": "error", "content": ...}` to `{"type": "error", "message": ...}` for consistency with frontend.
  </action>
  <verify>
    <automated>pytest tests/test_onboarding_agent.py</automated>
  </verify>
  <done>Backend models and AI prompt aligned with frontend expectations; error events use 'message' key.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Plan Retrieval Route</name>
  <files>api/routes/repo.py</files>
  <action>
    1. Add `GET /repo/{repo_id}/onboarding-plan` endpoint to `api/routes/repo.py`.
    2. Implementation:
       - Resolve `repo_id` to UUID.
       - Query `repo_onboarding_plans` table for the existing plan.
       - Return 404 if not found, 200 with the JSON plan if it exists.
       - Add migration logic to map old keys (setup -> setup_commands, why -> description) if the stored JSON uses the legacy schema.
  </action>
  <verify>
    <automated>curl -s http://localhost:8000/api/repo/{test_id}/onboarding-plan</automated>
  </verify>
  <done>Onboarding plans can be retrieved via standard GET request without re-triggering generation.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend Component Refinement and Fixes</name>
  <files>web/src/hooks/useOnboarding.ts, web/src/components/onboarding/OnboardingGuide.tsx, web/src/components/onboarding/OnboardingStepCard.tsx</files>
  <action>
    1. Update `web/src/hooks/useOnboarding.ts`:
       - In `startGeneration`, first check if a plan already exists via `GET /api/backend/repo/{repoId}/onboarding-plan`.
       - If it exists, set the plan and return early.
       - If not, proceed with `EventSource` generation.
    2. Create `web/src/components/onboarding/OnboardingStepCard.tsx`:
       - Encapsulate the `Onboarding.Step` + `Onboarding.Header` + `CodeDiff` / `FileTree` logic.
       - Props: `step: number`, `title: string`, `description: string`, `files?: string[]`, `keyFiles?: string[]`.
    3. Refactor `web/src/components/onboarding/OnboardingGuide.tsx`:
       - Replace inline step rendering with `<OnboardingStepCard />`.
       - Move the `setFlowState("PLAN_READY")` state transition into a `useEffect` that watches `loading` and `plan` changes.
  </action>
  <verify>
    <automated>cd web && npm run lint src/components/onboarding/</automated>
  </verify>
  <done>Frontend uses retrieval path, React state updates are safe, and walkthrough UI is refactored for maintainability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Database → API | Cached plan retrieval |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Information Disclosure | GET /onboarding-plan | mitigate | Ensure repo_id lookup validates that the repository is accessible to the requester (per existing auth patterns). |
</threat_model>

<success_criteria>
1. Frontend/Backend contract is identical.
2. Cached plans load instantly on revisit.
3. Errors in SSE are correctly displayed in the UI.
4. No React warnings regarding state updates during render.
</success_criteria>

<output>
After completion, create `.planning/phases/23-Onboarding-UX-Improvements/23-03-SUMMARY.md`
</output>
