---
phase: 23-Onboarding-UX-Improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [sql/migrations/0028_add_onboarding_plans_table.sql, api/db/onboarding_models.py, api/agents/onboarding.py, api/routes/repo.py, tests/test_onboarding_agent.py]
autonomous: true
requirements: [FR-03]
must_haves:
  truths:
    - "Backend can store and retrieve onboarding plans from repo_onboarding_plans table."
    - "GET /repo/{repo_id}/start-here endpoint accepts a 'focus' query parameter."
    - "Onboarding agent uses the 'focus' parameter to tailor the system prompt for Qwen2.5-72B."
    - "SSE endpoint streams status updates and the final tailored plan."
    - "Agent implements exponential backoff for validation retries."
  artifacts:
    - path: "sql/migrations/0028_add_onboarding_plans_table.sql"
      provides: "Database table for onboarding plans"
    - path: "api/db/onboarding_models.py"
      provides: "Pydantic models for onboarding plan schema"
    - path: "api/agents/onboarding.py"
      provides: "Agent logic with focus-based prompt tailoring and backoff"
    - path: "api/routes/repo.py"
      provides: "SSE endpoint with focus parameter support"
    - path: "tests/test_onboarding_agent.py"
      provides: "Unit tests for agent personalization and retry logic"
  key_links:
    - from: "api/routes/repo.py"
      to: "api/agents/onboarding.py"
      via: "function call with focus parameter"
    - from: "api/agents/onboarding.py"
      to: "Qwen2.5-72B"
      via: "tailored system prompt"
---

<objective>
Implement the backend infrastructure for personalized AI-powered onboarding plans.
This includes the database schema, Pydantic models, and a focus-aware onboarding agent that uses the Big Model to generate tailored guides delivered via SSE, featuring robust exponential backoff for retries.
</objective>

<execution_context>
@.planning/workflows/execute-plan.md
@.planning/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-Onboarding-UX-Improvements/23-CONTEXT.md
@.planning/phases/23-Onboarding-UX-Improvements/23-VALIDATION.md
@api/routes/repo.py
@api/agents/utils/llm.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database Migration and Models</name>
  <files>sql/migrations/0028_add_onboarding_plans_table.sql, api/db/onboarding_models.py</files>
  <action>
    1. Create migration `sql/migrations/0028_add_onboarding_plans_table.sql` with table `repo_onboarding_plans` (repo_id UUID PK, plan JSONB, updated_at TIMESTAMP).
    2. Create `api/db/onboarding_models.py`.
    3. Define `OnboardingStep`, `KeyFile`, and `OnboardingPlan` Pydantic models as specified in 23-CONTEXT.md.
    4. Add a helper function `upsert_onboarding_plan(repo_id, plan_dict)` to save results to the DB.
  </action>
  <verify>
    <automated>python api/db/onboarding_models.py && ls sql/migrations/0028_add_onboarding_plans_table.sql</automated>
  </verify>
  <done>Database table created and Pydantic models defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Focus-Aware Onboarding Agent with Exponential Backoff</name>
  <files>api/agents/onboarding.py, tests/test_onboarding_agent.py</files>
  <behavior>
    - Should accept `focus` parameter (Backend, Frontend, Fullstack, Exploring).
    - Should use `focus` to tailor the system prompt (e.g., "Analyze this repo from a [focus] perspective...").
    - Should use code_search to identify entry points relevant to the focus.
    - Should implement 2 retries with Pydantic validation using Qwen2.5-72B.
    - Should implement exponential backoff for retries (e.g., 1s delay for 1st retry, 2s for 2nd).
    - Should yield status updates referencing the focus (e.g., "Analyzing [focus] entry points...").
  </behavior>
  <action>
    1. Create `tests/test_onboarding_agent.py` as specified in 23-VALIDATION.md:
       - Test that `focus` parameter correctly influences prompt generation (mocking LLM).
       - Test that invalid JSON from LLM triggers retries with exponential backoff (verify timing/count).
    2. Create `api/agents/onboarding.py`:
       - Implement `generate_onboarding_plan(repo_id: str, focus: str)` as an async generator.
       - Use `asyncio.sleep` to implement exponential backoff on validation failure.
       - Discovery Phase: Use `code_search` to find files matching the user's intent.
       - Synthesis Phase: Prompt Qwen with discovered context and strict JSON instructions, emphasizing the `focus`.
       - Validation: Use `OnboardingPlan.model_validate_json`. Retry up to 2 times on failure.
       - Yield `{"type": "status", "content": "..."}` and finally `{"type": "plan", "content": {...}}`.
  </action>
  <verify>
    <automated>pytest tests/test_onboarding_agent.py</automated>
  </verify>
  <done>Agent can generate focus-tailored onboarding plans with exponential backoff retries and validation.</done>
</task>

<task type="auto">
  <name>Task 3: SSE Endpoint with Focus Parameter</name>
  <files>api/routes/repo.py</files>
  <action>
    Add `GET /repo/{repo_id}/start-here` to `api/routes/repo.py`:
    - Accept optional query parameter `focus`.
    - Default `focus` to "Exploring" if not provided.
    - Resolve `repo_id` to UUID.
    - Return `StreamingResponse` using `generate_onboarding_plan(repo_id, focus)`.
    - Ensure headers include `"Content-Type": "text/event-stream"`.
  </action>
  <verify>
    <automated>curl -N "http://localhost:8000/api/repo/{test_repo_id}/start-here?focus=Frontend"</automated>
  </verify>
  <done>SSE endpoint correctly handles focus parameter and streams tailored updates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Frontend → API | SSE request with repo_id and focus parameter |
| API → Big Model | Sending codebase context and user intent for synthesis |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | Onboarding Agent | mitigate | Ensure repo_id is validated and only authorized repo info is processed. |
| T-23-02 | Denial of Service | SSE Endpoint | mitigate | Limit concurrent onboarding generation requests per user/repo. |
</threat_model>

<success_criteria>
1. SSE endpoint returns status updates referencing the user's focus.
2. Final event contains a valid JSON plan tailored to the selected focus.
3. Agent implements exponential backoff on retries.
4. Successful plans are cached in the database.
</success_criteria>

<output>
After completion, create `.planning/phases/23-Onboarding-UX-Improvements/23-01-SUMMARY.md`
</output>
