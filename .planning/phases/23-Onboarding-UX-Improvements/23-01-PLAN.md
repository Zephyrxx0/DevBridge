---
phase: 23-Onboarding-UX-Improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [sql/migrations/0028_add_onboarding_plans_table.sql, api/db/onboarding_models.py, api/agents/onboarding.py, api/routes/repo.py]
autonomous: true
requirements: [FR-03]
must_haves:
  truths:
    - "Backend can store and retrieve onboarding plans from repo_onboarding_plans table."
    - "GET /repo/{repo_id}/start-here endpoint streams status updates via SSE."
    - "Onboarding agent uses Big Model (Qwen2.5-72B) to generate plans with discovery tools."
    - "Onboarding plans are validated against Pydantic schema with 2 retries on failure."
  artifacts:
    - path: "sql/migrations/0028_add_onboarding_plans_table.sql"
      provides: "Database table for onboarding plans"
    - path: "api/db/onboarding_models.py"
      provides: "Pydantic models for onboarding plan schema"
    - path: "api/agents/onboarding.py"
      provides: "Agent logic for codebase discovery and plan synthesis"
    - path: "api/routes/repo.py"
      provides: "SSE endpoint implementation"
  key_links:
    - from: "api/routes/repo.py"
      to: "api/agents/onboarding.py"
      via: "function call in SSE generator"
    - from: "api/agents/onboarding.py"
      to: "api/db/onboarding_models.py"
      via: "Pydantic validation"
---

<objective>
Implement the backend infrastructure for AI-powered onboarding plans.
This includes the database schema, Pydantic models for strict JSON validation, an onboarding agent for codebase discovery, and an SSE endpoint for streaming status updates to the frontend.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@api/routes/repo.py
@api/main.py
@api/agents/orchestrator.py
@api/agents/utils/llm.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database Migration and Models</name>
  <files>sql/migrations/0028_add_onboarding_plans_table.sql, api/db/onboarding_models.py</files>
  <action>
    1. Create migration `sql/migrations/0028_add_onboarding_plans_table.sql` with table `repo_onboarding_plans` (repo_id PK, plan JSONB, updated_at).
    2. Create `api/db/onboarding_models.py`.
    3. Define `OnboardingStep`, `KeyFile`, and `OnboardingPlan` Pydantic models as specified in 23-CONTEXT.md.
    4. Add a helper function `upsert_onboarding_plan(repo_id, plan_dict)` to save results to the DB.
  </action>
  <verify>
    <automated>python api/db/onboarding_models.py && ls sql/migrations/0028_add_onboarding_plans_table.sql</automated>
  </verify>
  <done>Database table created and Pydantic models defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Onboarding Discovery & Synthesis Agent</name>
  <files>api/agents/onboarding.py</files>
  <behavior>
    - Should use code_search to identify entry points.
    - Should fetch README.md and package.json/requirements.txt for setup commands.
    - Should use Big Model (Qwen2.5-72B) via get_model(is_fast=False).
    - Should implement 2 retries with Pydantic validation.
    - Should yield status updates (e.g., "Analyzing...", "Generating...") via an async generator.
  </behavior>
  <action>
    Create `api/agents/onboarding.py`:
    - Implement `generate_onboarding_plan(repo_id: str)` as an async generator.
    - Phase 1: Discovery. Use `code_search` and `_fetch_github_file` (if available in routes/repo.py or similar helper).
    - Phase 2: Synthesis. Prompt Qwen with discovered context and strict JSON instructions.
    - Phase 3: Validation. Use `OnboardingPlan.model_validate_json`. On failure, retry with error feedback in prompt.
    - Yield `{"type": "status", "content": "..."}` and finally `{"type": "plan", "content": {...}}`.
  </action>
  <verify>
    <automated>pytest tests/test_onboarding_agent.py</automated>
  </verify>
  <done>Agent can discover repo info and generate a valid JSON onboarding plan with retries.</done>
</task>

<task type="auto">
  <name>Task 3: SSE Endpoint Implementation</name>
  <files>api/routes/repo.py</files>
  <action>
    Add `GET /repo/{repo_id}/start-here` to `api/routes/repo.py`:
    - Resolve `repo_id` to UUID.
    - Return `StreamingResponse` using the generator from `api/agents/onboarding.py`.
    - Handle client disconnection.
    - Ensure headers include `"Content-Type": "text/event-stream"`.
  </action>
  <verify>
    <automated>curl -N http://localhost:8000/api/repo/{test_repo_id}/start-here</automated>
  </verify>
  <done>SSE endpoint active and streaming status updates/plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Frontend → API | SSE request with repo_id |
| API → Big Model | Sending codebase context for synthesis |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | Onboarding Agent | mitigate | Ensure repo_id is validated and only public/authorized repo info is processed. |
| T-23-02 | Denial of Service | SSE Endpoint | mitigate | Limit concurrent onboarding generation requests (resource intensive Big Model). |
</threat_model>

<success_criteria>
1. SSE endpoint returns status updates during generation.
2. Final event contains a valid JSON plan matching the Pydantic schema.
3. Database caches the successful plan.
</success_criteria>

<output>
After completion, create `.planning/phases/23-Onboarding-UX-Improvements/23-01-SUMMARY.md`
</output>
