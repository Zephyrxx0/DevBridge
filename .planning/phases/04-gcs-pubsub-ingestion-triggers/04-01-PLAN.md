---
phase: 04-gcs-pubsub-ingestion-triggers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
user_setup:
  - service: gcp
    why: "GCS bucket for code snapshots"
    env_vars:
      - name: GCS_BUCKET_NAME
        source: "GCP Console -> Cloud Storage -> Buckets"
        description: "Name of the code snapshots bucket"
    dashboard_config:
      - task: "Create GCS bucket with versioning"
        location: "GCP Console -> Cloud Storage -> Create Bucket"
      - task: "Enable object versioning"
        location: "Bucket Settings -> Versioning"
      - task: "Create Pub/Sub topic for bucket notifications"
        location: "GCP Console -> Pub/Sub -> Topics"

---

<objective>
Setup GCS bucket for code snapshots with versioning enabled and Pub/Sub topic for event notifications.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-gcs-pubsub-ingestion-triggers/04-CONTEXT.md
@.planning/phases/03-code-parsing-with-tree-sitter/03-CONTEXT.md
@.planning/phases/02-data-foundation-secrets-management/02-CONTEXT.md
</context>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Configure GCP storage configuration</name>
  <files>infra/gcs/variables.tf</files>
  <read_first>
- .planning/phases/02-data-foundation-secrets-management/02-CONTEXT.md
- .planning/phases/03-code-parsing-with-tree-sitter/03-CONTEXT.md
  </read_first>
  <action>
Create Terraform configuration for GCS bucket with the following specifications:
1. Bucket name: `{project_id}-code-snapshots` (use variable from existing config)
2. Location: `us-central1` (regional)
3. Enable `versioning` = true
4. Default storage class: `STANDARD`
5. Enable uniform bucket-level access
6. Add labels: `environment = "production"`, `team = "devbridge"`

Reference existing infrastructure from Phase 02 for Terraform patterns.
  </action>
  <verify>
<automated>ls -la infra/gcs/ 2>/dev/null || echo "infra/gcs/ not found"</automated>
  </verify>
  <done>GCS bucket configuration created in Terraform</done>
</task>

<task type="auto">
  <name>Task 2: Create Pub/Sub topic for GCS events</name>
  <files>infra/pubsub/topics.tf</files>
  <read_first>
- .planning/phases/02-data-foundation-secrets-management/02-CONTEXT.md
  </read_first>
  <action>
Create Terraform configuration for Pub/Sub topic:
1. Topic name: `code-snapshot-events`
2. Message retention: 7 days
3. Enable message deduplication
4. Schema: None (raw JSON from GCS events)

Add subscription for Cloud Run Job:
1. Pull subscription: `code-snapshot-trigger`
2. Ack deadline: 600 seconds (10 minutes)
3. Retry policy: exponential backoff, min wait 10s, max wait 600s
4. Dead-letter topic for failed messages
  </action>
  <verify>
<automated>ls -la infra/pubsub/ 2>/dev/null || echo "infra/pubsub/ not found"</automated>
  </verify>
  <done>Pub/Sub topic and subscription configured</done>
</task>

<task type="auto">
  <name>Task 3: Configure GCS notification to Pub/Sub</name>
  <files>infra/gcs/notifications.tf</files>
  <read_first>
- infra/gcs/variables.tf (from Task 1)
- infra/pubsub/topics.tf (from Task 2)
  </read_first>
  <action>
Create GCS bucket notification configuration:
1. Event type: `google.storage.object.finalize` (upload/create)
2. Event type: `google.storage.object.delete` (cleanup trigger)
3. Payload format: JSON
4. Topic reference: uses `code-snapshot-events` Pub/Sub topic

Use Terraform google_storage_notification resource.
  </action>
  <verify>
<automated>grep -r "google_storage_notification" infra/gcs/ 2>/dev/null || echo "notification resource not found"</automated>
  </verify>
  <done>GCS event notifications configured to trigger Pub/Sub</done>
</task>

</tasks>

<verification>
- [ ] GCS bucket Terraform configuration exists with versioning
- [ ] Pub/Sub topic created with appropriate retention
- [ ] GCS notifications configured to publish to topic
- [ ] Terraform plan validates successfully
</verification>

<success_criteria>
- Terraform applies successfully and creates bucket/topic
- Bucket has versioning enabled
- Pub/Sub subscription ready for Cloud Run Job consumption
</success_criteria>

<output>
After completion, create `.planning/phases/04-gcs-pubsub-ingestion-triggers/04-01-SUMMARY.md`
</output>