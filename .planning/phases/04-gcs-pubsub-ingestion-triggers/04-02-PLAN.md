---
phase: 04-gcs-pubsub-ingestion-triggers
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: []
autonomous: true
requirements: []
user_setup:
  - service: gcp
    why: "Cloud Run Job for event-driven ingestion"
    env_vars:
      - name: CLOUD_RUN_JOB_SERVICE_ACCOUNT
        source: "GCP Console -> IAM -> Service Accounts"
        description: "Service account for Cloud Run Job execution"
    dashboard_config:
      - task: "Create Cloud Run Job"
        location: "GCP Console -> Cloud Run -> Jobs"
      - task: "Configure job trigger"
        location: "Cloud Run Job -> Triggers -> Pub/Sub"

---

<objective>
Implement Pub/Sub triggered Cloud Run Job for code ingestion that pulls from GCS, invokes chunking, and persists to database.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-gcs-pubsub-ingestion-triggers/04-CONTEXT.md
@.planning/phases/03-code-parsing-with-tree-sitter/03-CONTEXT.md
</context>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cloud Run Job configuration</name>
  <files>infra/cloudrun/ingestion-job.tf</files>
  <read_first>
- .planning/phases/02-data-foundation-secrets-management/02-CONTEXT.md
- infra/gcs/variables.tf (from Plan 01)
- infra/pubsub/topics.tf (from Plan 01)
  </read_first>
  <action>
Create Terraform configuration for Cloud Run Job:
1. Job name: `code-ingestion-job`
2. Image: Use existing container image or deploy from Cloud Build
3. Memory: 2Gi
4. CPU: 1
5. Timeout: 3600s (1 hour max)
6. Max retries: 3
7. Task concurrency: 10
8. Environment variables:
   - `GCS_BUCKET_NAME` (reference from Plan 01)
   - `PUBSUB_TOPIC` = "code-snapshot-events"
   - `DB_CONNECTION_STRING` (from Secret Manager)
9. Service account: Use configured service account
  </action>
  <verify>
<automated>ls -la infra/cloudrun/ 2>/dev/null || echo "infra/cloudrun/ not found"</automated>
  </verify>
  <done>Cloud Run Job configuration created in Terraform</done>
</task>

<task type="auto">
  <name>Task 2: Create Pub/Sub triggered job configuration</name>
  <files>infra/cloudrun/trigger.tf</files>
  <read_first>
- infra/cloudrun/ingestion-job.tf (from Task 1)
- infra/pubsub/topics.tf (from Plan 01)
  </read_first>
  <action>
Create Pub/Sub trigger for Cloud Run Job:
1. Trigger name: `code-ingestion-trigger`
2. Event: `google.cloud.pubsub.messagePublished`
3. Filter: `attributes.topic_id == "code-snapshot-events"`
4. Service account: Same as job service account
5. Payload: Pass full message to job

Configuration uses Cloud Scheduler or Eventarc pattern.
  </action>
  <verify>
<automated>grep -r "pubsub" infra/cloudrun/ 2>/dev/null || echo "pubsub config not found"</automated>
  </verify>
  <done>Pub/Sub trigger configured for Cloud Run Job</done>
</task>

<task type="auto">
  <name>Task 3: Implement ingestion handler in API module</name>
  <files>api/ingest/trigger.py</files>
  <read_first>
- .planning/phases/03-code-parsing-with-tree-sitter/03-RESEARCH.md
- api/ingest/chunker.py (Phase 03 implementation)
- api/db/session.py
- api/core/config.py
  </read_first>
  <action>
Create ingestion trigger handler `api/ingest/trigger.py`:
1. Function: `handle_pubsub_event(event: dict) -> dict`
2. Parses GCS object URI from Pub/Sub message
3. Downloads file content from GCS bucket using `google-cloud-storage` library
4. Invokes existing `chunker.parse_file()` from Phase 03
5. Persists chunks via `api/db/session.py` async session
6. Returns: `{status, file_path, chunk_count, error?}`

Handle failures gracefully:
- Log errors with structured logger
- Return error status for Pub/Sub retry
- Ensure idempotency (check for existing chunks before insert)
  </action>
  <verify>
<automated>grep -A 20 "def handle_pubsub_event" api/ingest/trigger.py 2>/dev/null || echo "handler not found"</automated>
  </verify>
  <done>Ingestion handler created and integrated</done>
</task>

</tasks>

<verification>
- [ ] Cloud Run Job Terraform configuration exists
- [ ] Pub/Sub trigger configured
- [ ] Ingestion handler implements GCS pull -> chunk -> persist flow
- [ ] Idempotency check in place
- [ ] Tests pass for handler logic
</verification>

<success_criteria>
- Cloud Run Job triggers successfully on Pub/Sub message
- Code content is pulled from GCS and chunked via Phase 03 logic
- Chunks persist to database
- Retry mechanism handles transient failures
</success_criteria>

<output>
After completion, create `.planning/phases/04-gcs-pubsub-ingestion-triggers/04-02-SUMMARY.md`
</output>