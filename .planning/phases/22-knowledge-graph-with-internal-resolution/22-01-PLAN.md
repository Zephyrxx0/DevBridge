---
phase: 22-knowledge-graph-with-internal-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [sql/migrations/0027_add_repo_graph_table.sql, api/db/models.py, api/db/graph_store.py, tests/test_repo_graph.py]
autonomous: true
requirements: [FR-02, DR-01]

must_haves:
  truths:
    - "repo_graph table exists in database"
    - "RepoGraph dataclass represents the graph structure"
    - "GraphStoreManager can save and load graphs using JSONB"
  artifacts:
    - path: "sql/migrations/0027_add_repo_graph_table.sql"
      provides: "Database table for graph storage"
    - path: "api/db/models.py"
      provides: "RepoGraph dataclass"
    - path: "api/db/graph_store.py"
      provides: "Graph storage management logic"
    - path: "tests/test_repo_graph.py"
      provides: "Storage layer verification"
  key_links:
    - from: "api/db/graph_store.py"
      to: "repo_graph table"
      via: "SQLAlchemy text() queries"
---

<objective>
Establish the storage foundation for the repository knowledge graph. This includes the database schema, data models, and a management service for CRUD operations on the graph.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-knowledge-graph-with-internal-resolution/22-CONTEXT.md
@.planning/phases/22-knowledge-graph-with-internal-resolution/22-RESEARCH.md
@api/db/models.py
@api/db/vector_store.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL migration for repo_graph table</name>
  <files>sql/migrations/0027_add_repo_graph_table.sql</files>
  <read_first>
    - sql/migrations/0025_bootstrap_code_intel_tables.sql (for pattern)
  </read_first>
  <action>
    Create a new migration file `sql/migrations/0027_add_repo_graph_table.sql` with the following content:
    ```sql
    -- Add repository graph storage table (FR-02, DR-01)
    CREATE TABLE IF NOT EXISTS repo_graph (
      id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
      repo TEXT UNIQUE NOT NULL,
      graph JSONB NOT NULL,
      updated_at TIMESTAMPTZ DEFAULT NOW()
    );

    CREATE INDEX IF NOT EXISTS repo_graph_repo_idx ON repo_graph (repo);
    ```
    Ensure it follows the project's migration pattern.
  </action>
  <verify>
    <automated>python apply_migration.py sql/migrations/0027_add_repo_graph_table.sql</automated>
  </verify>
  <acceptance_criteria>
    - sql/migrations/0027_add_repo_graph_table.sql exists
    - Table repo_graph exists in database with columns: id (uuid), repo (text), graph (jsonb), updated_at (timestamptz)
    - repo column has a UNIQUE constraint
    - Index repo_graph_repo_idx exists on the repo column
  </acceptance_criteria>
  <done>Database table created and ready for graph storage</done>
</task>

<task type="auto">
  <name>Task 2: Add RepoGraph model and GraphStoreManager service</name>
  <files>api/db/models.py, api/db/graph_store.py</files>
  <read_first>
    - api/db/models.py (to append new dataclass)
    - api/db/vector_store.py (for service pattern)
    - api/db/session.py (for get_engine)
  </read_first>
  <action>
    1. Append `RepoGraph` dataclass to `api/db/models.py`:
    ```python
    @dataclass
    class RepoGraph:
        repo: str
        graph: Dict[str, Any]
        updated_at: datetime
    ```
    Ensure necessary imports are present (Dict, Any, datetime).

    2. Create `api/db/graph_store.py` with `GraphStoreManager` class:
    - Implement `save_graph(repo_slug, graph_data)`: D-03 (Full Rebuild) - DELETE then INSERT.
    - Implement `get_graph(repo_slug)`: Returns `RepoGraph` or None.
    - Use `get_engine()` and `async with engine.connect() as conn:` pattern.
    - Use `text()` for SQL queries and `json.dumps(graph_data)` for storage.
  </action>
  <verify>
    <automated>python -m pytest tests/test_repo_graph.py -k storage</automated>
  </verify>
  <acceptance_criteria>
    - api/db/models.py contains RepoGraph dataclass
    - api/db/graph_store.py exists and exports GraphStoreManager
    - GraphStoreManager.save_graph implements DELETE-then-INSERT logic per D-03
    - GraphStoreManager.get_graph returns data as Dict from JSONB
  </acceptance_criteria>
  <done>Storage layer and data models implemented</done>
</task>

<task type="auto">
  <name>Task 3: Create test suite for graph storage</name>
  <files>tests/test_repo_graph.py</files>
  <read_first>
    - tests/test_vector_db.py (for test patterns)
  </read_first>
  <action>
    Create `tests/test_repo_graph.py` to verify the storage layer:
    - Test `save_graph` creates a record.
    - Test `save_graph` overwrites existing record (D-03).
    - Test `get_graph` retrieves the correct JSON structure.
    - Use mock data for the graph JSONB content.
  </action>
  <verify>
    <automated>pytest tests/test_repo_graph.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_repo_graph.py exists
    - All storage-related tests pass
    - Tests verify the UNIQUE constraint handling on repo column
  </acceptance_criteria>
  <done>Storage layer verified with automated tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Database | SQL queries for graph storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | Graph storage | mitigate | Use SQLAlchemy parameterization for all queries to prevent SQL injection. |
| T-22-02 | Denial of Service | Graph storage | mitigate | Ensure the `graph` JSONB column size is monitored; add database-level constraints if necessary in future phases. |
</threat_model>

<verification>
Check table existence: `psql $DATABASE_URL -c "\d repo_graph"`
Run storage tests: `pytest tests/test_repo_graph.py`
</verification>

<success_criteria>
1. repo_graph table is live in Supabase.
2. GraphStoreManager can persist and retrieve arbitrary JSONB graph data.
3. Tests confirm full rebuild logic (D-03).
</success_criteria>

<output>
After completion, create `.planning/phases/22-knowledge-graph-with-internal-resolution/22-01-SUMMARY.md`
</output>
