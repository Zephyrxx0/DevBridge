---
phase: 22-knowledge-graph-with-internal-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [sql/migrations/0027_add_repo_graph_table.sql, api/db/models.py, api/db/graph_store.py, tests/test_phase22_schema.py]
autonomous: true
requirements: [FR-02, DR-01]

must_haves:
  truths:
    - "repo_graph table exists in database with repo_id UUID PK (DR-01)"
    - "RepoGraph dataclass represents the graph structure with separate nodes/edges"
    - "GraphStoreManager can save and load graphs using separate JSONB columns"
  artifacts:
    - path: "sql/migrations/0027_add_repo_graph_table.sql"
      provides: "Database table for graph storage"
    - path: "api/db/models.py"
      provides: "RepoGraph dataclass"
    - path: "api/db/graph_store.py"
      provides: "Graph storage management logic"
    - path: "tests/test_phase22_schema.py"
      provides: "Storage layer verification"
  key_links:
    - from: "api/db/graph_store.py"
      to: "repo_graph table"
      via: "SQLAlchemy text() queries"
---

<objective>
Establish the storage foundation for the repository knowledge graph. This includes the database schema (aligned with DR-01), data models using UUID repo identifiers, and a management service for CRUD operations on the graph.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-knowledge-graph-with-internal-resolution/22-CONTEXT.md
@.planning/phases/22-knowledge-graph-with-internal-resolution/22-RESEARCH.md
@api/db/models.py
@api/db/vector_store.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL migration for repo_graph table</name>
  <files>sql/migrations/0027_add_repo_graph_table.sql</files>
  <read_first>
    - sql/migrations/0025_bootstrap_code_intel_tables.sql (for pattern)
  </read_first>
  <action>
    Create a new migration file `sql/migrations/0027_add_repo_graph_table.sql` following DR-01:
    ```sql
    -- Add repository graph storage table (FR-02, DR-01)
    CREATE TABLE IF NOT EXISTS repo_graph (
      repo_id UUID PRIMARY KEY REFERENCES repositories(id) ON DELETE CASCADE,
      nodes JSONB NOT NULL DEFAULT '[]',
      edges JSONB NOT NULL DEFAULT '[]',
      updated_at TIMESTAMPTZ DEFAULT NOW()
    );

    -- Index for updated_at to support maintenance tasks
    CREATE INDEX IF NOT EXISTS repo_graph_updated_at_idx ON repo_graph (updated_at);
    ```
    Note: Using `repo_id` as UUID PRIMARY KEY per DR-01 and checker feedback.
  </action>
  <verify>
    <automated>python apply_migration.py sql/migrations/0027_add_repo_graph_table.sql</automated>
  </verify>
  <acceptance_criteria>
    - sql/migrations/0027_add_repo_graph_table.sql exists
    - Table repo_graph exists in database with columns: repo_id (uuid, PK), nodes (jsonb), edges (jsonb), updated_at (timestamptz)
    - repo_id column has a FOREIGN KEY reference to repositories(id)
  </acceptance_criteria>
  <done>Database table created and ready for graph storage</done>
</task>

<task type="auto">
  <name>Task 2: Implement RepoGraph model and GraphStoreManager</name>
  <files>api/db/models.py, api/db/graph_store.py, tests/test_phase22_schema.py</files>
  <read_first>
    - api/db/models.py (to append new dataclass)
    - api/db/vector_store.py (for service pattern)
    - api/db/session.py (for get_engine)
  </read_first>
  <action>
    1. Append `RepoGraph` dataclass to `api/db/models.py`:
    ```python
    @dataclass
    class RepoGraph:
        repo_id: UUID
        nodes: list[dict]
        edges: list[dict]
        updated_at: datetime
    ```
    Ensure necessary imports are present (list, dict, datetime, UUID).

    2. Create `api/db/graph_store.py` with `GraphStoreManager` class:
    - Implement `save_graph(repo_id: UUID, nodes: list[dict], edges: list[dict])`: D-03 (Full Rebuild) - Use `ON CONFLICT (repo_id) DO UPDATE` or DELETE-then-INSERT. DR-01 requires separate columns for nodes and edges.
    - Implement `get_graph(repo_id: UUID)`: Returns `RepoGraph` or None.
    - Use `get_engine()` and `async with engine.connect() as conn:` pattern.

    3. Create `tests/test_phase22_schema.py` with storage verification tests:
    - Test `save_graph` creates a record.
    - Test `save_graph` overwrites existing record (D-03).
    - Test `get_graph` retrieves the correct JSON structure for nodes and edges separately.
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase22_schema.py</automated>
  </verify>
  <acceptance_criteria>
    - api/db/models.py contains RepoGraph dataclass with UUID repo_id
    - api/db/graph_store.py exists and exports GraphStoreManager
    - tests/test_phase22_schema.py exists and passes
    - GraphStoreManager handles separate nodes/edges columns per DR-01
  </acceptance_criteria>
  <done>Storage layer, data models, and schema tests implemented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Database | SQL queries for graph storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | Graph storage | mitigate | Use SQLAlchemy parameterization for all queries to prevent SQL injection. |
| T-22-02 | Denial of Service | Graph storage | mitigate | Ensure the `nodes` and `edges` JSONB columns are monitored; add database-level constraints if necessary. |
</threat_model>

<verification>
Check table existence: `psql $DATABASE_URL -c "\d repo_graph"`
Run storage tests: `pytest tests/test_phase22_schema.py`
</verification>

<success_criteria>
1. repo_graph table is live in Supabase with UUID PK.
2. GraphStoreManager can persist and retrieve separate nodes/edges JSONB data.
3. Tests confirm full rebuild logic (D-03).
</success_criteria>

<output>
After completion, create `.planning/phases/22-knowledge-graph-with-internal-resolution/22-01-SUMMARY.md`
</output>
