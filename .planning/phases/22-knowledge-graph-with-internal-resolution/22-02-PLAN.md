---
phase: 22-knowledge-graph-with-internal-resolution
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified: [api/ingestion/graph_builder.py, api/routes/repo.py, tests/test_phase22_extraction.py, tests/test_phase22_resolution.py, tests/test_phase22_integration.py]
autonomous: true
requirements: [FR-02]

must_haves:
  truths:
    - "Graph nodes are restricted to Files and Shadow Nodes (D-01)"
    - "Internal symbols are resolved to their source files using a global symbol map"
    - "Edges only include IMPORTS and CALLS between files (No DEFINES)"
    - "Ingestion pipeline triggers graph building automatically via repo_id"
  artifacts:
    - path: "api/ingestion/graph_builder.py"
      provides: "Graph extraction and resolution logic"
    - path: "api/routes/repo.py"
      provides: "Hook in _run_ingestion task"
    - path: "tests/test_phase22_extraction.py"
      provides: "Symbol discovery verification"
    - path: "tests/test_phase22_resolution.py"
      provides: "Relationship resolution verification"
    - path: "tests/test_phase22_integration.py"
      provides: "Pipeline integration verification"
  key_links:
    - from: "api/routes/repo.py"
      to: "api/ingestion/graph_builder.py"
      via: "Function call at end of ingestion"
---

<objective>
Implement the graph construction logic using Tree-sitter for symbol extraction and internal resolution. Adhere to file-level granularity (D-01) and integrate the builder into the ingestion pipeline using UUID repo identifiers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-knowledge-graph-with-internal-resolution/22-CONTEXT.md
@.planning/phases/22-knowledge-graph-with-internal-resolution/22-RESEARCH.md
@.planning/phases/22-knowledge-graph-with-internal-resolution/22-01-SUMMARY.md
@api/ingestion/tree_sitter_chunker.py
@api/routes/repo.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Pass 1 - Global Symbol Discovery</name>
  <files>api/ingestion/graph_builder.py, tests/test_phase22_extraction.py</files>
  <read_first>
    - api/ingestion/tree_sitter_chunker.py (for Tree-sitter usage patterns)
    - 22-RESEARCH.md (for Tree-sitter query patterns)
  </read_first>
  <action>
    1. Create `api/ingestion/graph_builder.py` with `GraphBuilder` class.
    2. Implement **Pass 1: Discovery**. 
       - Function `discover_symbols()`: Scan repository content to build a global map of `symbol -> file_path`.
       - Focus on exported classes and functions (symbols that can be imported/called from other files).
       - Use Tree-sitter queries for Python and TypeScript as defined in RESEARCH.md.
    3. Create `tests/test_phase22_extraction.py` to verify symbol extraction from sample code snippets.
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase22_extraction.py</automated>
  </verify>
  <acceptance_criteria>
    - GraphBuilder.discover_symbols correctly maps symbols to their defining file paths
    - Supports Python and TypeScript exports
    - tests/test_phase22_extraction.py passes
  </acceptance_criteria>
  <done>Global symbol discovery (Pass 1) implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement Pass 2 - Relationship Extraction and Resolution</name>
  <files>api/ingestion/graph_builder.py, tests/test_phase22_resolution.py</files>
  <action>
    1. Implement **Pass 2: Extraction & Resolution** in `GraphBuilder`.
       - Function `resolve_relationships()`: Scan each file for `IMPORTS` and `CALLS`.
       - Resolve local symbols to target files using the map from Pass 1.
       - Resolve external calls to "Shadow Nodes" if they belong to the blessed list (react, fastapi, etc.) per D-02.
       - STRICT D-01 COMPLIANCE: Graph nodes must be `Files` or `Shadow Nodes`. No symbol nodes. No `DEFINES` edges.
    2. Implement `build_graph()`:
       - Aggregate into DR-01 format: `nodes: [{"id": "...", "type": "file|shadow", "name": "...", "file_path": "..."}]`, `edges: [{"from": "...", "to": "...", "type": "IMPORTS|CALLS"}]`.
    3. Create `tests/test_phase22_resolution.py` to verify file-to-file edge creation and Shadow Node handling.
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase22_resolution.py</automated>
  </verify>
  <acceptance_criteria>
    - GraphBuilder.build_graph produces correct nodes and edges per D-01 and DR-01
    - Shadow Nodes are created only for blessed libraries
    - Unresolvable symbols are correctly dropped
    - No DEFINES edges or symbol-level nodes exist in output
  </acceptance_criteria>
  <done>Relationship resolution (Pass 2) and graph aggregation implemented</done>
</task>

<task type="auto">
  <name>Task 3: Integrate graph building into ingestion pipeline</name>
  <files>api/routes/repo.py, tests/test_phase22_integration.py</files>
  <read_first>
    - api/routes/repo.py (specifically _run_ingestion task)
    - api/db/graph_store.py (to use GraphStoreManager)
  </read_first>
  <action>
    1. Modify `api/routes/repo.py` in the `_run_ingestion` function:
       - Import `GraphBuilder` and `GraphStoreManager`.
       - After the `code_chunks` ingestion is successfully completed:
         ```python
         # Build and save knowledge graph
         try:
             # repo_id (UUID) is available in _run_ingestion scope
             builder = GraphBuilder(repo_id=repo_id, engine=engine)
             nodes, edges = await builder.build_graph()
             
             store = GraphStoreManager(engine)
             await store.save_graph(repo_id, nodes, edges)
             logger.info(f"Graph build for {repo_id} completed.")
         except Exception as graph_err:
             logger.error(f"Graph build failed for {repo_id} (non-fatal): {graph_err}")
         ```
       - Ensure `repo_id` (UUID) is used instead of slug.

    2. Create `tests/test_phase22_integration.py` to verify the hook is called during a mock ingestion run.
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase22_integration.py</automated>
  </verify>
  <acceptance_criteria>
    - api/routes/repo.py calls the graph builder with UUID repo_id
    - Graph building is non-fatal to the ingestion process
    - tests/test_phase22_integration.py passes
  </acceptance_criteria>
  <done>Knowledge graph automatically built on repository ingestion</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Code → Tree-sitter | Parsing untrusted repository code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Denial of Service | Tree-sitter Parser | mitigate | Cap file size for parsing; monitor execution time of Tree-sitter queries. |
| T-22-04 | Information Disclosure | Graph Metadata | mitigate | Ensure "Shadow Nodes" only store public library names. |
</threat_model>

<verification>
Trigger a repo ingestion via API and verify the `repo_graph` table has data for that repo_id.
Run all phase 22 tests: `pytest tests/test_phase22_*.py`
</verification>

<success_criteria>
1. Knowledge graph is populated after a successful repository index using UUID repo_id.
2. Graph contains ONLY file-level internal resolution edges (CALLS/IMPORTS).
3. Shadow Nodes represent external dependencies from the blessed list.
4. No DEFINES edges or symbol-level nodes are present.
</success_criteria>

<output>
After completion, create `.planning/phases/22-knowledge-graph-with-internal-resolution/22-02-SUMMARY.md`
</output>
