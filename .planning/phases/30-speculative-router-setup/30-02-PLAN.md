---
phase: 30
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified: [api/agents/state.py, api/agents/nodes/cascade.py]
autonomous: true
requirements: [ROUT-01, ROUT-02]
must_haves:
  truths:
    - "AgentState includes model_used and cascaded fields"
    - "CascadeAgent is configured with Fast and Big models"
    - "Big model uses ModelConfig with retries/rate-limit handling"
    - "Cascade node uses SchemaValidator for speculative checks"
    - "Metadata (model_used, cascaded) is returned from the node"
  artifacts:
    - path: "api/agents/nodes/cascade.py"
      provides: "LangGraph node for speculative execution"
    - path: "api/agents/state.py"
      provides: "Updated state schema with routing metadata"
  key_links:
    - from: "api/agents/nodes/cascade.py"
      to: "api/agents/utils/validation.py"
      via: "import"
      pattern: "SchemaValidator"
    - from: "api/agents/nodes/cascade.py"
      to: "cascadeflow"
      via: "implementation"
      pattern: "CascadeAgent"
---

<objective>
Implement the speculative router node using Cascadeflow and update the agent state to track model usage. This node will attempt to use the Fast model first, validate the output, and escalate to the Big model only if validation fails.

Purpose: Optimize resource usage (VRAM/Cost) and track routing decisions.
Output: Integrated speculative routing node and updated state.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-speculative-router-setup/30-01-SUMMARY.md
@.planning/phases/30-speculative-router-setup/30-RESEARCH.md
@api/agents/state.py
@api/agents/utils/llm.py
@api/agents/utils/validation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AgentState</name>
  <files>api/agents/state.py</files>
  <read_first>api/agents/state.py</read_first>
  <action>
    - Add `model_used: Optional[str]` to `AgentState` in api/agents/state.py.
    - Add `cascaded: bool` (default False) to `AgentState`.
    - These fields will store metadata from the speculative router for use in Phase 32 UX.
  </action>
  <verify>
    <automated>python -c "from api.agents.state import AgentState; print('AgentState updated')"</automated>
  </verify>
  <acceptance_criteria>
    - api/agents/state.py contains `model_used` and `cascaded` fields.
  </acceptance_criteria>
  <done>State schema updated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement cascade node with rate-limiting</name>
  <files>api/agents/nodes/cascade.py</files>
  <read_first>api/agents/nodes/fast.py, api/agents/utils/llm.py, api/agents/utils/validation.py</read_first>
  <action>
    - Create api/agents/nodes/cascade.py.
    - Import `CascadeAgent`, `ModelConfig` from `cascadeflow`.
    - Import `get_model` from `api.agents.utils.llm`.
    - Import `SchemaValidator` from `api.agents.utils.validation`.
    - Configure `CascadeAgent`:
        - Model 1 (Fast): "fast" (via get_model(is_fast=True))
        - Model 2 (Big): "big" (via get_model(is_fast=False))
        - Per ROUT-02: For the "Big" model (Gemini 2.5 Flash), use `ModelConfig` to wrap the LLM with retry logic (e.g., using `langchain.util.retry` or equivalent if supported by cascadeflow, otherwise standard LangChain retry params).
    - Implement `async def cascade_node(state: AgentState) -> dict`:
        - Extract messages from state.
        - Run `cascade_agent.run(messages)`.
        - Extract the final response and metadata:
            - `final_response`: The message from the last model used.
            - `model_used`: ID/Name of the model that provided the final response.
            - `cascaded`: True if more than one model was used (escalation occurred).
        - Return dictionary updating `messages`, `model_used`, and `cascaded`.
  </action>
  <verify>
    <automated>pytest tests/test_phase30_routing.py</automated>
  </verify>
  <acceptance_criteria>
    - api/agents/nodes/cascade.py exists.
    - Tests for gemma_path and escalation_path pass.
    - Tests verify that `model_used` and `cascaded` are correctly set in the return dict.
    - Big model configuration includes retry/rate-limit handling.
  </acceptance_criteria>
  <done>Speculative node implemented with metadata propagation and rate-limit handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CascadeAgent -> Remote LLM | Sensitive messages are sent to Google AI Studio. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02 | Information Disclosure | CascadeAgent | accept | Data is sent to a trusted third-party (Google) as per architectural decision D-30-01. |
</threat_model>

<success_criteria>
- Cascade node correctly routes between models based on validation.
- Metadata (model_used, cascaded) is correctly captured and returned.
- Rate-limit handling is configured for the Big model.
</success_criteria>

<output>
After completion, create `.planning/phases/30-speculative-router-setup/30-02-SUMMARY.md`
</output>
