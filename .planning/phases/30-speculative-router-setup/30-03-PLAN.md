---
phase: 30
plan: 03
type: execute
wave: 3
depends_on: [30-02]
files_modified: [api/agents/graph.py]
autonomous: true
requirements: [ROUT-01, ROUT-02]
must_haves:
  truths:
    - "LangGraph uses cascade_node instead of manual router/worker nodes"
    - "Recall and Retain nodes are correctly wired to the cascade node"
    - "System handles end-to-end chat requests with speculative routing"
    - "Model metadata (model_used, cascaded) is persisted in the graph state"
  artifacts:
    - path: "api/agents/graph.py"
      provides: "Updated agent workflow with speculative execution"
  key_links:
    - from: "api/agents/graph.py"
      to: "api/agents/nodes/cascade.py"
      via: "node registration"
      pattern: "add_node.*cascade"
---

<objective>
Integrate the cascade node into the main LangGraph agent workflow, replacing the legacy router and worker nodes.

Purpose: Finalize the migration to speculative routing and ensure metadata is correctly propagated through the graph.
Output: Production-ready speculative agent graph.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-speculative-router-setup/30-02-SUMMARY.md
@api/agents/graph.py
@api/agents/nodes/cascade.py
@api/agents/state.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update LangGraph wiring</name>
  <files>api/agents/graph.py</files>
  <read_first>api/agents/graph.py, api/agents/nodes/cascade.py, api/agents/state.py</read_first>
  <action>
    - Modify api/agents/graph.py.
    - Import `cascade_node` from `api.agents.nodes.cascade`.
    - Remove imports for `big_worker_node`, `fast_worker_node`, and `intent_classifier`.
    - Update graph builder:
        - builder.add_node("cascade", cascade_node)
        - Remove "router", "fast_worker", "big_worker" nodes.
    - Update edges:
        - builder.add_edge("recall", "cascade")
        - builder.add_edge("cascade", "retain")
        - Remove conditional edges from router.
    - Per D-30-02: Ensure Cascadeflow handles the routing logic, simplifying the graph structure.
    - Verify that `AgentState` in the graph correctly handles the new `model_used` and `cascaded` fields returned by `cascade_node`.
  </action>
  <verify>
    <automated>pytest tests/test_phase21_e2e.py</automated>
  </verify>
  <acceptance_criteria>
    - api/agents/graph.py uses cascade_node.
    - No "router" or "worker" nodes in the graph builder.
    - E2E chat tests pass (using speculative routing under the hood).
  </acceptance_criteria>
  <done>Graph integrated with speculative routing.</done>
</task>

<task type="auto">
  <name>Task 2: Final verification</name>
  <files>tests/test_phase30_routing.py</files>
  <read_first>tests/test_phase30_routing.py</read_first>
  <action>
    - Run the full test suite including the new phase 30 tests and existing E2E tests.
    - Ensure that model metadata (`model_used`, `cascaded`) is correctly propagated to the final graph output.
    - Verify that no regressions occurred in memory recall/retain logic.
  </action>
  <verify>
    <automated>pytest tests/test_phase30_routing.py tests/test_phase21_e2e.py</automated>
  </verify>
  <acceptance_criteria>
    - All tests pass.
    - Total test runtime is within limits (~15s).
    - Tests confirm `model_used` and `cascaded` are present in final state.
  </acceptance_criteria>
  <done>Phase 30 complete and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Graph -> Memory | Cascade node output is persisted to pgvector via Retain node. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Information Disclosure | Memory | mitigate | Use authenticated `user_id` context in Recall/Retain nodes (established in Phase 29) to ensure speculative results are isolated per user. |
</threat_model>

<success_criteria>
- Speculative routing is the default behavior for all agent interactions.
- Legacy nodes are removed from the active graph.
- Model usage metadata is persisted and available for future Phase 32 UX work.
</success_criteria>

<output>
After completion, create `.planning/phases/30-speculative-router-setup/30-03-SUMMARY.md`
</output>
