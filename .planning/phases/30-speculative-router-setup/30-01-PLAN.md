---
phase: 30
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api/requirements.txt, api/agents/utils/validation.py, tests/test_phase30_routing.py]
autonomous: true
requirements: [ROUT-01]
must_haves:
  truths:
    - "System has cascadeflow library installed"
    - "System defines Pydantic schemas for speculative validation"
    - "Test scaffold exists with failing tests for routing scenarios"
  artifacts:
    - path: "api/agents/utils/validation.py"
      provides: "Validation logic for Cascadeflow"
    - path: "tests/test_phase30_routing.py"
      provides: "Automated verification for model escalation"
  key_links:
    - from: "api/agents/utils/validation.py"
      to: "cascadeflow"
      via: "inheritance"
      pattern: "CustomValidator"
---

<objective>
Setup the foundation for speculative routing by installing dependencies, defining validation schemas, and creating a test scaffold.

Purpose: Ensure the system can validate "Fast" model drafts and has a testing ground for escalation logic.
Output: Validation utility and test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-speculative-router-setup/30-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create validation schema</name>
  <files>api/requirements.txt, api/agents/utils/validation.py</files>
  <read_first>api/requirements.txt, api/agents/utils/validation.py</read_first>
  <action>
    - Add "cascadeflow[langchain]==1.1.0" to api/requirements.txt.
    - Create api/agents/utils/validation.py per D-30-01.
    - Implement `ValidationSchema` (Pydantic BaseModel) with fields: `content: str`, `is_complete: bool`, `confidence: float`.
    - Implement `SchemaValidator` class inheriting from `cascadeflow.quality.CustomValidator`.
    - The `validate` method should attempt to parse the response content using `ValidationSchema`.
    - If parsing fails or `is_complete` is False, return `CustomValidationResult(passed=False, score=0.0, reason="Schema mismatch or incomplete")`.
    - Otherwise, return `CustomValidationResult(passed=True, score=1.0)`.
  </action>
  <verify>
    <automated>pip show cascadeflow && python -c "from api.agents.utils.validation import SchemaValidator; print('Import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - cascadeflow is installed.
    - api/agents/utils/validation.py contains SchemaValidator class.
    - grep -c "CustomValidator" api/agents/utils/validation.py returns 1.
  </acceptance_criteria>
  <done>Validation schema and library ready.</done>
</task>

<task type="auto">
  <name>Task 2: Create test scaffold</name>
  <files>tests/test_phase30_routing.py</files>
  <read_first>api/agents/state.py</read_first>
  <action>
    - Create tests/test_phase30_routing.py.
    - Define tests:
        - `test_gemma_path`: Mocks Fast model returning valid schema, verifies No escalation.
        - `test_escalation_path`: Mocks Fast model returning malformed text, verifies Big model is invoked.
    - Use `monkeypatch` to mock `api.agents.utils.llm.get_model`.
    - Ensure tests assert that `model_used` and `cascaded` metadata is present in the output (preparation for Task 30-02).
  </action>
  <verify>
    <automated>pytest tests/test_phase30_routing.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_phase30_routing.py exists.
    - Tests run (expect failures if implementation not finished, but scaffold must be valid python).
  </acceptance_criteria>
  <done>Test suite established.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fast Model -> Validator | Untrusted text from Fast model is parsed by Pydantic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | Validator | mitigate | Use Pydantic's strict parsing and schema enforcement to reject malformed Fast model outputs. |
</threat_model>

<success_criteria>
- Library installed.
- Validator implemented.
- Tests ready.
</success_criteria>

<output>
After completion, create `.planning/phases/30-speculative-router-setup/30-01-SUMMARY.md`
</output>
