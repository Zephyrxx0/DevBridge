---
phase: 30
plan: 04
type: execute
wave: 4
depends_on: ["30-03"]
files_modified: ["api/agents/nodes/cascade.py", "tests/test_phase30_routing.py"]
autonomous: true
requirements: ["ROUT-01", "ROUT-02"]
gap_closure: true

must_haves:
  truths:
    - "Cascade node executes a second turn with the big model when the fast model's response fails schema validation."
    - "The final response returned by the cascade node is the big-model's output after a validation-triggered escalation."
    - "Routing tests prove escalation by mocking a failing fast-model response and verifying big-model invocation."
  artifacts:
    - path: "api/agents/nodes/cascade.py"
      provides: "Native Cascadeflow escalation with wired validator"
    - path: "tests/test_phase30_routing.py"
      provides: "Robust escalation path verification"
  key_links:
    - from: "api/agents/nodes/cascade.py"
      to: "api/agents/utils/validation.py"
      via: "validators argument in CascadeAgent"
---

<objective>
Close the escalation gap in Phase 30. Ensure that validation failures on the fast model trigger a real re-execution with the big model (Gemini 2.5 Flash), and that tests accurately verify this behavior.

Purpose: Guarantee high-quality responses by automatically escalating to the big model when the fast model produces invalid schemas.
Output: Updated cascade node and robust routing tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-speculative-router-setup/30-03-SUMMARY.md
@.planning/phases/30-speculative-router-setup/30-VERIFICATION.md
@api/agents/nodes/cascade.py
@tests/test_phase30_routing.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire native validator and implement real escalation</name>
  <files>api/agents/nodes/cascade.py</files>
  <read_first>api/agents/nodes/cascade.py</read_first>
  <action>
    Modify `api/agents/nodes/cascade.py`:
    1. Update `CascadeAgent` instantiation to include `validators=[_schema_validator]`.
    2. In `cascade_node`, remove the manual metadata patch (lines 55-58) which only sets `model_used` and `cascaded` flags without re-running the turn.
    3. Ensure `cascade_agent.run` is called such that it uses the wired validator to automatically handle escalation. If `cascadeflow` 1.1.0 requires explicit quality gate configuration, apply it.
    4. Ensure `model_used` and `cascaded` are correctly extracted from the `result` object returned by `cascade_agent.run`.
  </action>
  <verify>
    <automated>grep -n "validators=\[.*_schema_validator.*\]" api/agents/nodes/cascade.py</automated>
  </verify>
  <acceptance_criteria>
    - `CascadeAgent` constructor includes `validators=[_schema_validator]`.
    - Manual metadata-only fallback block is removed from `cascade_node`.
    - `model_used` and `cascaded` are derived from the `result` of `cascade_agent.run`.
  </acceptance_criteria>
  <done>Cascade node uses native Cascadeflow validation for true turn escalation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement robust escalation tests</name>
  <files>tests/test_phase30_routing.py</files>
  <read_first>tests/test_phase30_routing.py</read_first>
  <action>
    Rewrite `tests/test_phase30_routing.py` to move beyond simple monkeypatching of the result:
    1. Create a `MockCascadeAgent` that simulates the cascade logic if `cascadeflow` internals are hard to mock, OR mock the LLM calls made by `CascadeAgent`.
    2. Specifically for `test_escalation_path`:
       - Mock the first call (fast model) to return an invalid JSON (e.g., `{"wrong": "schema"}`).
       - Mock the second call (big model) to return a valid JSON (e.g., `{"content": "valid", "is_complete": true, "confidence": 1.0}`).
       - Verify that `cascade_node` returns the content from the second (big model) call.
       - Verify that `cascaded` is `True` and `model_used` matches the big model name.
    3. Ensure `test_gemma_path` still passes by mocking a valid fast model response.
  </action>
  <verify>
    <automated>pytest tests/test_phase30_routing.py</automated>
  </verify>
  <acceptance_criteria>
    - `test_escalation_path` fails if the escalation logic in `cascade_node` is removed.
    - `test_escalation_path` passes when escalation works and returns the big-model output.
    - Tests verify the actual content replacement, not just metadata flags.
  </acceptance_criteria>
  <done>Routing tests prove real validation-triggered escalation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fast Model -> Validator | Untrusted (potential hallucination) output from fast model enters validator. |
| Validator -> Big Model | Validation failure triggers escalation to big model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04-01 | Tampering | `cascade_node` | mitigate | Ensure validator cannot be bypassed by malicious fast-model output. Use Pydantic strict validation. |
| T-30-04-02 | Denial of Service | `cascade_node` | mitigate | Prevent infinite escalation loops. `CascadeAgent` should limit to 1 escalation per turn. |
| T-30-04-03 | Elevation of Privilege | Escalation Path | mitigate | Ensure escalated turn uses the same `user_id` and security context as the original turn. (Handled by passing `provider_messages` directly). |
</threat_model>

<verification>
`pytest tests/test_phase30_routing.py` green with real escalation logic proof.
</verification>

<success_criteria>
- Cascade node performs re-execution with big model on validation failure.
- Tests prove that final response is replaced by big-model output during escalation.
</success_criteria>

<output>
After completion, create `.planning/phases/30-speculative-router-setup/30-04-SUMMARY.md`
</output>
