---
phase: 28
plan: 04
type: execute
wave: 3
depends_on: [28-03]
files_modified: [web/src/components/chat/ChatStream.tsx, web/src/components/chat/ChatInput.tsx, web/src/app/repo/[id]/page.tsx]
autonomous: true
requirements: [FR-07]
must_haves:
  truths:
    - "Chat stream renders using Vercel AI Elements"
    - "Chat input supports multi-line text and attachments"
    - "Repo workspace is fully migrated to modular components"
  artifacts:
    - path: "web/src/components/chat/ChatStream.tsx"
      provides: "AI-native message stream rendering"
    - path: "web/src/components/chat/ChatInput.tsx"
      provides: "Rich chat input with attachments"
    - path: "web/src/app/repo/[id]/page.tsx"
      provides: "Migrated workspace entry point"
  key_links:
    - from: "web/src/app/repo/[id]/page.tsx"
      to: "web/src/components/chat/ChatStream.tsx"
      via: "Component composition"
---

<objective>
Implement the core chat experience using Vercel AI Elements and the new layout components.

Purpose: Provide a modern, rich, and interactive chat interface with professional streaming feedback.
Output: New ChatStream and ChatInput components integrated into the redesigned RepoWorkspacePage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-CONTEXT.md
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-00-SUMMARY.md
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AI-native ChatStream component</name>
  <files>web/src/components/chat/ChatStream.tsx</files>
  <action>
    - Build `ChatStream.tsx` using `Conversation`, `Message`, and `MessageResponse` from `ai-elements` (D-03).
    - Map `messages` state from the workspace to the AI Elements components.
    - Implement smooth scrolling and streaming feedback using the `Conversation` primitive.
    - Style messages according to the brand tokens (Orange Flame accent).
  </action>
  <verify>
    <automated>cd web && npx playwright test tests/chat.spec.ts</automated>
  </verify>
  <done>Chat stream implemented with rich rendering and auto-scroll management.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AI-native ChatInput component</name>
  <files>web/src/components/chat/ChatInput.tsx</files>
  <action>
    - Build `ChatInput.tsx` using `PromptInput` and `Attachments` from `ai-elements` (D-03).
    - Integrate `Attachments` for adding code context (files/snippets) to the prompt.
    - Ensure multi-line support and professional focus states.
  </action>
  <verify>
    <automated>cd web && npx playwright test tests/chat.spec.ts</automated>
  </verify>
  <done>Chat input overhauled with multi-line support and rich attachment management.</done>
</task>

<task type="auto">
  <name>Task 3: Update RepoWorkspacePage to use new components</name>
  <files>web/src/app/repo/[id]/page.tsx</files>
  <action>
    - Replace the monolithic UI in `RepoWorkspacePage` with `ChatLayout`, `HistorySidebar`, `FileExplorer`, `ChatStream`, and `ChatInput`.
    - Wire the `useChat` hook (or equivalent SSE logic) into the new components.
    - Remove legacy chat UI code from the page.
    - Apply "Soft UI Transitions" when switching between chat threads.
  </action>
  <verify>
    <automated>cd web && npx playwright test tests/chat.spec.ts</automated>
  </verify>
  <done>Repo Workspace fully migrated to the new modular, AI-native architecture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client-side | Handling of streaming data and attachments |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | I | Attachments | mitigate | Ensure large attachments don't block the UI thread; use streaming for content if possible. |
</threat_model>

<verification>
Verify chat streaming and sidebar navigation in Playwright.
</verification>

<success_criteria>
1. Chat area uses AI Elements components.
2. Messages stream into the view with auto-scroll.
3. Attachments can be added to the input.
4. "Soft UI Transitions" are visible during layout changes.
</success_criteria>

<output>
After completion, create `.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-04-SUMMARY.md`
</output>
