---
phase: 28
plan: 01
type: execute
wave: 1
depends_on: [28-00]
files_modified: [web/src/app/layout.tsx, web/src/app/globals.css, web/src/components/layout/AppSidebar.tsx]
autonomous: true
requirements: [FR-07]
must_haves:
  truths:
    - "Root layout supports system theme synchronization"
    - "Design tokens for spacing and color are defined in CSS"
    - "Sidebar skeleton is present and collapsible"
  artifacts:
    - path: "web/src/app/layout.tsx"
      provides: "Global layout and theme provider"
    - path: "web/src/app/globals.css"
      provides: "Design system tokens"
    - path: "web/src/components/layout/AppSidebar.tsx"
      provides: "Main navigation sidebar"
  key_links:
    - from: "web/src/app/layout.tsx"
      to: "ThemeProvider"
      via: "enableSystem prop"
---

<objective>
Implement global layout foundation, theme synchronization, and design tokens.

Purpose: Establish the "Modern Premium Polish" foundation and ensure consistent design across the application.
Output: Updated layout with system theme sync, standardized design tokens in CSS, and a new Sidebar skeleton.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-CONTEXT.md
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-UI-SPEC.md
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-00-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update RootLayout for theme sync and transitions</name>
  <files>web/src/app/layout.tsx</files>
  <action>
    - Update `ThemeProvider` to set `enableSystem={true}` and `defaultTheme="system"` per D-05.
    - Wrap `{children}` in a Framer Motion `AnimatePresence` or a layout transition wrapper to support "Soft UI Transitions" (D-04).
    - Ensure Geist Sans and Geist Mono are correctly applied as CSS variables.
  </action>
  <verify>
    <automated>cd web && npx playwright test tests/theme.spec.ts</automated>
  </verify>
  <done>Root layout supports system theme synchronization and provides transition infrastructure.</done>
</task>

<task type="auto">
  <name>Task 2: Align Design Tokens in globals.css</name>
  <files>web/src/app/globals.css</files>
  <action>
    - Update spacing tokens to match 28-UI-SPEC.md (xs: 4px, sm: 8px, md: 16px, lg: 24px, xl: 32px, 2xl: 48px, 3xl: 64px).
    - Ensure OKLCH colors match the spec (Dominant: Near-Black, Accent: Orange Flame).
    - Define "Soft UI Transitions" keyframes and utility classes (D-04).
  </action>
  <verify>
    <automated>grep -q "\-\-space-xs: 4px" web/src/app/globals.css || grep -q "\-\-space-xs: 0.25rem" web/src/app/globals.css</automated>
  </verify>
  <done>Global CSS tokens aligned with the UI design contract.</done>
</task>

<task type="auto">
  <name>Task 3: Implement AppSidebar skeleton</name>
  <files>web/src/components/layout/AppSidebar.tsx</files>
  <action>
    - Create a new `AppSidebar` component using shadcn/ui `Sidebar` primitives.
    - Implement the "Sidebar History" skeleton on the left (D-02).
    - Ensure it is collapsible and styled using the new surface and border tokens.
  </action>
  <verify>
    <automated>cd web && npx playwright test tests/chat.spec.ts</automated>
  </verify>
  <done>Sidebar skeleton implemented and ready for thread history integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client-side | Global styling and layout transitions |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | — | layout.tsx | accept | Low risk structural change. |
</threat_model>

<verification>
Check `globals.css` for updated tokens and `layout.tsx` for `enableSystem`. Run theme smoke tests.
</verification>

<success_criteria>
1. Spacing tokens match the 8-point grid principle from UI-SPEC.
2. Root layout syncs with system theme preference.
3. AppSidebar skeleton exists in the components directory.
</success_criteria>

<output>
After completion, create `.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-01-SUMMARY.md`
</output>
