---
phase: 28
plan: 06
type: execute
wave: 5
depends_on: [28-02, 28-05]
files_modified: [web/src/app/globals.css, web/src/components/layout/AppSidebar.tsx, web/src/app/repo/[id]/page.tsx]
autonomous: true
requirements: [FR-07]
must_haves:
  truths:
    - "Global spacing and typography strictly follow the design system tokens"
    - "Core chat workspace is fully responsive and optimized for mobile viewports"
    - "Knowledge graph is updated with the final architectural state"
  artifacts:
    - path: "web/src/app/globals.css"
      provides: "Finalized design tokens"
    - path: "graphify-out/GRAPH_REPORT.md"
      provides: "Updated codebase knowledge graph"
  key_links:
    - from: "web/src/components/layout/AppSidebar.tsx"
      to: "Mobile Viewport"
      via: "Responsive sheet/drawer logic"
---

<objective>
Finalize the UI overhaul with a global design audit and mobile optimization.

Purpose: Ensure a cohesive, professional experience across all devices and resolutions.
Output: Refined spacing, typography, and responsive behavior globally.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-CONTEXT.md
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-UI-SPEC.md
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global Spacing and Typography Audit</name>
  <files>web/src/app/globals.css, web/src/components/ui/</files>
  <action>
    - Audit all core pages (Dashboard, Settings, Repo Workspace) to ensure strict adherence to the new spacing scale (8-point grid).
    - Ensure Geist Sans is correctly applied for body text and Geist Mono for code blocks.
    - Standardize font sizes across all common UI elements (labels, tooltips, buttons) per UI-SPEC.
  </action>
  <verify>
    <automated>cd web && npm run test</automated>
  </verify>
  <done>Global UI elements consistent with the standardized design system tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile Responsiveness Optimization and Graph Update</name>
  <files>web/src/components/layout/AppSidebar.tsx, web/src/app/repo/[id]/page.tsx</files>
  <action>
    - Optimize the Chat Sidebar for mobile (convert to a sheet or drawer).
    - Ensure the Chat Stream and Input are usable on small screens without layout breakage.
    - Tweak responsive breakpoints for the Landing Page hero and feature grid.
    - Implement touch-friendly interaction targets for core chat actions.
    - Run `graphify update .` to refresh the knowledge graph with the new UI architecture.
  </action>
  <verify>
    <automated>cd web && npx playwright test tests/landing.spec.ts tests/chat.spec.ts && graphify update .</automated>
  </verify>
  <done>Core user flows fully responsive and optimized for mobile devices; knowledge graph updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client-side | Global UI consistency |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06-01 | — | Global UI | accept | Aesthetic and responsive refinements. |
</threat_model>

<verification>
Perform manual visual regression checks on mobile resolutions and run final E2E suites.
</verification>

<success_criteria>
1. All core pages follow the spacing and typography spec.
2. Chat interface is fully functional on mobile viewports.
3. Landing page sections wrap correctly on small screens.
</success_criteria>

<output>
After completion, create `.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-06-SUMMARY.md`
</output>
