---
phase: 28
plan: 05
type: execute
wave: 4
depends_on: [28-04]
files_modified: [web/src/components/chat/ChatStream.tsx, web/src/components/chat/ArtifactViewer.tsx]
autonomous: true
requirements: [FR-07]
must_haves:
  truths:
    - "Agent reasoning and tool calls are visible in the message stream"
    - "Interactive artifacts and JSX previews are integrated"
    - "Inline citations correctly reference source context"
  artifacts:
    - path: "web/src/components/chat/ArtifactViewer.tsx"
      provides: "Side-by-side or inline preview for agent content"
    - path: "web/src/components/chat/ChatStream.tsx"
      provides: "Enhanced stream with reasoning components"
  key_links:
    - from: "web/src/components/chat/ChatStream.tsx"
      to: "web/src/components/chat/ArtifactViewer.tsx"
      via: "Rendering condition"
---

<objective>
Enhance the chat interface with advanced AI feedback and interactive artifacts.

Purpose: Visualize agent reasoning, tool usage, and provide interactive previews for generated content.
Output: Integrated ToolCall, Progress, Reasoning, Artifact, and JSXPreview components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-CONTEXT.md
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Agent Feedback Components</name>
  <files>web/src/components/chat/ChatStream.tsx</files>
  <action>
    - Integrate `ToolCall`, `Progress`, and `Reasoning` (Chain of Thought) from `ai-elements` into the message stream (D-03).
    - Map agent status and tool call updates from the AI SDK state to these components.
    - Implement `Inline Citation` to reference files and line ranges within assistant responses.
  </action>
  <verify>
    <automated>cd web && npx playwright test tests/chat.spec.ts</automated>
  </verify>
  <done>Agent reasoning and tool usage visualized in the chat interface.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Artifact and JSXPreview integration</name>
  <files>web/src/components/chat/ArtifactViewer.tsx, web/src/components/chat/ChatStream.tsx</files>
  <action>
    - Build `ArtifactViewer.tsx` using the `Artifact` component from `ai-elements`.
    - Integrate `JSXPreview` to live-render React code generated by the agent.
    - Wire these into the `ChatStream` to display rich content side-by-side or inline.
    - Ensure `shiki` is used for code highlighting within artifacts.
  </action>
  <verify>
    <automated>cd web && npx playwright test tests/chat.spec.ts</automated>
  </verify>
  <done>Rich content and interactive UI previews enabled for agent-generated code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → User | Execution of agent-provided JSX code |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05-01 | T | JSXPreview | mitigate | Pass only sanitized, allow-listed components to `JSXPreview`. Avoid evaluating raw `eval()` or dangerous script tags. |
</threat_model>

<verification>
Test agent responses with simulated tool calls and JSX output in a dev environment.
</verification>

<success_criteria>
1. Tool usage and "Thinking" states are visible during generation.
2. Code blocks can be viewed as "Artifacts".
3. React code blocks are rendered in `JSXPreview`.
</success_criteria>

<output>
After completion, create `.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-05-SUMMARY.md`
</output>
