---
phase: 28
plan: 03
type: execute
wave: 2
depends_on: [28-01]
files_modified: [web/src/components/chat/HistorySidebar.tsx, web/src/components/chat/FileExplorer.tsx, web/src/components/chat/ChatLayout.tsx]
autonomous: true
requirements: [FR-07]
must_haves:
  truths:
    - "History and File Explorer are standalone modular components"
    - "ChatLayout implements the Sidebar History + Center Chat structure"
    - "Framer Motion transitions are applied between view states"
  artifacts:
    - path: "web/src/components/chat/HistorySidebar.tsx"
      provides: "Chat session history UI"
    - path: "web/src/components/chat/FileExplorer.tsx"
      provides: "Repository file tree navigation"
    - path: "web/src/components/chat/ChatLayout.tsx"
      provides: "New workspace structural layout"
  key_links:
    - from: "web/src/components/chat/ChatLayout.tsx"
      to: "web/src/components/chat/HistorySidebar.tsx"
      via: "Component placement"
---

<objective>
Decompose the monolithic Chat Workspace into modular, reusable components.

Purpose: Improve maintainability and enable the implementation of the new layout (Sidebar History + Center Chat).
Output: Extracted HistorySidebar, FileExplorer, and a high-level ChatLayout component.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-CONTEXT.md
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-01-SUMMARY.md
@web/src/app/repo/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract HistorySidebar and FileExplorer</name>
  <files>web/src/components/chat/HistorySidebar.tsx, web/src/components/chat/FileExplorer.tsx</files>
  <action>
    - Extract the chat session history logic and UI from `web/src/app/repo/[id]/page.tsx` into `HistorySidebar.tsx`.
    - Extract the file tree navigation logic and UI into `FileExplorer.tsx`.
    - Ensure these components use shadcn/ui `Sidebar` primitives where appropriate.
    - Props should include necessary callbacks (e.g., `onSelectSession`, `onSelectFile`).
  </action>
  <verify>
    <automated>cd web && npx playwright test tests/chat.spec.ts</automated>
  </verify>
  <done>Chat history and file exploration logic modularized into separate components.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ChatLayout component</name>
  <files>web/src/components/chat/ChatLayout.tsx</files>
  <action>
    - Create a `ChatLayout` component that implements the "Sidebar History + Center Chat" structure (D-02).
    - Sidebar on the left (collapsible), focused chat area in the center.
    - Use Framer Motion for soft transitions between different view states.
    - Ensure layout handles mobile responsiveness (e.g., sidebar becoming a drawer).
  </action>
  <verify>
    <automated>cd web && npx playwright test tests/chat.spec.ts</automated>
  </verify>
  <done>High-level chat layout component implemented with modern responsiveness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client-side | Component refactoring and state passing |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | — | Chat Layout | accept | Structural refactor. |
</threat_model>

<verification>
Verify component exports and layout structure in tests.
</verification>

<success_criteria>
1. HistorySidebar and FileExplorer are separate files.
2. ChatLayout correctly positions the sidebar on the left and chat in the center.
3. Components use the updated global design tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-03-SUMMARY.md`
</output>
