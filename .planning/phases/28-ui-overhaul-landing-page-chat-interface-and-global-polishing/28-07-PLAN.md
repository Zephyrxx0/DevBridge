---
phase: 28
plan: 07
type: execute
wave: 6
depends_on: [28-06]
files_modified: [web/src/app/globals.css, web/src/components/chat/ChatStream.tsx, web/src/app/admin/page.tsx, web/src/app/repo/[id]/onboarding/page.tsx, web/package.json]
autonomous: true
requirements: [FR-07]
must_haves:
  truths:
    - "Performance and accessibility audits pass for core pages"
    - "Onboarding and Admin UIs are consistent with the new design tokens"
    - "Offline and disconnect states are handled with clear UI feedback"
  artifacts:
    - path: "web/src/components/ui/ResilienceHandler.tsx"
      provides: "Offline and disconnect UI feedback"
    - path: "web/src/components/chat/FeedbackButtons.tsx"
      provides: "Agent response quality feedback loop"
  key_links:
    - from: "web/src/app/layout.tsx"
      to: "ResilienceHandler"
      via: "Global component inclusion"
---

<objective>
Finalize the UI overhaul with resilience features, performance optimization, and cross-feature UI synchronization.

Purpose: Ensure the application is accessible, performant, and consistently polished across all functional modules.
Output: Optimized bundle, accessibility fixes, synchronized Admin/Onboarding UIs, and resilience UX handlers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-CONTEXT.md
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-UI-SPEC.md
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Performance & Accessibility Audit</name>
  <files>web/package.json, web/src/app/globals.css</files>
  <action>
    - Audit bundle size and implement dynamic imports for heavy libraries like `shiki` or `@paper-design/shaders-react`.
    - Run `axe-core` accessibility audits on Landing and Chat pages.
    - Fix identified a11y issues: ARIA labels, focus traps in modals/sidebars, and color contrast.
    - Implement a `Skip to Content` link for keyboard navigation.
  </action>
  <verify>
    <automated>cd web && npm run build</automated>
  </verify>
  <done>Core pages meet accessibility standards and performance budget.</done>
</task>

<task type="auto">
  <name>Task 2: UI Synchronization (Onboarding & Admin)</name>
  <files>web/src/app/admin/page.tsx, web/src/app/repo/[id]/onboarding/page.tsx</files>
  <action>
    - Update the Onboarding UI (Phase 23) to use the new design tokens (OKLCH colors, Geist typography, 8pt spacing).
    - Update the Admin Dashboard (Phase 26) to match the "Premium Polish" aesthetic.
    - Ensure consistent use of `Card`, `Button`, and `Input` components across these modules.
  </action>
  <verify>
    <automated>cd web && npx playwright test tests/chat.spec.ts</automated>
  </verify>
  <done>Extended application modules (Admin, Onboarding) aligned with the new global design system.</done>
</task>

<task type="auto">
  <name>Task 3: Resilience UX & Feedback Loop</name>
  <files>web/src/components/ui/ResilienceHandler.tsx, web/src/components/chat/ChatStream.tsx</files>
  <action>
    - Implement `ResilienceHandler.tsx` to manage offline states and SSE/Socket disconnect notifications.
    - Add "Helpful/Not Helpful" feedback buttons to agent messages in `ChatStream.tsx`.
    - Standardize `Skeleton` loading states for initial repo indexing and thread fetching.
  </action>
  <verify>
    <automated>ls web/src/components/ui/ResilienceHandler.tsx</automated>
  </verify>
  <done>Application gracefully handles connectivity issues and captures user feedback on agent quality.</done>
</task>

<task type="auto">
  <name>Task 4: SEO & Meta Overhaul</name>
  <files>web/src/app/page.tsx, web/src/app/layout.tsx</files>
  <action>
    - Update Next.js `metadata` in the root layout and landing page.
    - Add descriptive `title`, `description`, and OpenGraph images for social sharing.
    - Ensure semantic HTML usage (`main`, `nav`, `footer`) for better SEO and accessibility.
  </action>
  <verify>
    <automated>grep -q "metadata =" web/src/app/page.tsx</automated>
  </verify>
  <done>Application optimized for search engines and social discovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client-side | SEO and Accessibility metadata |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07-01 | — | Meta tags | accept | Low risk configuration. |
</threat_model>

<verification>
Check `metadata` exports and run `npm run build` to verify performance.
</verification>

<success_criteria>
1. Accessibility audit score > 90 on Lighthouse.
2. Admin and Onboarding UIs use the new theme variables.
3. Disconnect UI appears when network is toggled offline.
4. Metadata tags are present in the document head.
</success_criteria>

<output>
After completion, create `.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-07-SUMMARY.md`
</output>
