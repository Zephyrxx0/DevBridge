---
phase: 28
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [web/tests/landing.spec.ts, web/tests/chat.spec.ts, web/tests/theme.spec.ts, web/package.json]
autonomous: true
requirements: [FR-07]
must_haves:
  truths:
    - "Test stubs for landing, chat, and theme exist"
    - "AI SDK and AI Elements dependencies are installed"
    - "AI Elements component library is initialized in the codebase"
  artifacts:
    - path: "web/tests/landing.spec.ts"
      provides: "Landing page smoke tests"
    - path: "web/tests/chat.spec.ts"
      provides: "Chat workspace smoke tests"
    - path: "web/src/components/ai-elements/message.tsx"
      provides: "Vercel AI Elements message component"
  key_links:
    - from: "web/package.json"
      to: "web/src/components/ai-elements/"
      via: "npx ai-elements init"
---

<objective>
Setup test scaffolding and initialize AI Elements dependencies for the UI overhaul.

Purpose: Ensure visual and functional verification is possible from the start and prepare the environment for new UI components.
Output: Playwright test stubs and ai-elements components initialized.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-RESEARCH.md
@.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Playwright test stubs</name>
  <files>web/tests/landing.spec.ts, web/tests/chat.spec.ts, web/tests/theme.spec.ts</files>
  <action>
    Create smoke test stubs for the new UI components to enable automated verification in later tasks.
    - web/tests/landing.spec.ts: Check for hero text and dithered background presence.
    - web/tests/chat.spec.ts: Check for sidebar and chat stream layout.
    - web/tests/theme.spec.ts: Check for dark/light mode toggle and system preference sync.
  </action>
  <verify>
    <automated>cd web && npx playwright test tests/landing.spec.ts tests/chat.spec.ts tests/theme.spec.ts</automated>
  </verify>
  <done>Test stubs created and failing or passing as expected (before implementation).</done>
</task>

<task type="auto">
  <name>Task 2: Initialize AI Elements and install dependencies</name>
  <files>web/package.json</files>
  <action>
    Install required AI SDK and AI Elements dependencies, and initialize the components.
    - Run: `npm install ai @ai-sdk/react` in web directory.
    - Run: `npx ai-elements@latest add message prompt-input attachments tool-call progress conversation reasoning inline-citation artifact jsx-preview` to inject components into `web/src/components/ai-elements/`.
  </action>
  <verify>
    <automated>ls web/src/components/ai-elements/message.tsx web/src/components/ai-elements/conversation.tsx</automated>
  </verify>
  <done>Dependencies installed and AI Elements components present in the codebase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → API | External dependencies added to frontend |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-00-01 | T | AI Elements | mitigate | Ensure components are used with standard sanitization; check for XSS in JSX Preview later. |
</threat_model>

<verification>
Verify that tests run (even if they fail due to missing elements) and that the `ai-elements` components are correctly injected.
</verification>

<success_criteria>
1. web/tests/ contains the three requested spec files.
2. web/package.json includes 'ai' and '@ai-sdk/react'.
3. web/src/components/ai-elements/ contains initialized component files.
</success_criteria>

<output>
After completion, create `.planning/phases/28-ui-overhaul-landing-page-chat-interface-and-global-polishing/28-00-SUMMARY.md`
</output>
