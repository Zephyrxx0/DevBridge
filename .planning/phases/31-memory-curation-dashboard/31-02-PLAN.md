---
phase: 31
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [web/tests/memory-dashboard.spec.ts, web/src/components/floating-header.tsx, web/src/app/dashboard/memory/page.tsx]
autonomous: true
requirements: [MEM-04]

must_haves:
  truths:
    - "User can navigate to Memory Dashboard from the header"
    - "Memory Dashboard shell exists at /dashboard/memory"
    - "Loading state is shown with skeletons"
  artifacts:
    - path: "web/src/app/dashboard/memory/page.tsx"
      provides: "Memory Dashboard UI page"
    - path: "web/tests/memory-dashboard.spec.ts"
      provides: "E2E verification for dashboard navigation and layout"
---

<objective>
Establish the frontend foundation for the Memory Curation Dashboard, including navigation integration and the initial page structure at /dashboard/memory.

Purpose: Provide the entry point and visual container for memory management.
Output: Integrated navigation link and dashboard page shell with skeleton state.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/31-memory-curation-dashboard/31-CONTEXT.md
@.planning/phases/31-memory-curation-dashboard/31-RESEARCH.md
@.planning/phases/31-memory-curation-dashboard/31-PATTERNS.md
@web/src/components/floating-header.tsx
@web/src/app/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 UI Test Scaffold</name>
  <files>web/tests/memory-dashboard.spec.ts</files>
  <read_first>
    - web/tests/memory-dashboard.spec.ts (to be created)
    - playwright.config.ts
  </read_first>
  <action>
    Create `web/tests/memory-dashboard.spec.ts`.
    Include test cases for:
    - Navigation: Verify clicking "Memory" in header navigates to `/dashboard/memory`.
    - Page Load: Verify the dashboard page renders a heading "Memory Curation".
    - Loading State: Verify skeleton cards are visible initially (or when data is fetching).
  </action>
  <verify>
    <automated>npx playwright test web/tests/memory-dashboard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `web/tests/memory-dashboard.spec.ts`.
    - Tests fail initially as the link and page don't exist yet.
  </acceptance_criteria>
  <done>UI test scaffold created.</done>
</task>

<task type="auto">
  <name>Task 1: Add Memory Link to Navigation</name>
  <files>web/src/components/floating-header.tsx</files>
  <read_first>
    - web/src/components/floating-header.tsx
  </read_first>
  <action>
    Update the `links` array in `FloatingHeader` to include the Memory Dashboard.
    Add:
    ```typescript
    {
        label: 'Memory',
        href: '/dashboard/memory',
    },
    ```
    Ensure it is placed after "Workspaces".
  </action>
  <verify>
    <automated>grep -q "/dashboard/memory" web/src/components/floating-header.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `FloatingHeader` component contains a link to `/dashboard/memory`.
    - The label for the link is "Memory".
  </acceptance_criteria>
  <done>Memory Dashboard link added to navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Memory Dashboard Shell</name>
  <files>web/src/app/dashboard/memory/page.tsx</files>
  <read_first>
    - web/src/app/dashboard/memory/page.tsx (to be created)
    - web/src/app/dashboard/page.tsx (analog for layout/styling)
  </read_first>
  <action>
    Create `web/src/app/dashboard/memory/page.tsx`.
    - Use "use client" directive.
    - Implement a page component that replicates the layout of `DashboardPage`:
        - Wrapper `div` with `pb-12`.
        - `section` with border, background, and glassmorphism styling.
        - Header section with `p` (uppercase) and `h1` ("Memory Curation").
        - Loading state that renders a grid of skeleton cards (using `Skeleton` from Shadcn UI).
    - Match the "Visual Cards" decision from CONTEXT.md: "expandable card grid".
  </action>
  <verify>
    <automated>npx playwright test web/tests/memory-dashboard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `web/src/app/dashboard/memory/page.tsx`.
    - Page renders "Memory Curation" heading.
    - Grid layout for cards exists.
    - Playwright tests for page load pass.
  </acceptance_criteria>
  <done>Memory Dashboard page shell implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Navigation to restricted paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Information Disclosure | `/dashboard/memory` | mitigate | Frontend protects navigation via Auth checks (reusing existing Dashboard patterns); Backend enforces 403 (implemented in Plan 01). |
</threat_model>

<verification>
npx playwright test web/tests/memory-dashboard.spec.ts
</verification>

<success_criteria>
- User can navigate to the Memory Dashboard.
- Page shell renders correctly with consistent styling.
- Loading states work.
</success_criteria>

<output>
After completion, create `.planning/phases/31-memory-curation-dashboard/31-02-SUMMARY.md`
</output>
