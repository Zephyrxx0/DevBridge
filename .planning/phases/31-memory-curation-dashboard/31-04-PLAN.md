---
phase: 31
plan: 04
type: execute
wave: 3
depends_on: [31-01, 31-03]
files_modified: [api/routes/memory.py, web/src/app/dashboard/memory/page.tsx]
autonomous: true
requirements: [MEM-04]

must_haves:
  truths:
    - "User can edit memory text directly"
    - "Memory updates are persisted to the database"
    - "UI reflects updated text immediately after edit"
  artifacts:
    - path: "api/routes/memory.py"
      provides: "Memory update (PUT) endpoint"
  key_links:
    - from: "web/src/app/dashboard/memory/page.tsx"
      to: "PUT /api/backend/memory/{id}"
      via: "fetch call in Edit UI"
      pattern: "PUT.*api/backend/memory"
---

<objective>
Implement direct text editing for memories, allowing users to correct or refine stored agent knowledge and history.

Purpose: Facilitate memory curation via direct modification.
Output: Memory update API endpoint and frontend editing interface.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/31-memory-curation-dashboard/31-CONTEXT.md
@.planning/phases/31-memory-curation-dashboard/31-RESEARCH.md
@.planning/phases/31-memory-curation-dashboard/31-PATTERNS.md
@api/routes/memory.py
@web/src/app/dashboard/memory/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Memory Update Endpoint</name>
  <files>api/routes/memory.py</files>
  <read_first>
    - api/routes/memory.py
    - api/db/hindsight.py
  </read_first>
  <action>
    Add a `PUT /{memory_id}` endpoint to `api/routes/memory.py`.
    - Depends on `verify_admin`.
    - Accept a JSON body with `text: str`.
    - **Direct Edit Resolution**: As per RESEARCH.md resolution, implement a direct SQL update to the database row if Hindsight API doesn't support it directly.
    - Implementation:
      ```python
      from sqlalchemy import text
      from api.routes.admin import get_engine
      
      # Try hindsight_db._client.memories.update if it exists.
      # Otherwise, use direct SQL:
      engine = get_engine()
      with engine.connect() as conn:
          conn.execute(
              text("UPDATE hindsight.memories SET text = :text WHERE id = :id AND bank_id = :bank_id"),
              {"text": body.text, "id": memory_id, "bank_id": user_id}
          )
          conn.commit()
      ```
    - Ensure `bank_id=user_id` is used for isolation.
  </action>
  <verify>
    <automated>pytest api/tests/test_phase31_memory.py</automated>
  </verify>
  <acceptance_criteria>
    - `PUT /api/backend/memory/{id}` endpoint exists.
    - It accepts a `text` field in the body.
    - It returns the updated memory or `{"status": "updated"}`.
    - Test in `api/tests/test_phase31_memory.py` verifies the update persists.
  </acceptance_criteria>
  <done>Memory update endpoint implemented with direct SQL fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Edit UI</name>
  <files>web/src/app/dashboard/memory/page.tsx</files>
  <read_first>
    - web/src/app/dashboard/memory/page.tsx
    - web/src/components/add-repo-modal.tsx (analog for modal/sheet interaction)
  </read_first>
  <action>
    Implement the editing interface in the Memory Dashboard.
    - Add an "Edit" button (Pencil icon) to each memory card.
    - Clicking Edit opens a `Sheet` or `Modal` (User choice: Modal or Sheet). Use `Sheet` from Shadcn UI.
    - The Sheet contains a `Textarea` for the memory text.
    - On "Save", call `PUT /api/backend/memory/${id}` with the new text.
    - Update the local state on success.
  </action>
  <verify>
    <automated>npx playwright test web/tests/memory-dashboard.spec.ts --grep @edit</automated>
  </verify>
  <acceptance_criteria>
    - Edit button opens a Sheet/Modal with the current memory text.
    - "Save" button triggers the PUT request to `/api/backend/memory/${id}`.
    - UI updates after saving.
  </acceptance_criteria>
  <done>Memory editing UI implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | PUT request with memory_id and text body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Tampering | `PUT /{memory_id}` | mitigate | Enforce `bank_id=user_id` and `verify_admin` to prevent unauthorized edits or cross-user tampering. |
| T-31-08 | Injection | `text` field | mitigate | Use parameterized SQL or safe API calls to prevent SQL injection when updating memory text. |
</threat_model>

<verification>
npx playwright test web/tests/memory-dashboard.spec.ts
</verification>

<success_criteria>
- Memories can be edited directly from the UI.
- Edits are saved and persisted.
- End-to-end curation flow (View -> Edit -> Delete) is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/31-memory-curation-dashboard/31-04-SUMMARY.md`
</output>
