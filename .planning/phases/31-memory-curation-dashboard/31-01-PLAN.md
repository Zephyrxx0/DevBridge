---
phase: 31
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api/tests/test_phase31_memory.py, api/routes/memory.py, api/main.py]
autonomous: true
requirements: [MEM-04]

must_haves:
  truths:
    - "Admin can list memories via API"
    - "Non-admin users are rejected with 403"
    - "Unauthenticated requests are rejected with 401"
    - "Memories are isolated by user_id (bank_id)"
  artifacts:
    - path: "api/routes/memory.py"
      provides: "Memory curation endpoints"
    - path: "api/tests/test_phase31_memory.py"
      provides: "Automated verification for memory isolation and auth"
  key_links:
    - from: "api/routes/memory.py"
      to: "api/db/hindsight.py"
      via: "hindsight_db._client"
      pattern: "hindsight_db\\._client"
---

<objective>
Implement the backend foundation for memory curation, including endpoints for listing and deleting memories with strict admin authorization and per-user isolation.

Purpose: Provide a secure and functional API for the Memory Dashboard.
Output: Memory router with List and Delete endpoints, verified by tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/31-memory-curation-dashboard/31-CONTEXT.md
@.planning/phases/31-memory-curation-dashboard/31-RESEARCH.md
@.planning/phases/31-memory-curation-dashboard/31-PATTERNS.md
@api/routes/admin.py
@api/db/hindsight.py
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 Test Scaffold</name>
  <files>api/tests/test_phase31_memory.py</files>
  <read_first>
    - api/tests/test_phase31_memory.py (to be created)
    - tests/test_admin_auth.py (analog for auth mocking)
  </read_first>
  <action>
    Create a new test file `api/tests/test_phase31_memory.py`.
    Include test cases for:
    - `test_list_memories_unauthorized`: Verify 401 when X-User-Id is missing.
    - `test_list_memories_forbidden`: Verify 403 when X-User-Id is not an admin.
    - `test_list_memories_success`: Mock Hindsight client and verify 200 with data.
    - `test_memory_isolation`: Verify that `bank_id` passed to Hindsight matches the requesting `user_id`.
    
    Use the mocking pattern from `31-PATTERNS.md`:
    ```python
    def _build_memory_app(monkeypatch, row):
        from fastapi import FastAPI
        from api.routes.memory import router
        app = FastAPI()
        # Note: Testing the router directly with its own prefix
        app.include_router(router, prefix="/memory")
        monkeypatch.setattr("api.routes.admin.get_engine", lambda: _FakeEngine(row))
        return TestClient(app)
    ```
  </action>
  <verify>
    <automated>pytest api/tests/test_phase31_memory.py</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `api/tests/test_phase31_memory.py`
    - Tests fail initially (Red stage) with "ModuleNotFoundError: No module named 'api.routes.memory'" or similar.
  </acceptance_criteria>
  <done>Test scaffold created and failing correctly.</done>
</task>

<task type="auto">
  <name>Task 1: Implement Memory List and Delete Endpoints</name>
  <files>api/routes/memory.py</files>
  <read_first>
    - api/routes/memory.py (to be created)
    - api/routes/admin.py (analog for verify_admin)
    - api/db/hindsight.py (for hindsight_db singleton)
  </read_first>
  <action>
    Create `api/routes/memory.py`.
    - Import `verify_admin` from `api.routes.admin`.
    - Import `hindsight_db` from `api.db.hindsight`.
    - Implement `GET /list`:
        - Depends on `verify_admin`.
        - Use `hindsight_db._client.memories.list(bank_id=user_id, limit=100)`.
        - Handle `client is None` (Hindsight disabled) with 503.
    - Implement `DELETE /{memory_id}`:
        - Depends on `verify_admin`.
        - Use `hindsight_db._client.delete_document(bank_id=user_id, document_id=memory_id)`.
        - Return `{"status": "deleted"}`.
  </action>
  <verify>
    <automated>pytest api/tests/test_phase31_memory.py</automated>
  </verify>
  <acceptance_criteria>
    - `api/routes/memory.py` contains `router = APIRouter()`.
    - `GET /list` uses `Depends(verify_admin)`.
    - `DELETE` endpoint uses `bank_id=user_id` in Hindsight call.
    - Tests in `api/tests/test_phase31_memory.py` pass (Green stage).
  </acceptance_criteria>
  <done>Memory curation endpoints implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Register Memory Router</name>
  <files>api/main.py</files>
  <read_first>
    - api/main.py
    - api/routes/memory.py
  </read_first>
  <action>
    Update `api/main.py` to include the new memory router.
    - Import `router as memory_router` from `api.routes.memory`.
    - Add `app.include_router(memory_router, prefix="/api/backend/memory", tags=["memory"])`.
    - NOTE: Using `/api/backend/memory` prefix per `31-PATTERNS.md` and checker issue 3.
  </action>
  <verify>
    <automated>grep -q "memory_router" api/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `api/main.py` imports `memory_router`.
    - `app.include_router` called with prefix `/api/backend/memory`.
  </acceptance_criteria>
  <done>Memory router registered in the main application.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted input (X-User-Id header, memory_id) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | `GET /list` | mitigate | Use `verify_admin` to restrict access to authorized users only. |
| T-31-02 | Tampering | `DELETE /{memory_id}` | mitigate | Enforce `bank_id=user_id` to prevent users from deleting others' memories. |
| T-31-03 | Denial of Service | `GET /list` | mitigate | Implement `limit=100` in Hindsight retrieval to prevent memory exhaustion. |
</threat_model>

<verification>
pytest api/tests/test_phase31_memory.py
</verification>

<success_criteria>
- Endpoints `GET /api/backend/memory/list` and `DELETE /api/backend/memory/{id}` are functional.
- Authentication and isolation are enforced.
- Tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/31-memory-curation-dashboard/31-01-SUMMARY.md`
</output>
