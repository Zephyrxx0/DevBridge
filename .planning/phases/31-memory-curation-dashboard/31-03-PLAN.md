---
phase: 31
plan: 03
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified: [web/src/app/dashboard/memory/page.tsx]
autonomous: true
requirements: [MEM-04]

must_haves:
  truths:
    - "Real memory data is fetched and displayed"
    - "Memory cards show semantic tags (e.g., type: experience)"
    - "Admin can delete a memory with confirmation"
  artifacts:
    - path: "web/src/app/dashboard/memory/page.tsx"
      provides: "Memory card rendering and deletion logic"
  key_links:
    - from: "web/src/app/dashboard/memory/page.tsx"
      to: "/api/backend/memory/list"
      via: "fetch call in useEffect"
      pattern: "/api/backend/memory/list"
---

<objective>
Implement memory data fetching, rendering of interactive cards with semantic metadata, and the ability to delete memories from the dashboard.

Purpose: Allow users to view and remove agent memories.
Output: Functional memory grid with deletion capabilities.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/31-memory-curation-dashboard/31-CONTEXT.md
@.planning/phases/31-memory-curation-dashboard/31-RESEARCH.md
@.planning/phases/31-memory-curation-dashboard/31-PATTERNS.md
@web/src/app/dashboard/memory/page.tsx
@api/routes/memory.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch and Render Memory Cards</name>
  <files>web/src/app/dashboard/memory/page.tsx</files>
  <read_first>
    - web/src/app/dashboard/memory/page.tsx
    - web/src/app/dashboard/page.tsx (analog for card layout)
  </read_first>
  <action>
    Implement data fetching and rendering in `web/src/app/dashboard/memory/page.tsx`.
    - Use `useEffect` to fetch from `/api/backend/memory/list` (Note: `/api/backend` prefix per PATTERNS.md).
    - Define a `Memory` type: `{ id: string, text: string, metadata: { type: string, tags: string[] }, created_at: string }`.
    - Map over fetched memories and render `Card` components:
        - `CardHeader`: Show metadata `type` as a badge or tag.
        - `CardContent`: Show the memory `text`.
        - **Expandable Behavior**: Implement a toggle or collapsible area for long memory text. Use a "Show More" button or a click-to-expand interaction on the card content if the text exceeds a threshold (e.g., 200 characters).
        - `CardFooter`: Show `created_at` and a Delete button (Trash icon).
    - Match CONTEXT.md requirement for semantic tags and "Reflect" indicators (check metadata for reflect flags if present).
  </action>
  <verify>
    <automated>npx playwright test web/tests/memory-dashboard.spec.ts --grep @list</automated>
  </verify>
  <acceptance_criteria>
    - `fetch("/api/backend/memory/list")` is called.
    - Memories are rendered as cards.
    - Each card displays text and type metadata.
    - Long text is truncated with an "Expand" option (checker requirement).
  </acceptance_criteria>
  <done>Memory card fetching and rendering implemented with expandable behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Memory Deletion</name>
  <files>web/src/app/dashboard/memory/page.tsx</files>
  <read_first>
    - web/src/app/dashboard/memory/page.tsx
  </read_first>
  <action>
    Add deletion logic to the Memory Dashboard.
    - Implement `handleDelete(id: string)` function:
        - Calls `DELETE /api/backend/memory/${id}`.
        - Optimistically updates UI by removing the card from the state.
        - Shows a toast or notification on success/failure.
    - Add a confirmation step before deletion (use `window.confirm` for simplicity or a Shadcn `AlertDialog` if preferred).
  </action>
  <verify>
    <automated>npx playwright test web/tests/memory-dashboard.spec.ts --grep @delete</automated>
  </verify>
  <acceptance_criteria>
    - Delete button in card triggers `handleDelete`.
    - API call to `/api/backend/memory/${id}` is made.
    - UI updates after deletion.
  </acceptance_criteria>
  <done>Memory deletion functionality implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Delete request with memory_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Information Disclosure | `Card` | mitigate | Ensure only non-sensitive metadata is displayed in the UI cards. |
| T-31-06 | Tampering | `handleDelete` | mitigate | Confirmation dialog prevents accidental deletions. |
</threat_model>

<verification>
npx playwright test web/tests/memory-dashboard.spec.ts
</verification>

<success_criteria>
- Memories are correctly listed in the dashboard.
- User can delete memories.
- UI reflects the current state of stored memories.
</success_criteria>

<output>
After completion, create `.planning/phases/31-memory-curation-dashboard/31-03-SUMMARY.md`
</output>
