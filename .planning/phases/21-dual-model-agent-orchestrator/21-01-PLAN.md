---
phase: 21-dual-model-agent-orchestrator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api/agents/state.py, api/agents/utils/llm.py, api/agents/nodes/router.py, api/agents/nodes/fast.py, api/agents/graph.py, api/core/config.py, tests/test_phase21_foundation.py, tests/test_phase21_routing.py, tests/test_phase21_fallback.py, tests/test_phase21_sse.py, tests/test_phase21_e2e.py]
autonomous: true
requirements: [MR-02, FR-01]

must_haves:
  truths:
    - "System can classify 'hi' as FAST and 'refactor this' as DEEP"
    - "Fast Model responds to simple queries without triggering Big Model"
    - "Agent state persists messages across graph nodes"
  artifacts:
    - path: "api/agents/state.py"
      provides: "LangGraph AgentState definition"
    - path: "api/agents/utils/llm.py"
      provides: "LLM factory for dual-model access"
    - path: "api/agents/nodes/router.py"
      provides: "Gemma-4 binary intent classifier"
    - path: "api/agents/nodes/fast.py"
      provides: "Fast Model worker node"
    - path: "api/agents/graph.py"
      provides: "Compiled LangGraph Supervisor graph"
  key_links:
    - from: "api/agents/nodes/router.py"
      to: "api/agents/nodes/fast.py"
      via: "conditional edge"
      pattern: "goto=\"fast_worker\""
---

<objective>
Setup the foundational LangGraph state, dual-model LLM factory, and implement the fast path (intent classification and fast worker). Includes Wave 0 test stub creation.

Purpose: Enable low-latency routing for simple queries using the Gemma-4 model.
Output: Working state graph that handles FAST queries.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-dual-model-agent-orchestrator/21-CONTEXT.md
@.planning/phases/21-dual-model-agent-orchestrator/21-AI-SPEC.md
@.planning/phases/21-dual-model-agent-orchestrator/21-RESEARCH.md
@.planning/phases/21-dual-model-agent-orchestrator/21-PATTERNS.md
@api/agents/orchestrator.py
@api/core/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 Test Stubs</name>
  <files>tests/test_phase21_foundation.py, tests/test_phase21_routing.py, tests/test_phase21_fallback.py, tests/test_phase21_sse.py, tests/test_phase21_e2e.py</files>
  <action>
    Create test stubs that will be used for verification in current and downstream plans.
    - tests/test_phase21_foundation.py: Tests for AgentState, get_model factory.
    - tests/test_phase21_routing.py: Tests for router node (classification logic) and fast worker node.
    - tests/test_phase21_fallback.py: Tests for fallback logic (Wave 2).
    - tests/test_phase21_sse.py: Tests for SSE metadata signaling (Wave 3).
    - tests/test_phase21_e2e.py: E2E orchestration tests (Wave 3).
  </action>
  <verify>
    ls tests/test_phase21_foundation.py tests/test_phase21_routing.py tests/test_phase21_fallback.py tests/test_phase21_sse.py tests/test_phase21_e2e.py
  </verify>
  <done>Wave 0 test stubs are ready.</done>
</task>

<task type="auto">
  <name>Task 1: Foundation (State & LLM Factory)</name>
  <files>api/agents/state.py, api/agents/utils/llm.py, api/core/config.py</files>
  <read_first>
    - api/core/config.py
    - api/agents/orchestrator.py
    - .planning/phases/21-dual-model-agent-orchestrator/21-PATTERNS.md
  </read_first>
  <action>
    1. Update api/core/config.py:
       - Add BIG_MODEL_PORT=8000
       - Add FAST_MODEL_PORT=8001
       - Add FAST_MODEL_TIMEOUT=30
       - Add BIG_MODEL_TIMEOUT=120
    2. Create api/agents/state.py:
       - Define AgentState(TypedDict) with messages (Annotated[list, operator.add]), next (str), and fallback (bool).
    3. Create api/agents/utils/llm.py:
       - Implement get_model(is_fast: bool) returning ChatOpenAI.
       - Use settings.FAST_MODEL_PORT and settings.BIG_MODEL_PORT.
       - Set temperature=0.0 for fast, 0.7 for big.
  </action>
  <verify>
    <automated>pytest tests/test_phase21_foundation.py</automated>
  </verify>
  <acceptance_criteria>
    - api/core/config.py contains BIG_MODEL_PORT=8000
    - api/agents/state.py contains AgentState and operator.add
    - api/agents/utils/llm.py contains ChatOpenAI(base_url=...:8001/v1) for fast model
  </acceptance_criteria>
  <done>Infrastructure for dual-model orchestration is ready.</done>
</task>

<task type="auto">
  <name>Task 2: Fast Path Routing (Router & Fast Worker)</name>
  <files>api/agents/nodes/router.py, api/agents/nodes/fast.py, api/agents/graph.py</files>
  <read_first>
    - .planning/phases/21-dual-model-agent-orchestrator/21-AI-SPEC.md
    - .planning/phases/21-dual-model-agent-orchestrator/21-PATTERNS.md
  </read_first>
  <action>
    1. Create api/agents/nodes/router.py:
       - Implement intent_classifier node using get_model(is_fast=True).
       - Wrap model invocation in asyncio.wait_for(..., timeout=30). (Per D-04)
       - Prompt: "Classify: 'FAST' (greetings/clarifications) or 'DEEP' (code analysis/complex logic). Reply ONLY with the word. Query: [user query]"
       - Return {"next": "fast_worker" if "FAST" in response else "big_worker"}. (Per D-01)
    2. Create api/agents/nodes/fast.py:
       - Implement fast_worker_node using get_model(is_fast=True).
       - Simply invoke the model with state["messages"], wrapped in asyncio.wait_for(..., timeout=30). (Per D-04)
    3. Create api/agents/graph.py:
       - Assemble StateGraph with router, fast_worker, and placeholder big_worker nodes.
       - Add conditional edges from router based on "next" key. (Per D-02)
  </action>
  <verify>
    <automated>pytest tests/test_phase21_routing.py</automated>
  </verify>
  <acceptance_criteria>
    - api/agents/nodes/router.py contains "ONLY 'FAST' or 'DEEP'" in prompt
    - api/agents/nodes/router.py uses asyncio.wait_for(..., timeout=30)
    - api/agents/graph.py contains builder.add_conditional_edges("router", ...)
    - Unit test for 'hi' routes to 'fast_worker'
  </acceptance_criteria>
  <done>Fast path routing is implemented and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> Router | Untrusted user input is classified by LLM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | router.py | mitigate | Strict prompt formatting and binary output constraint to prevent prompt injection from affecting routing logic. |
| T-21-02 | Denial of Service | llm.py | mitigate | Enforce strict timeouts (30s/120s) per D-04 to prevent long-running inference from blocking the pool. |
</threat_model>

<verification>
- Fast model (8001) is reachable.
- Intent classifier correctly routes simple queries to Fast worker.
- Agent state correctly merges messages.
</verification>

<success_criteria>
- Intent classification for simple queries responds in < 5s.
- LangGraph supervisor pattern implemented correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/21-dual-model-agent-orchestrator/21-01-SUMMARY.md`
</output>
