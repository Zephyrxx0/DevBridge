---
phase: 21-dual-model-agent-orchestrator
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified: [api/agents/nodes/big.py, api/agents/utils/fallback.py, api/agents/graph.py]
autonomous: true
requirements: [MR-01, FR-01]

must_haves:
  truths:
    - "Big Model call times out after 120s and triggers fallback"
    - "Fallback path sets 'fallback=True' in state for UI signaling"
    - "System responds with Fast Model if Big Model is unavailable"
  artifacts:
    - path: "api/agents/nodes/big.py"
      provides: "Big Model reasoning node with timeout"
    - path: "api/agents/utils/fallback.py"
      provides: "Centralized fallback handling"
  key_links:
    - from: "api/agents/nodes/big.py"
      to: "api/agents/nodes/fast.py"
      via: "LangGraph Command goto"
      pattern: "goto=\"fast_worker\""
---

<objective>
Implement the Big Model reasoning node and the robust fallback mechanism that switches to the Fast Model on timeout or failure.

Purpose: Provide deep reasoning capabilities while maintaining system availability through graceful fallback.
Output: Integrated reasoning path with 120s timeout and Fast Model failover.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-dual-model-agent-orchestrator/21-CONTEXT.md
@.planning/phases/21-dual-model-agent-orchestrator/21-AI-SPEC.md
@.planning/phases/21-dual-model-agent-orchestrator/21-RESEARCH.md
@.planning/phases/21-dual-model-agent-orchestrator/21-PATTERNS.md
@api/agents/graph.py
@api/core/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Big Model Node & Timeout Fallback</name>
  <files>api/agents/nodes/big.py, api/agents/utils/fallback.py</files>
  <read_first>
    - .planning/phases/21-dual-model-agent-orchestrator/21-AI-SPEC.md
    - .planning/phases/21-dual-model-agent-orchestrator/21-PATTERNS.md
    - api/core/config.py
    - api/agents/nodes/fast.py
  </read_first>
  <action>
    1. Create api/agents/utils/fallback.py:
       - Implement a helper that returns a LangGraph Command to goto="fast_worker" with update={"fallback": True}. (Per D-03)
    2. Create api/agents/nodes/big.py:
       - Implement big_worker_node using get_model(is_fast=False).
       - Wrap model invocation in asyncio.wait_for(..., timeout=settings.BIG_MODEL_TIMEOUT). (Per D-04)
       - On TimeoutError or Exception, use the fallback utility to trigger failover to fast_worker.
  </action>
  <verify>
    <automated>pytest tests/test_phase21_fallback.py</automated>
  </verify>
  <acceptance_criteria>
    - api/agents/nodes/big.py uses asyncio.wait_for and timeout=120
    - api/agents/nodes/big.py returns Command(..., goto="fast_worker") on failure
    - Fallback utility sets fallback=True in state
  </acceptance_criteria>
  <done>Big Model integration with fallback is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Complete Graph Assembly</name>
  <files>api/agents/graph.py</files>
  <read_first>
    - .planning/phases/21-dual-model-agent-orchestrator/21-PATTERNS.md
    - api/agents/graph.py
    - api/agents/nodes/big.py
  </read_first>
  <action>
    1. Update api/agents/graph.py:
       - Import big_worker_node and register it in the StateGraph.
       - Ensure all paths (Fast, Big, Fallback) are correctly wired and terminate at END.
       - Compile the graph with MemorySaver for session persistence.
  </action>
  <verify>
    <automated>pytest tests/test_phase21_graph_e2e.py</automated>
  </verify>
  <acceptance_criteria>
    - api/agents/graph.py contains builder.add_node("big_worker", big_worker_node)
    - Integration test for DEEP query routes to Big worker
    - Integration test for Big worker timeout successfully returns Fast worker output
  </acceptance_criteria>
  <done>LangGraph orchestrator is fully assembled and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Big Model -> Fast Model | Failover path must not leak internal error details to user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Availability | big.py | mitigate | Enforce 120s timeout and automatic fallback to Fast Model to ensure system remains responsive even if Big Model hangs. |
</threat_model>

<verification>
- Big model (8000) is reachable.
- Intent classifier correctly routes complex queries to Big worker.
- Timeout correctly triggers fallback path.
</verification>

<success_criteria>
- Fallback succeeds within 30s timeout after Big Model failover.
- Both models load concurrently without OOM (requires MI300X setup from Phase 20).
</success_criteria>

<output>
After completion, create `.planning/phases/21-dual-model-agent-orchestrator/21-02-SUMMARY.md`
</output>
