---
phase: 21-dual-model-agent-orchestrator
plan: 03
type: execute
wave: 3
depends_on: [21-02]
files_modified: [api/routes/chats.py, api/main.py, web/src/app/repo/[id]/page.tsx]
autonomous: true
requirements: [FR-01]

must_haves:
  truths:
    - "Streaming SSE includes a metadata packet with 'fallback' flag"
    - "Frontend receives notification when system switches to Fast Mode"
    - "Chat endpoint uses the new LangGraph orchestrator for all requests"
    - "UI displays 'Fast Mode' badge on messages when fallback is active"
  artifacts:
    - path: "api/routes/chats.py"
      provides: "Updated chat route with LangGraph integration"
    - path: "api/main.py"
      provides: "SSE streaming with metadata support"
    - path: "web/src/app/repo/[id]/page.tsx"
      provides: "UI badge rendering and SSE metadata handling"
  key_links:
    - from: "api/routes/chats.py"
      to: "api/agents/graph.py"
      via: "graph.astream_events"
      pattern: "graph\\.astream_events"
    - from: "web/src/app/repo/[id]/page.tsx"
      to: "api/chat/stream"
      via: "fetch SSE"
      pattern: "fetch.*chat/stream"
---

<objective>
Wire the LangGraph orchestrator into the main chat route, implement SSE metadata signaling, and update the web UI to display the "Fast Mode" badge.

Purpose: Connect the backend multi-agent logic to the user interface with transparent status signaling and UI feedback.
Output: E2E streaming chat with dual-model routing, fallback notifications, and UI badge.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-dual-model-agent-orchestrator/21-CONTEXT.md
@.planning/phases/21-dual-model-agent-orchestrator/21-PATTERNS.md
@api/routes/chats.py
@api/main.py
@api/agents/graph.py
@web/src/app/repo/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat Route Integration (Backend)</name>
  <files>api/routes/chats.py, api/main.py</files>
  <read_first>
    - api/routes/chats.py
    - api/main.py
    - api/agents/graph.py
    - .planning/phases/21-dual-model-agent-orchestrator/21-PATTERNS.md
  </read_first>
  <action>
    1. Update api/routes/chats.py (or api/main.py if endpoint is there):
       - Replace current orchestrator invocation with compiled LangGraph graph.
       - Use graph.astream_events(..., version="v2") for streaming.
       - Capture the 'fallback' flag from the final state or intermediate events.
    2. Enhance SSE Event Generator:
       - Yield an initial metadata event: `data: {"type": "metadata", "fallback": false}`.
       - If a fallback event is detected during the stream (e.g., from a Command update or state change), yield an updated metadata event: `data: {"type": "metadata", "fallback": true}`. (Per D-03)
  </action>
  <verify>
    <automated>pytest tests/test_phase21_sse.py</automated>
  </verify>
  <acceptance_criteria>
    - api/routes/chats.py calls graph.astream_events
    - SSE stream contains "fallback": true when Big Model failover occurs
    - Streaming chunks are correctly yielded as they arrive from the graph
  </acceptance_criteria>
  <done>Chat route is integrated with the dual-model orchestrator and supports fallback signaling.</done>
</task>

<task type="auto">
  <name>Task 2: UI "Fast Mode" Badge (Frontend)</name>
  <files>web/src/app/repo/[id]/page.tsx</files>
  <read_first>
    - web/src/app/repo/[id]/page.tsx
  </read_first>
  <action>
    Implement the "Fast Mode" badge in the chat UI (D-03).
    1. Update `Message` interface to include `fallback?: boolean`.
    2. Import `Badge` from `@/components/ui/badge`.
    3. Update the SSE message listener in `RepoWorkspacePage` to handle `type: "metadata"`. If `fallback` is true, update the current assistant message state to set `fallback: true`.
    4. Update the message rendering loop:
       - For assistant messages, if `message.fallback` is true, render a `<Badge>` component with "Fast Mode" text next to the "DevBridge" label.
       - Use styling consistent with the design: `bg-yellow-500/10 text-yellow-600 border-yellow-500/20` or similar subtle warning style.
  </action>
  <verify>
    <automated>grep -E "fallback\?: boolean|Fast Mode" web/src/app/repo/[id]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - web/src/app/repo/[id]/page.tsx contains "Fast Mode" text in JSX
    - Message interface has fallback field
    - SSE listener updates message state with fallback flag
  </acceptance_criteria>
  <done>UI badge for Fast Mode is implemented and wired to SSE metadata.</done>
</task>

<task type="auto">
  <name>Task 3: E2E Verification, Cleanup & Graph Update</name>
  <files>api/agents/orchestrator.py</files>
  <read_first>
    - api/agents/orchestrator.py
  </read_first>
  <action>
    1. Refactor or deprecate the old api/agents/orchestrator.py:
       - Move any remaining reusable logic to the new node structure.
       - Ensure all imports in the project now point to the new LangGraph-based system.
    2. Final E2E testing:
       - Verify FAST routing latency.
       - Verify DEEP routing quality.
       - Verify Fallback UI signaling under simulated high-load/timeout conditions.
    3. Run `graphify update .` to refresh the knowledge graph with new agent structures and route connections.
  </action>
  <verify>
    <automated>pytest tests/test_phase21_e2e.py</automated>
  </verify>
  <acceptance_criteria>
    - Old orchestrator code is cleanly refactored/removed
    - All chat functional tests pass using the new system
    - graphify report reflects new graph nodes (router, fast, big)
  </acceptance_criteria>
  <done>Phase 21 is complete, verified E2E, and knowledge graph is updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE Stream -> Client | Metadata packet must be correctly parsed by frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Information Disclosure | chats.py | mitigate | Ensure the SSE 'fallback' flag is the only internal state leaked to the frontend; do not send raw exception tracebacks in the stream. |
</threat_model>

<verification>
- SSE stream yields valid JSON packets.
- Metadata packet correctly reflects fallback state.
- Latency and throughput meet success criteria.
- "Fast Mode" badge appears in UI when triggered.
</verification>

<success_criteria>
- Fallback UI badge is triggered via SSE metadata.
- End-to-end chat flow works for both FAST and DEEP paths.
- Codebase graph updated via graphify.
</success_criteria>

<output>
After completion, create `.planning/phases/21-dual-model-agent-orchestrator/21-03-SUMMARY.md`
</output>
