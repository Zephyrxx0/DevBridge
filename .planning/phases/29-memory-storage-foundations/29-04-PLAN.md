---
phase: 29-memory-storage-foundations
plan: 29-04
type: execute
wave: 4
depends_on: [29-03]
files_modified: [api/main.py, api/tests/test_phase29_memory.py]
autonomous: true
requirements: [MEM-01]
gap_closure: true

must_haves:
  truths:
    - "Chat endpoints reject unauthenticated requests instead of falling back to default_user"
    - "Memory banks are strictly isolated by user_id in both /chat and /chat/stream"
    - "Tests prove User A cannot access User B's memories"
  artifacts:
    - path: "api/main.py"
      provides: "Authenticated chat endpoints"
    - path: "api/tests/test_phase29_memory.py"
      provides: "Behavioral isolation tests"
  key_links:
    - from: "api/main.py"
      to: "HTTPException(401)"
      via: "missing request.state.user_id"
---

<objective>
Close safety gaps in memory isolation by enforcing unique authenticated identity and implementing behavioral tests.

Purpose: Prevent cross-user memory leakage and verify isolation.
Output: Secure chat endpoints and comprehensive isolation tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-memory-storage-foundations/29-VERIFICATION.md
@api/main.py
@api/tests/test_phase29_memory.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce authentication in chat endpoints</name>
  <files>api/main.py</files>
  <read_first>api/main.py</read_first>
  <action>
    - Locate the `/chat` and `/chat/stream` endpoints in `api/main.py`.
    - In `/chat`, replace `user_id = getattr(request.state, "user_id", "default_user")` with a check that raises `HTTPException(401)` if `request.state.user_id` is missing.
    - In `/chat/stream` (inside the `event_generator`), do the same for the `user_id` assignment.
    - Reference decision from 29-VERIFICATION.md regarding memory isolation safety.
  </action>
  <verify>
    <automated>pytest api/tests/test_phase29_memory.py -k test_unauthenticated_chat_rejected</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "default_user" api/main.py` should decrease or return 0 for chat endpoints.
    - `HTTPException(status_code=401` is raised when `user_id` is missing.
  </acceptance_criteria>
  <done>Chat endpoints return 401 for unauthenticated requests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement User A/B isolation tests</name>
  <files>api/tests/test_phase29_memory.py</files>
  <read_first>api/tests/test_phase29_memory.py, api/main.py</read_first>
  <behavior>
    - Test 1: Sending request without internal auth headers results in 401.
    - Test 2: Two requests with different `X-User-Id` (and valid `X-Internal-Auth`) result in separate Hindsight bank calls.
  </behavior>
  <action>
    - Replace the scaffold tests in `api/tests/test_phase29_memory.py`.
    - Add `test_unauthenticated_chat_rejected` to verify the fix from Task 1.
    - Add `test_user_isolation` that mocks `hindsight_langgraph.create_recall_node` (or similar) to verify `user_id` propagation.
    - Use `FastAPI`'s `TestClient` to simulate requests.
    - For isolation test, mock `graph.ainvoke` and `stream_graph_events` to check the `config` object passed.
  </action>
  <verify>
    <automated>pytest api/tests/test_phase29_memory.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests for 401 rejection pass.
    - Tests for user_id propagation to LangGraph config pass.
  </acceptance_criteria>
  <done>Isolation tests pass, proving user separation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External -> API | Unauthenticated requests must be blocked before reaching memory-sensitive logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Information Disclosure | Hindsight Memory Bank | mitigate | Enforce `user_id` presence; reject `default_user` fallback to prevent cross-user bank mixing. |
</threat_model>

<verification>
- Run `pytest api/tests/test_phase29_memory.py` to ensure all tests pass.
- Manually verify with `curl` (if possible) that a request without headers returns 401.
</verification>

<success_criteria>
- No `default_user` fallback in `api/main.py` chat endpoints.
- 100% pass rate for `api/tests/test_phase29_memory.py`.
- Memory isolation Truth in 29-VERIFICATION.md can be marked as VERIFIED.
</success_criteria>

<output>
After completion, create `.planning/phases/29-memory-storage-foundations/29-04-SUMMARY.md`
</output>
