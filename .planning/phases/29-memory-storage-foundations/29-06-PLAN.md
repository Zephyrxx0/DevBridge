---
phase: 29-memory-storage-foundations
plan: 29-06
type: execute
wave: 6
depends_on: [29-05]
files_modified: [api/tests/test_phase29_memory.py, api/agents/graph.py, api/main.py]
autonomous: true
requirements: [MEM-01, MEM-03]
gap_closure: true

must_haves:
  truths:
    - "Authenticated follow-up chat can exercise recall path and bind recalled context into hindsight_memory."
    - "Retain path executes after model response without blocking request completion."
    - "Automated tests assert ordering: response path completes before reflection side effects."
  artifacts:
    - path: "api/tests/test_phase29_memory.py"
      provides: "Endpoint-level recall binding + retain non-blocking assertions"
    - path: "api/agents/graph.py"
      provides: "Stable seams for recall/retain behavior observation (if required by tests)"
    - path: "api/main.py"
      provides: "Request-path contract preserved while memory side effects run async"
  key_links:
    - from: "api/main.py"
      to: "api.agents.graph.graph.ainvoke"
      via: "/chat endpoint request execution"
    - from: "api/agents/graph.py"
      to: "hindsight_memory"
      via: "recall node output_key binding"
    - from: "api/main.py"
      to: "hindsight_db.reflect"
      via: "SchedulerManager.add_job async cron path"
---

<objective>
Close UAT gaps #4 and #5 by adding executable assertions for recall-memory binding and retain non-blocking behavior in real chat lifecycle.

Purpose: Prove memory enrichment and async reflection contracts, not just wiring existence.
Output: Integration/perf-style tests that fail on recall/retain regressions.
</objective>

<execution_context>
@C:/Users/tronl/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/tronl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-memory-storage-foundations/29-UAT.md
@.planning/phases/29-memory-storage-foundations/29-05-SUMMARY.md
@api/agents/graph.py
@api/main.py
@api/tests/test_phase29_memory.py

<interfaces>
From `api/agents/graph.py`:
```python
create_recall_node(client=client, bank_id_from_config="user_id", output_key="hindsight_memory")
create_retain_node(client=client, bank_id_from_config="user_id", retain_ai=True)
```

From `api/main.py`:
```python
config = {"configurable": {"thread_id": payload.thread_id, "user_id": user_id}}
result = await graph.ainvoke(input_data, config=config)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add recall-binding integration assertion (per D-29-01/D-29-03)</name>
  <files>api/tests/test_phase29_memory.py, api/agents/graph.py</files>
  <behavior>
    - Test 1: for authenticated user with seeded prior memory, recall path returns hindsight_memory and chat still returns normal response.
    - Test 2: recalled context belongs to same user_id bank; no cross-user bleed.
  </behavior>
  <action>
    Extend `api/tests/test_phase29_memory.py` with endpoint-level recall assertion for `/chat`. Seed or mock recall data for one user, invoke authenticated follow-up, assert state contains `hindsight_memory` contribution and response continuity (200 + response body). If direct state visibility unavailable, add minimal seam in `api/agents/graph.py` to expose recall output in a test-safe way (no production behavior drift). Keep `bank_id_from_config="user_id"` unchanged per D-29-01/D-29-03.
  </action>
  <verify>
    <automated>pytest api/tests/test_phase29_memory.py -k "recall and hindsight_memory" -x</automated>
  </verify>
  <done>Recall behavior verified in request flow with explicit hindsight_memory assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add retain non-blocking timing + ordering assertions (per D-29 retention decision)</name>
  <files>api/tests/test_phase29_memory.py, api/main.py</files>
  <behavior>
    - Test 1: /chat returns before simulated heavy reflection work completes.
    - Test 2: ordering shows worker response emitted before retain-side reflection completion.
  </behavior>
  <action>
    Add integration/perf test in `api/tests/test_phase29_memory.py` that injects controlled delay into reflection path (mocked `hindsight_db.reflect` or scheduled job target) and measures `/chat` response completion. Assert response latency stays below bound and does not wait for reflection completion. Add event/order assertions using captured timestamps/call-log list. Adjust `api/main.py` only if tiny test seam needed for deterministic ordering capture; preserve async APScheduler model per locked retention decision.
  </action>
  <verify>
    <automated>pytest api/tests/test_phase29_memory.py -k "retain and non_blocking" -x</automated>
  </verify>
  <done>Retain/reflect non-blocking contract proven with automated timing + ordering checks.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate phase memory gap suite and gate command</name>
  <files>api/tests/test_phase29_memory.py</files>
  <action>
    Organize new tests with stable names/markers for UAT gap mapping (test 4 + test 5). Keep deterministic mocks (no external network). Ensure suite includes prior auth/isolation tests plus new recall/retain checks in one executable gate for phase 29 memory behavior.
  </action>
  <verify>
    <automated>pytest api/tests/test_phase29_memory.py -x</automated>
  </verify>
  <done>Single command proves auth + isolation + recall + retain contracts for phase 29.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auth context -> Graph config | `user_id` crosses from request middleware into recall/retain bank routing. |
| Request path -> Background reflection | Sync HTTP response must stay isolated from async memory consolidation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06-01 | Information Disclosure | `api/agents/graph.py` recall bank routing | mitigate | Assert `user_id`-scoped recall binding and no cross-user memory in endpoint test. |
| T-29-06-02 | Denial of Service | `/chat` request path | mitigate | Add latency/order tests proving reflect work does not block HTTP response lifecycle. |
| T-29-06-03 | Tampering | memory state propagation | mitigate | Verify `hindsight_memory` output binding survives follow-up flow and cannot silently drop in refactors. |
</threat_model>

<verification>
Run:
- `pytest api/tests/test_phase29_memory.py -k "recall and hindsight_memory" -x`
- `pytest api/tests/test_phase29_memory.py -k "retain and non_blocking" -x`
- `pytest api/tests/test_phase29_memory.py -x`
</verification>

<success_criteria>
- UAT gap #4 has endpoint-level recall binding assertion.
- UAT gap #5 has explicit non-blocking timing + ordering assertion.
- Full phase-29 memory suite passes with deterministic mocks.
</success_criteria>

<output>
After completion, create `.planning/phases/29-memory-storage-foundations/29-06-SUMMARY.md`
</output>
