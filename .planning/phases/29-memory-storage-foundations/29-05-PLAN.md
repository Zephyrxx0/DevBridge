---
phase: 29-memory-storage-foundations
plan: 29-05
type: execute
wave: 5
depends_on: [29-04]
files_modified: [api/db/hindsight.py, api/main.py, api/tests/test_phase29_memory.py, api/tests/test_phase29_startup_smoke.py]
autonomous: true
requirements: [MEM-02, MEM-03]
gap_closure: true

must_haves:
  truths:
    - "Cold start from clean process reaches ready state and serves live health response."
    - "Startup path initializes Hindsight embedded client with schema=hindsight without crash."
    - "Startup smoke harness asserts initialization + readiness contract in automated test run."
  artifacts:
    - path: "api/db/hindsight.py"
      provides: "Deterministic Hindsight initialize contract (env + success/failure behavior)"
    - path: "api/main.py"
      provides: "Lifespan startup contract test points for Hindsight init + scheduler boot"
    - path: "api/tests/test_phase29_memory.py"
      provides: "Unit coverage for Hindsight initialize env contract and failure path"
    - path: "api/tests/test_phase29_startup_smoke.py"
      provides: "Cold-start integration harness with readiness probe"
  key_links:
    - from: "api/main.py"
      to: "api/db/hindsight.py"
      via: "lifespan -> hindsight_db.initialize()"
    - from: "api/tests/test_phase29_startup_smoke.py"
      to: "api/main.py"
      via: "TestClient startup + / + /health/db probe"
---

<objective>
Close UAT gaps #1 and #6. Add executable startup harness proving clean boot, Hindsight schema initialization, and readiness response path.

Purpose: Lock startup reliability contract for memory stack so regressions fail fast in CI.
Output: Startup-focused tests plus explicit initialization assertions.
</objective>

<execution_context>
@C:/Users/tronl/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/tronl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-memory-storage-foundations/29-UAT.md
@.planning/phases/29-memory-storage-foundations/29-04-SUMMARY.md
@api/main.py
@api/db/hindsight.py
@api/tests/test_phase29_memory.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Hindsight initialize contract tests (per D-29 schema decision)</name>
  <files>api/tests/test_phase29_memory.py, api/db/hindsight.py</files>
  <behavior>
    - Test 1: initialize() sets HINDSIGHT_API_DATABASE_SCHEMA=hindsight and returns True when client constructs.
    - Test 2: initialize() returns False when constructor raises; no crash bubbles to caller.
    - Test 3: initialize() returns False when DB URL missing.
  </behavior>
  <action>
    Extend `api/tests/test_phase29_memory.py` with focused unit tests for `HindsightManager.initialize()` using monkeypatch on `HindsightEmbedded` and settings values. Assert schema env var is exactly `hindsight` per locked decision in Phase 29 context/research (schema isolation). If needed, make minimal `api/db/hindsight.py` refactor for deterministic test seams (no behavior change). Do not change runtime schema value away from `hindsight`.
  </action>
  <verify>
    <automated>pytest api/tests/test_phase29_memory.py -k "hindsight and initialize" -x</automated>
  </verify>
  <done>Initialize contract covered: success + failure + missing-url paths all asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cold-start lifespan smoke harness</name>
  <files>api/tests/test_phase29_startup_smoke.py, api/main.py</files>
  <behavior>
    - Test 1: clean TestClient startup executes lifespan and reaches ready state.
    - Test 2: startup calls hindsight_db.initialize() exactly once.
    - Test 3: after boot, primary probe (`/` or `/health/db`) returns non-crash response.
  </behavior>
  <action>
    Create `api/tests/test_phase29_startup_smoke.py` integration harness for UAT gap #1. Use monkeypatch/stubs to isolate external network/DB side effects while still executing real FastAPI lifespan. Assert call ordering includes `init_db_pool` then `hindsight_db.initialize` then scheduler start path, and assert primary response contract (`/` 200 with status online, plus `/health/db` structured response). Add minimal non-invasive seam in `api/main.py` only if required to observe startup calls; no feature behavior downgrade.
  </action>
  <verify>
    <automated>pytest api/tests/test_phase29_startup_smoke.py -x</automated>
  </verify>
  <done>Automated cold-start smoke proves boot path + readiness probe + Hindsight init invocation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Startup config -> Memory client | Env/database config crosses into Hindsight embedded initialization. |
| App boot -> Request handling | Lifespan readiness gates first request safety. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05-01 | Denial of Service | `api/main.py` lifespan | mitigate | Add cold-start smoke test that fails CI when startup sequence crashes or never reaches ready probe. |
| T-29-05-02 | Tampering | `api/db/hindsight.py` env contract | mitigate | Assert `HINDSIGHT_API_DATABASE_SCHEMA=hindsight` and initialization failure handling in unit tests. |
| T-29-05-03 | Information Disclosure | startup fallback behavior | mitigate | Keep fail-safe initialize=False path explicit; prevent silent partial init assumptions by testing missing URL branch. |
</threat_model>

<verification>
Run:
- `pytest api/tests/test_phase29_memory.py -k "hindsight and initialize" -x`
- `pytest api/tests/test_phase29_startup_smoke.py -x`
</verification>

<success_criteria>
- UAT gap #1 (cold-start smoke) has executable automated coverage.
- UAT gap #6 (Hindsight startup schema init) has explicit assertions and passing tests.
- No startup crash regression introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/29-memory-storage-foundations/29-05-SUMMARY.md`
</output>
