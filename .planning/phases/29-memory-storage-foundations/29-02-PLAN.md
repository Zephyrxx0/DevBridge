---
phase: 29-memory-storage-foundations
plan: 29-02
type: execute
wave: 2
depends_on: [29-01]
files_modified: [api/agents/state.py, api/db/hindsight.py]
autonomous: true
requirements: [MEM-01, MEM-02]
must_haves:
  truths:
    - "AgentState includes a field for Hindsight memory"
    - "HindsightManager successfully initializes the embedded client"
  artifacts:
    - path: "api/agents/state.py"
      provides: "Structured memory state"
      contains: "hindsight_memory: Optional[str]"
    - path: "api/db/hindsight.py"
      provides: "Hindsight client management"
      contains: "HindsightManager"
---

<objective>
Define the memory state in AgentState and implement the HindsightManager service to manage the embedded memory client.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-memory-storage-foundations/29-01-SUMMARY.md
@api/db/vector_store.py
@api/agents/state.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AgentState with memory field</name>
  <files>api/agents/state.py</files>
  <read_first>
    - api/agents/state.py
    - .planning/phases/29-memory-storage-foundations/29-CONTEXT.md
  </read_first>
  <action>
    Modify `api/agents/state.py` to add `hindsight_memory: Optional[str]` to the `AgentState` TypedDict.
    Ensure `Optional` is imported from `typing`.
    This implements user decision D-02 to keep memory structured and precise.
  </action>
  <verify>
    <automated>grep "hindsight_memory: Optional\[str\]" api/agents/state.py</automated>
  </verify>
  <acceptance_criteria>
    - api/agents/state.py contains 'hindsight_memory: Optional[str]'
  </acceptance_criteria>
  <done>AgentState updated with memory field</done>
</task>

<task type="auto">
  <name>Task 2: Implement HindsightManager service</name>
  <files>api/db/hindsight.py</files>
  <read_first>
    - api/db/vector_store.py
    - api/core/config.py
    - .planning/phases/29-memory-storage-foundations/29-RESEARCH.md
  </read_first>
  <action>
    Create `api/db/hindsight.py` following the singleton manager pattern from `api/db/vector_store.py`.
    Implementation details:
    - Define `HindsightManager` class.
    - `initialize()` method:
      - Uses `hindsight.HindsightEmbedded` (if available in the library) or the equivalent embedded client factory.
      - Sets `database_url` from `settings.sync_supabase_connection_string`.
      - Sets `schema` to "hindsight".
      - Configures LLM access using available settings (Vertex/Google).
    - `reflect()` method: Wrapper for `self._client.reflect()`.
    - Export a singleton instance: `hindsight_db = HindsightManager()`.
  </action>
  <verify>
    <automated>pytest api/tests/test_phase29_memory.py::test_embedded_init -x</automated>
  </verify>
  <acceptance_criteria>
    - api/db/hindsight.py contains 'class HindsightManager'
    - initialize method configures the 'hindsight' schema
    - Exported singleton 'hindsight_db' is present
  </acceptance_criteria>
  <done>HindsightManager implemented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API → Database | Untrusted database schema access (mitigated by isolated schema) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02 | Information Disclosure | Memory Store | mitigate | Strictly configure `bank_id_from_config="user_id"` in subsequent node integration to ensure user isolation. |
</threat_model>

<verification>
Run the initialization test.
</verification>

<success_criteria>
- AgentState has the new field
- HindsightManager initializes without errors
</success_criteria>

<output>
After completion, create .planning/phases/29-memory-storage-foundations/29-02-SUMMARY.md
</output>
