---
phase: 29-memory-storage-foundations
plan: 29-01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, sql/migrations/0032_create_hindsight_schema.sql, api/tests/test_phase29_memory.py]
autonomous: true
requirements: [MEM-02]
must_haves:
  truths:
    - "Hindsight dependencies are installed"
    - "A dedicated 'hindsight' schema exists in the database"
    - "A test scaffold exists for validating memory features"
  artifacts:
    - path: "sql/migrations/0032_create_hindsight_schema.sql"
      provides: "Schema isolation"
      contains: "CREATE SCHEMA IF NOT EXISTS hindsight"
    - path: "api/tests/test_phase29_memory.py"
      provides: "Verification infrastructure"
      min_lines: 20
---

<objective>
Setup the foundation for agent memory by installing dependencies, creating the isolated database schema, and establishing the test scaffold.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-memory-storage-foundations/29-RESEARCH.md
@.planning/phases/29-memory-storage-foundations/29-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Hindsight dependencies</name>
  <files>api/requirements.txt</files>
  <read_first>
    - api/requirements.txt
    - .planning/phases/29-memory-storage-foundations/29-RESEARCH.md
  </read_first>
  <action>
    Add `hindsight-all-slim` and `hindsight-langgraph` to `api/requirements.txt`.
    Run `pip install hindsight-all-slim hindsight-langgraph` in the API environment.
    Note: Using -slim version per research to avoid local ML bloat.
  </action>
  <verify>
    <automated>pip show hindsight-all-slim hindsight-langgraph</automated>
  </verify>
  <acceptance_criteria>
    - api/requirements.txt contains 'hindsight-all-slim'
    - api/requirements.txt contains 'hindsight-langgraph'
    - pip show returns version information for both packages
  </acceptance_criteria>
  <done>Dependencies installed and recorded</done>
</task>

<task type="auto">
  <name>Task 2: Create hindsight schema migration</name>
  <files>sql/migrations/0032_create_hindsight_schema.sql</files>
  <read_first>
    - sql/migrations/0016_add_cache_table.sql
    - .planning/phases/29-memory-storage-foundations/29-CONTEXT.md
  </read_first>
  <action>
    Create a new migration file `sql/migrations/0032_create_hindsight_schema.sql` with the following content:
    ```sql
    -- Create isolated schema for Hindsight memory
    CREATE SCHEMA IF NOT EXISTS hindsight;
    ```
    This ensures Hindsight tables are isolated from application tables per user decision D-01.
  </action>
  <verify>
    <automated>ls sql/migrations/0032_create_hindsight_schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - File sql/migrations/0032_create_hindsight_schema.sql exists
    - File contains 'CREATE SCHEMA IF NOT EXISTS hindsight;'
  </acceptance_criteria>
  <done>Migration file created</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push database schema</name>
  <files>sql/migrations/0032_create_hindsight_schema.sql</files>
  <read_first>
    - sql/migrations/0032_create_hindsight_schema.sql
  </read_first>
  <action>
    Push the new migration to the Supabase instance to create the 'hindsight' schema.
    Command: `supabase db push`
    Note: Ensure SUPABASE_ACCESS_TOKEN is set for non-interactive push.
  </action>
  <verify>
    <automated>psql $SUPABASE_CONNECTION_STRING -c "\dn" | grep hindsight</automated>
  </verify>
  <acceptance_criteria>
    - Output of psql command confirms schema 'hindsight' exists
  </acceptance_criteria>
  <done>Schema created in remote database</done>
</task>

<task type="auto">
  <name>Task 4: Create test scaffold</name>
  <files>api/tests/test_phase29_memory.py</files>
  <read_first>
    - api/tests/test_phase29_memory.py
    - .planning/phases/29-memory-storage-foundations/29-VALIDATION.md
  </read_first>
  <action>
    Create the test file `api/tests/test_phase29_memory.py` with stubs for the following tests:
    - `test_memory_nodes`: Validates LangGraph recall/retain node creation.
    - `test_embedded_init`: Validates HindsightManager initialization.
    - `test_reflect_job_scheduled`: Validates APScheduler integration for reflection.
    Use pytest-asyncio for async tests.
  </action>
  <verify>
    <automated>pytest api/tests/test_phase29_memory.py --collect-only</automated>
  </verify>
  <acceptance_criteria>
    - api/tests/test_phase29_memory.py exists
    - Contains test_memory_nodes, test_embedded_init, and test_reflect_job_scheduled
    - pytest can collect the tests
  </acceptance_criteria>
  <done>Test scaffold established</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API → Database | Untrusted database schema access (mitigated by isolated schema) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Information Disclosure | Database | mitigate | Create dedicated 'hindsight' schema to prevent unintended access to core application tables by Hindsight client. |
</threat_model>

<verification>
Check schema existence and package installation.
</verification>

<success_criteria>
- Hindsight packages show up in pip list
- 'hindsight' schema visible in Supabase
- Test file exists and is recognized by pytest
</success_criteria>

<output>
After completion, create .planning/phases/29-memory-storage-foundations/29-01-SUMMARY.md
</output>
