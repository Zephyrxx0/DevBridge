---
phase: 29-memory-storage-foundations
plan: 29-03
type: execute
wave: 3
depends_on: [29-02]
files_modified: [api/main.py, api/agents/graph.py]
autonomous: true
requirements: [MEM-01, MEM-03]
must_haves:
  truths:
    - "LangGraph includes recall and retain nodes"
    - "Memory is injected into AgentState using a dedicated field"
    - "The reflect() operation runs asynchronously via APScheduler"
  artifacts:
    - path: "api/agents/graph.py"
      provides: "Memory-aware agent graph"
      contains: "create_recall_node"
    - path: "api/main.py"
      provides: "Asynchronous reflection scheduling"
      contains: "hindsight_db.reflect"
---

<objective>
Integrate Hindsight recall and retain nodes into the LangGraph, ensure the user context is passed correctly, and schedule the asynchronous reflection job.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-memory-storage-foundations/29-02-SUMMARY.md
@api/main.py
@api/agents/graph.py
@api/core/scheduler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass user_id in chat endpoints</name>
  <files>api/main.py</files>
  <read_first>
    - api/main.py
    - .planning/phases/29-memory-storage-foundations/29-RESEARCH.md
  </read_first>
  <action>
    Update the `chat` and `chat_stream` endpoints in `api/main.py` to pass the `user_id` from `request.state.user_id` into the LangGraph `config`.
    Example:
    ```python
    user_id = getattr(request.state, "user_id", "default_user")
    config = {"configurable": {"thread_id": request.thread_id, "user_id": user_id}}
    ```
    This ensures that the `recall` and `retain` nodes can isolate memory by user per D-06 and MEM-01.
  </action>
  <verify>
    <automated>grep "user_id\": user_id" api/main.py</automated>
  </verify>
  <acceptance_criteria>
    - api/main.py chat endpoint passes 'user_id' in config
    - api/main.py chat_stream endpoint passes 'user_id' in config
  </acceptance_criteria>
  <done>User context passed to graph execution</done>
</task>

<task type="auto">
  <name>Task 2: Integrate recall and retain nodes into LangGraph</name>
  <files>api/agents/graph.py</files>
  <read_first>
    - api/agents/graph.py
    - api/db/hindsight.py
    - .planning/phases/29-memory-storage-foundations/29-RESEARCH.md
  </read_first>
  <action>
    Modify `api/agents/graph.py` to integrate memory nodes:
    - Import `create_recall_node` and `create_retain_node` from `hindsight_langgraph`.
    - Import `hindsight_db` from `api.db.hindsight`.
    - Create the `recall` node:
      ```python
      recall = create_recall_node(
          client=hindsight_db._client,
          bank_id_from_config="user_id",
          output_key="hindsight_memory"
      )
      ```
    - Create the `retain` node:
      ```python
      retain = create_retain_node(
          client=hindsight_db._client,
          bank_id_from_config="user_id",
          retain_ai=True
      )
      ```
    - Add nodes to the builder: `builder.add_node("recall", recall)`, `builder.add_node("retain", retain)`.
    - Update edges:
      - `builder.add_edge(START, "recall")`
      - `builder.add_edge("recall", "router")`
      - Connect worker nodes to `retain`: `builder.add_edge("fast_worker", "retain")`, `builder.add_edge("big_worker", "retain")`.
      - `builder.add_edge("retain", END)`
    Note: Since `graph` is compiled at import time, ensure `hindsight_db._client` is handled (may need a wrapper if None at import, or initialize HindsightManager at module level if possible).
  </action>
  <verify>
    <automated>pytest api/tests/test_phase29_memory.py::test_memory_nodes -x</automated>
  </verify>
  <acceptance_criteria>
    - api/agents/graph.py adds 'recall' and 'retain' nodes
    - recall node uses 'hindsight_memory' output key
    - Start node leads to recall, and workers lead to retain
  </acceptance_criteria>
  <done>LangGraph memory integration complete</done>
</task>

<task type="auto">
  <name>Task 3: Setup startup initialization and async reflection</name>
  <files>api/main.py</files>
  <read_first>
    - api/main.py
    - api/db/hindsight.py
    - .planning/phases/29-memory-storage-foundations/29-CONTEXT.md
  </read_first>
  <action>
    Update the `lifespan` function in `api/main.py`:
    - Call `hindsight_db.initialize()` during startup.
    - Schedule a periodic background job for reflection using `scheduler_manager.add_job`.
    - Example:
      ```python
      scheduler_manager.add_job(
          hindsight_db.reflect,
          trigger="cron",
          hour="*/4",  # Every 4 hours
          id="hindsight_reflect",
          replace_existing=True
      )
      ```
    This implements user decision D-03 to offload heavy consolidation to idle periods.
  </action>
  <verify>
    <automated>pytest api/tests/test_phase29_memory.py::test_reflect_job_scheduled -x</automated>
  </verify>
  <acceptance_criteria>
    - api/main.py calls hindsight_db.initialize()
    - api/main.py adds a job for hindsight_db.reflect to the scheduler
  </acceptance_criteria>
  <done>Startup hooks and async reflection configured</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User → API | Authenticated user ID used for memory bank isolation |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | Elevation of Privilege | Memory Banks | mitigate | Explicitly pass `user_id` from the authenticated request state to the memory nodes via the graph config, preventing cross-user memory leakage. |
</threat_model>

<verification>
Run all phase tests.
</verification>

<success_criteria>
- End-to-end memory nodes execution test passes
- Reflect job is visible in scheduler
- Chat endpoints pass user context
</success_criteria>

<output>
After completion, create .planning/phases/29-memory-storage-foundations/29-03-SUMMARY.md
</output>
