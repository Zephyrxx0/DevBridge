---
phase: 12-milestone-gap-wiring-ingestion-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "sql/migrations/0017_create_ingestion_jobs.sql",
  "api/ingest/trigger.py",
  "api/agents/orchestrator.py",
  ".planning/phases/03-code-parsing-with-tree-sitter/03-VERIFICATION.md"
]
autonomous: true
requirements: ["MR-01", "FR-AI-02"]

must_haves:
  truths:
    - "Ingestion jobs are tracked in a dedicated table with status and error messages."
    - "GCS object paths are correctly parsed into owner/repo and file_path components."
    - "The search agent (Orchestrator) enforces citations in its system prompt."
    - "Phase 03 has a canonical verification artifact documenting its completion."
  artifacts:
    - path: "sql/migrations/0017_create_ingestion_jobs.sql"
      provides: "Schema for tracking ingestion progress"
    - path: ".planning/phases/03-code-parsing-with-tree-sitter/03-VERIFICATION.md"
      provides: "Proof of completion for code parsing phase"
  key_links:
    - from: "api/ingest/trigger.py"
      to: "ingestion_jobs table"
      via: "SQL upsert in handle_pubsub_event"
    - from: "api/agents/orchestrator.py"
      to: "code_search tool"
      via: "citation enforcement in system prompt"
---

<objective>
Close wiring gaps for the v0.1 milestone audit. This includes implementing repository extraction logic in the ingestion pipeline, tracking ingestion jobs for observability, enforcing citations in the search agent, and finalizing historical documentation.

Purpose: Achieve end-to-end RAG functionality and milestone compliance.
Output: Tracking schema, updated trigger logic, refined agent prompt, and missing verification docs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.1-v0.1-MILESTONE-AUDIT.md
@.planning/phases/12-milestone-gap-wiring-ingestion-search/12-CONTEXT.md
@api/ingest/trigger.py
@api/agents/orchestrator.py
@sql/hybrid_search.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Infrastructure & Documentation Foundation</name>
  <files>
    sql/migrations/0017_create_ingestion_jobs.sql,
    .planning/phases/03-code-parsing-with-tree-sitter/03-VERIFICATION.md
  </files>
  <action>
    1. Create SQL migration `sql/migrations/0017_create_ingestion_jobs.sql` to implement the `ingestion_jobs` table per D-03/D-04.
    2. Apply the migration and also ensure `sql/hybrid_search.sql` is deployed to Supabase.
    3. Generate `.planning/phases/03-code-parsing-with-tree-sitter/03-VERIFICATION.md` by consolidating evidence from 03-01 and 03-02 SUMMARIES. Use the standard VERIFICATION template.
  </action>
  <verify>
    <automated>
      python -c "from api.db.session import get_engine; import asyncio; from sqlalchemy import text; async def check(): engine=get_engine(); async with engine.connect() as conn: await conn.execute(text('SELECT * FROM ingestion_jobs LIMIT 1')); await conn.execute(text('SELECT * FROM hybrid_search(\'test\', 1)')); print('OK'); asyncio.run(check())"
    </automated>
  </verify>
  <done>
    `ingestion_jobs` table exists, `hybrid_search` function exists, and Phase 03 verification doc is present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Ingestion Trigger Logic</name>
  <files>api/ingest/trigger.py</files>
  <action>
    1. Implement repo parsing logic in `_ingest_file` per D-01/D-02. Split `object_name` (GCS objectId) to extract `repo` (owner/repo) and `file_path`.
    2. Update `handle_pubsub_event` to insert a record into `ingestion_jobs` with status 'processing' at the start.
    3. Update `_ingest_file` to update the `ingestion_jobs` record with final status ('success' or 'error'), `chunk_count`, and `error_message` upon completion.
    4. Ensure `GOOGLE_CLOUD_PROJECT` is used for GCP context per D-07.
  </action>
  <verify>
    <automated>
      pytest tests/test_webhooks.py -k "ingest"  # or equivalent trigger test
    </automated>
  </verify>
  <done>
    Ingestion trigger correctly parses repo paths and updates the jobs table.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refine Orchestrator Citation Enforcement</name>
  <files>api/agents/orchestrator.py</files>
  <action>
    1. Update the `Orchestrator.graph` property to include a `state_modifier` (system prompt) that explicitly requires citations (File and Line Numbers) for any information retrieved via `code_search` per D-06.
    2. Ensure the prompt also instructs the agent to use PR/Commit history context when answering "Why" questions.
  </action>
  <verify>
    <automated>
      pytest tests/test_phase07_orchestrator_history.py
    </automated>
  </verify>
  <done>
    Orchestrator system prompt is updated to enforce citations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GCS -> Trigger | Untrusted GCS object IDs are parsed into repository and file paths. |
| DB -> Orchestrator | Retrieved code snippets are treated as grounded context for LLM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | Orchestrator | mitigate | Citations ensure transparency of source data; snippets are bounded in size. |
| T-12-02 | Tampering | SQL Migrations | mitigate | Use parameter binding for job status updates; migration files are version controlled. |
</threat_model>

<verification>
1. Check Supabase dashboard for `ingestion_jobs` table presence.
2. Run a sample ingestion via `curl` or manual Pub/Sub trigger and verify `ingestion_jobs` row update.
3. Query the Orchestrator with a "why" question and verify the response contains "Citations:" with file/line details.
</verification>

<success_criteria>
- [ ] `ingestion_jobs` table exists and tracks processing status.
- [ ] GCS triggers handle `owner/repo/path` format correctly.
- [ ] Search agent responses include explicit file/line citations.
- [ ] Phase 03 verification artifact is canonicalized.
</success_criteria>

<output>
After completion, create `.planning/phases/12-milestone-gap-wiring-ingestion-search/12-01-SUMMARY.md`
</output>
