---
phase: 32
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified: [web/src/components/chat/__tests__/ChatStream.test.tsx, web/tests/escalation-ux.spec.ts, web/src/components/chat/EscalationIndicator.tsx, web/src/components/chat/ChatStream.tsx]
autonomous: false
requirements: [UX-01]
must_haves:
  truths:
    - "Escalation events trigger a visual pulse effect in the chat bubble"
    - "Model labels (Fast Mode / Big Model) are displayed correctly"
  artifacts:
    - path: "web/src/components/chat/EscalationIndicator.tsx"
      provides: "Animated UI feedback"
    - path: "web/tests/escalation-ux.spec.ts"
      provides: "E2E visual verification"
  key_links:
    - from: "web/src/components/chat/EscalationIndicator.tsx"
      to: "web/src/components/chat/ChatStream.tsx"
      via: "component integration"
---

<objective>
Implement the visual escalation indicators in the chat UI, providing users with clear feedback on model transitions and escalation states.

Purpose: Improve user trust and transparency regarding AI reasoning and cost/quality tradeoffs.
Output: Animated escalation indicators and model badges in the chat interface.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-streaming-escalation-ux/32-01-SUMMARY.md
@web/src/components/chat/ChatStream.tsx
@web/src/components/chat/types.ts

<interfaces>
From 32-01-PLAN:
```typescript
export interface Message {
  // ...
  model_used?: string;
  cascaded?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementation & Component Wiring</name>
  <files>web/src/components/chat/EscalationIndicator.tsx, web/src/components/chat/ChatStream.tsx, web/src/components/chat/__tests__/ChatStream.test.tsx</files>
  <action>
    1. Create `EscalationIndicator.tsx` to render a status dot with a pulse animation (amber for `cascaded`, brand-color otherwise) and model label.
    2. Integrate `EscalationIndicator` into `ChatStream.tsx`, replacing the legacy `fallback` Badge. Pass `message.model_used` and `message.cascaded` props.
    3. Create `web/src/components/chat/__tests__/ChatStream.test.tsx` to verify that the indicator renders when metadata is present and handles missing metadata gracefully.
  </action>
  <verify>
    <automated>npm run build --prefix web && npm run test --prefix web -- ChatStream</automated>
  </verify>
  <done>Escalation UI is implemented and integrated with unit test coverage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual UX Verification</name>
  <what-built>Real-time escalation UI with pulse animations and model badges.</what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` in `web/` and `python api/main.py`.
    2. Open the browser and start a chat session.
    3. Ask a complex question that triggers Cascadeflow escalation.
    4. Verify that:
       - A "Fast Mode" indicator appears initially.
       - Upon escalation, it shifts to "Big Model" with an amber pulse animation.
       - The transition is smooth and visually consistent with the brand.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto">
  <name>Task 3: E2E Validation & Graph Maintenance</name>
  <files>web/tests/escalation-ux.spec.ts</files>
  <action>
    1. Create and implement E2E tests in `web/tests/escalation-ux.spec.ts` using Playwright to mock SSE events with `cascaded: true`.
    2. Verify the `EscalationIndicator` appears in the DOM with correct animation classes.
    3. Run `graphify update .` to refresh the codebase knowledge graph with the new UI components and relationships.
  </action>
  <verify>
    <automated>npx playwright test web/tests/escalation-ux.spec.ts && graphify update .</automated>
  </verify>
  <done>E2E tests pass and knowledge graph is updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A (UI-only)

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02 | Spoofing | Chat UI | accept | Low risk; model indicators are informational and don't affect security boundaries. |
</threat_model>

<verification>
Run `npx playwright test web/tests/escalation-ux.spec.ts` for automated verification.
Perform manual check (Task 2) for "feel" and animation quality.
</verification>

<success_criteria>
- Assistant messages show "Fast Mode" or "Big Model" based on metadata.
- Cascaded responses show an amber pulse animation.
- Playwright tests pass for both standard and escalated responses.
- Codebase knowledge graph is up to date.
</success_criteria>

<output>
After completion, create `.planning/phases/32-streaming-escalation-ux/32-02-SUMMARY.md`
</output>
