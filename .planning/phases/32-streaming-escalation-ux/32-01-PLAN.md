---
phase: 32
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api/tests/test_phase32_sse.py, api/main.py, web/src/components/chat/types.ts, web/src/app/repo/[id]/page.tsx]
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - "Backend emits SSE metadata events containing model_used and cascaded"
    - "Frontend parses enriched metadata and updates assistant message state"
  artifacts:
    - path: "api/tests/test_phase32_sse.py"
      provides: "SSE metadata verification"
    - path: "web/src/components/chat/types.ts"
      provides: "Updated Message interface"
  key_links:
    - from: "api/main.py"
      to: "SSE stream"
      via: "yield metadata events"
    - from: "SSE"
      to: "web/src/app/repo/[id]/page.tsx"
      via: "event loop subscription"
---

<objective>
Enhance the SSE streaming protocol to include model escalation metadata (model_used, cascaded) and update the frontend state management to store these fields.

Purpose: Provide the data layer needed for real-time escalation UX.
Output: Enriched SSE stream and updated frontend message state.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/main.py
@web/src/components/chat/types.ts
@web/src/app/repo/[id]/page.tsx

<interfaces>
From web/src/components/chat/types.ts:
```typescript
export interface Message {
  role: "user" | "assistant";
  content: string;
  fallback?: boolean;
  sources?: SourceReference[];
  artifacts?: SnippetChip[];
}
```

Metadata event structure (target):
```json
{
  "type": "metadata",
  "fallback": boolean,
  "model_used": string,
  "cascaded": boolean
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create SSE validation scaffold</name>
  <files>api/tests/test_phase32_sse.py</files>
  <action>
    Create a test file to verify that the SSE generator correctly extracts and yields model metadata.
    Include a mock event stream that simulates a LangGraph event with 'model_used' and 'cascaded' in the output.
    Test that the metadata event is yielded with the correct fields.
  </action>
  <verify>
    <automated>pytest api/tests/test_phase32_sse.py</automated>
  </verify>
  <done>SSE validation test exists and passes with stubs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Enrich SSE metadata in backend</name>
  <files>api/main.py</files>
  <behavior>
    - Refactor `_contains_fallback` to `_extract_metadata(value) -> dict`.
    - `_extract_metadata` should recursively find `fallback`, `model_used`, and `cascaded` in the event data.
    - Update `event_generator` to yield a `metadata` event whenever new metadata is detected in the graph events.
    - Ensure T-32-01 mitigation: only expose specific fields, sanitize other data.
  </behavior>
  <action>
    Implement `_extract_metadata` in `api/main.py`.
    Update the `async for event in stream_graph_events(...)` loop to track sent metadata and yield updates.
    Maintain backward compatibility with the existing `fallback` field.
  </action>
  <verify>
    <automated>pytest api/tests/test_phase32_sse.py</automated>
  </verify>
  <done>SSE metadata events now contain model_used and cascaded fields.</done>
</task>

<task type="auto">
  <name>Task 2: Update frontend state for escalation metadata</name>
  <files>web/src/components/chat/types.ts, web/src/app/repo/[id]/page.tsx</files>
  <action>
    1. Add `model_used?: string` and `cascaded?: boolean` to the `Message` interface in `web/src/components/chat/types.ts`.
    2. Update the SSE event loop in `web/src/app/repo/[id]/page.tsx` to handle the enriched `metadata` event.
    3. Update the last assistant message in state with `model_used` and `cascaded` values when received.
    4. Ensure `fallback` logic is preserved.
  </action>
  <verify>
    <automated>npm run build --prefix web && graphify update .</automated>
  </verify>
  <done>Frontend message state correctly captures and stores model escalation metadata from SSE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → SSE Client | Model metadata crosses here; untrusted internal model state must not leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure | `api/main.py` (SSE) | mitigate | Sanitize metadata: only include explicitly allowed fields (`fallback`, `model_used`, `cascaded`). |
</threat_model>

<verification>
Run `pytest api/tests/test_phase32_sse.py` to verify backend metadata emission.
Check `web` build to ensure type safety.
</verification>

<success_criteria>
- Backend emits SSE events with `{"type": "metadata", "model_used": "...", "cascaded": true}`.
- Frontend `messages` state contains these fields after a chat turn.
</success_criteria>

<output>
After completion, create `.planning/phases/32-streaming-escalation-ux/32-01-SUMMARY.md`
</output>
