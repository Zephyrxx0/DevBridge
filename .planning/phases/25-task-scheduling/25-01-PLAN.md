---
phase: 25-task-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [sql/migrations/0030_add_job_history_table.sql, api/core/config.py, api/db/session.py, api/jobs/base.py, api/core/scheduler.py, api/main.py]
autonomous: true
requirements: [FR-05, FR-06]
must_haves:
  truths:
    - "job_history table exists in database with status tracking columns"
    - "Scheduler initializes with SQLAlchemyJobStore using persistent Postgres store"
    - "Distributed locking prevents concurrent execution of the same job across instances"
  artifacts:
    - path: "sql/migrations/0030_add_job_history_table.sql"
      provides: "Persistence for job audit logs"
    - path: "api/jobs/base.py"
      provides: "with_distributed_lock and job_audit decorators"
    - path: "api/core/scheduler.py"
      provides: "AsyncIOScheduler lifecycle manager"
---

<objective>
Setup the persistent scheduling infrastructure for background tasks, including distributed locking for multi-instance safety and audit logging for observability.

Purpose: Ensure jobs run reliably once per schedule regardless of how many API instances are active.
Output: Persistent job store, advisory locking utility, and job history tracking.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-task-scheduling/25-CONTEXT.md
@.planning/phases/25-task-scheduling/25-RESEARCH.md
@api/main.py
@api/db/session.py
@api/core/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database Migration for Job History</name>
  <files>sql/migrations/0030_add_job_history_table.sql</files>
  <action>
    Create a new migration file to define the `job_history` table per D-04.
    Required columns:
    - id: UUID PRIMARY KEY (default gen_random_uuid())
    - job_id: TEXT NOT NULL (matches APScheduler job ID)
    - status: TEXT NOT NULL (running, success, failed)
    - start_time: TIMESTAMPTZ NOT NULL
    - end_time: TIMESTAMPTZ
    - duration_seconds: FLOAT
    - error_message: TEXT
    - node_id: TEXT (identifies the instance executing the job)
    - created_at: TIMESTAMPTZ DEFAULT NOW()
  </action>
  <verify>
    Run `python apply_migration.py` and verify table exists via `psql` or `python -c "from api.db.session import get_engine; import asyncio; from sqlalchemy import text; async def check(): engine=get_engine(); async with engine.connect() as c: print((await c.execute(text('SELECT count(*) FROM job_history'))).scalar()); asyncio.run(check())"`
  </verify>
  <done>job_history table created and accessible.</done>
</task>

<task type="auto">
  <name>Task 2: Sync Connection String & Locking Utilities</name>
  <files>api/core/config.py, api/db/session.py, api/jobs/base.py</files>
  <action>
    - Update `api/db/session.py`: Add `normalize_sync_url(connection_string: str) -> str` that returns a URL with `postgresql+psycopg://` scheme for APScheduler 3.x sync JobStore.
    - Update `api/core/config.py`: Add `sync_supabase_connection_string` property to `Settings` class using the new normalization helper (D-01).
    - Create `api/jobs/base.py`: Implement `with_distributed_lock` decorator per D-03.
      - Use `hashlib.sha256(lock_name.encode()).digest()[:8]` to generate a 64-bit integer for `pg_try_advisory_xact_lock`.
      - Use an async connection from `get_engine()` to execute the lock check.
    - Implement `job_audit` decorator in `api/jobs/base.py` per D-04 to log "running", "success", or "failed" states to `job_history`.
  </action>
  <verify>
    Create a temporary test script to verify `with_distributed_lock` prevents concurrent execution when two tasks use the same lock name.
  </verify>
  <done>Sync URL support and distributed locking decorators implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Scheduler Lifecycle Integration</name>
  <files>api/core/scheduler.py, api/main.py</files>
  <action>
    - Create `api/core/scheduler.py`: Implement `SchedulerManager` class.
      - Initialize `AsyncIOScheduler` with `SQLAlchemyJobStore` using `settings.sync_supabase_connection_string` (D-01, D-02).
      - Ensure jobstore table name is configured if needed (default is fine).
      - Add methods for `start()`, `shutdown()`, and `add_job()`.
    - Update `api/main.py`:
      - Import `SchedulerManager`.
      - Update `lifespan` context manager to initialize, start, and shutdown the `SchedulerManager`.
      - Remove the existing inline `AsyncIOScheduler` and the temporary `sync_issues` job registration (this will be moved to Plan 02).
  </action>
  <verify>
    Start the API server (`python api/run_server.py`) and verify logs show "APScheduler started" and no errors related to the JobStore connection. Verify `apscheduler_jobs` table is created in Postgres.
  </verify>
  <done>APScheduler integrated into FastAPI lifespan with persistent JobStore.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler → Database | Connection string must be protected; JobStore uses sync driver. |
| Job → Database | Advisory locks rely on shared Postgres instance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Elevation of Privilege | job_history table | mitigate | Ensure RLS or restricted DB user access for job logging. |
| T-25-02 | Denial of Service | Scheduler | mitigate | Use `max_instances` in APScheduler to prevent job pile-up. |
| T-25-03 | Tampering | Job arguments | mitigate | Pickled arguments in JobStore; only pass primitive IDs (strings/ints). |
</threat_model>

<verification>
Verify persistent scheduling by restarting the API server and checking that the jobstore table persists and the scheduler resumes.
</verification>

<success_criteria>
1. `job_history` table populated on job runs.
2. `apscheduler_jobs` table exists in Supabase.
3. API lifespan starts/stops scheduler cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/25-task-scheduling/25-01-SUMMARY.md`
</output>
