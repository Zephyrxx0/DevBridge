---
phase: 25-task-scheduling
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified: [api/jobs/sync.py, api/jobs/cleanup.py, api/jobs/metrics.py, api/reports/generator.py, api/reports/hub.py, api/routes/admin.py, api/main.py, docker-compose.yml]
autonomous: true
requirements: [FR-05, FR-06]
must_haves:
  truths:
    - "Daily GitHub sync runs and logs to job_history"
    - "Cache cleanup job prunes old files from /app/repo_cache"
    - "Admin API serves daily reports summarizing system usage"
    - "/app/reports persists across container restarts"
  artifacts:
    - path: "api/jobs/sync.py"
      provides: "GitHub issue synchronization job"
    - path: "api/jobs/cleanup.py"
      provides: "Automated storage maintenance"
    - path: "api/reports/hub.py"
      provides: "Storage and retrieval of generated reports"
    - path: "api/routes/admin.py"
      provides: "Manual trigger and visibility interface"
  key_links:
    - from: "api/jobs/sync.py"
      to: "api/core/scheduler.py"
      via: "Job registration"
      pattern: "scheduler\\.add_job"
    - from: "api/reports/generator.py"
      to: "api/reports/hub.py"
      via: "File persistence"
      pattern: "reports_hub\\.save"
    - from: "api/routes/admin.py"
      to: "api/reports/hub.py"
      via: "Service injection"
      pattern: "get_reports_hub"
---

<objective>
Implement the core job inventory (Sync, Cleanup, Metrics) and establish the Reports Hub for automated intelligence delivery and system monitoring.

Purpose: Automate repetitive maintenance tasks and provide visibility into system health and usage.
Output: Suite of background jobs and an admin interface for reports and job tracking.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-task-scheduling/25-CONTEXT.md
@.planning/phases/25-task-scheduling/25-RESEARCH.md
@api/main.py
@api/core/config.py
@api/jobs/base.py (created in 25-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Job Implementation</name>
  <files>api/jobs/sync.py, api/jobs/cleanup.py, api/jobs/metrics.py</files>
  <action>
    - Create `api/jobs/sync.py`: Move and refactor the `sync_issues` logic from `api/main.py`.
      - Apply `@job_audit`, `@with_retry`, and `@with_distributed_lock("sync_issues")`.
      - Ensure it handles OAuth tokens via `get_github_token` correctly.
    - Create `api/jobs/cleanup.py`: Implement `cleanup_job()`.
      - Scans `settings.repo_cache_dir` (e.g., `/app/repo_cache`) and deletes subdirectories not accessed in the last 7 days.
      - Apply `@job_audit` and `@with_distributed_lock("cache_cleanup")`.
    - Create `api/jobs/metrics.py`: Implement `collect_daily_metrics()`.
      - Query `questions` and `chat_messages` tables for counts in the last 24 hours.
      - Aggregate most common repo_id and user_id (if available).
      - Apply `@job_audit` and `@with_distributed_lock("metrics_collection")`.
  </action>
  <verify>
    Run `pytest tests/test_jobs.py` (scaffolded in Wave 0 or created here) to verify each job executes without error and logs 'success' to `job_history`.
  </verify>
  <done>Daily sync, cleanup, and metrics jobs implemented with auditing and locking.</done>
</task>

<task type="auto">
  <name>Task 2: Reports Hub & Generator with AI Summaries</name>
  <files>api/reports/generator.py, api/reports/hub.py, docker-compose.yml</files>
  <action>
    - Create `api/reports/hub.py`: Implement `ReportsHub` class per D-05.
      - Methods to save report strings (Markdown/JSON) to `/app/reports`.
      - Method to list available reports sorted by date.
      - Method to retrieve report content by filename.
    - Update `docker-compose.yml`: Ensure `/app/reports` is mapped to persistent storage (e.g. a named volume `reports_data` or a host bind to `./reports`) per D-05.
    - Create `api/reports/generator.py`: Implement `generate_daily_intelligence_report()`.
      - Uses data from `collect_daily_metrics()` and query logs to identify "Intern Confusion" topics (D-06, FR-06).
      - **Use the Fast Model (Gemma-4 via Vertex AI or local endpoint)** to summarize the query logs and generate the "Intern Confusion" insights per FR-06.
      - Formats findings into a Markdown report.
    - Create a wrapper job in `api/jobs/reports.py` that calls the generator and saves via the hub. Apply `@job_audit` and `@with_retry`.
  </action>
  <verify>
    Trigger the report generation job manually and verify a new file exists in `/app/reports` with AI-generated summaries.
  </verify>
  <done>Reports Hub and Daily Intelligence generator (using Gemma-4) established with persistence.</done>
</task>

<task type="auto">
  <name>Task 3: Admin API & Registration</name>
  <files>api/routes/admin.py, api/main.py</files>
  <action>
    - Create `api/routes/admin.py`: Implement FastAPI router.
      - `GET /admin/jobs/history`: Returns last 50 entries from `job_history`.
      - `POST /admin/jobs/{job_id}/run`: Manually triggers a job via the scheduler.
      - `GET /admin/reports`: Lists available reports from `ReportsHub`.
      - `GET /admin/reports/{filename}`: Returns report content.
    - Update `api/main.py`:
      - Import `api/jobs` and register the daily jobs (sync, cleanup, metrics, reports) with the scheduler in `lifespan`.
      - Register the `admin` router: `app.include_router(admin.router, prefix="/admin", tags=["Admin"])`.
  </action>
  <verify>
    Verify endpoints respond correctly via `curl` or FastAPI docs (`/docs`).
  </verify>
  <done>Admin visibility interface and job scheduling fully wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API → Reports | Access to internal file system (/app/reports). |
| Admin API → Scheduler | Permission to trigger jobs manually. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Information Disclosure | Admin API | mitigate | Restrict admin routes to `INTERNAL_AUTH_TOKEN` or specific User ID. |
| T-25-05 | Tampering | Report filenames | mitigate | Sanitize filenames in `GET /admin/reports/{filename}` to prevent path traversal. |
| T-25-06 | DoS | Manual Job Trigger | mitigate | Implement rate limiting or strict auth on manual triggers. |
</threat_model>

<verification>
E2E verification:
1. Trigger Daily Sync via Admin API.
2. Check `job_history` for success.
3. Check `repo_github_issues` for updated data.
4. Download the Daily Intelligence report from the Admin API.
</verification>

<success_criteria>
1. All core jobs scheduled and running.
2. Reports Hub serving Markdown reports.
3. Admin dashboard endpoints functional and secured.
</success_criteria>

<output>
After completion, create `.planning/phases/25-task-scheduling/25-02-SUMMARY.md`
</output>
