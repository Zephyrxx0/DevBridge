---
phase: 25-task-scheduling
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified: [api/jobs/sync.py, api/jobs/cleanup.py, api/jobs/metrics.py, api/reports/generator.py, api/reports/hub.py, api/routes/admin.py, api/main.py, docker-compose.yml]
autonomous: true
requirements: [FR-05, FR-06]
must_haves:
  truths:
    - "Daily GitHub and external docs sync runs and logs to job_history"
    - "Cache cleanup job prunes local repo_cache and GCS temporary files"
    - "Daily and Weekly intelligence reports generated using Gemma-4 summaries"
    - "Admin API serves reports summarizing system usage"
    - "/app/reports persists across container restarts"
  artifacts:
    - path: "api/jobs/sync.py"
      provides: "GitHub and external docs synchronization job"
    - path: "api/jobs/cleanup.py"
      provides: "Local and GCS storage maintenance"
    - path: "api/reports/generator.py"
      provides: "Daily and Weekly automated intelligence reports"
    - path: "api/reports/hub.py"
      provides: "Storage and retrieval of generated reports"
    - path: "api/routes/admin.py"
      provides: "Manual trigger and visibility interface"
  key_links:
    - from: "api/jobs/sync.py"
      to: "api/core/scheduler.py"
      via: "Job registration"
      pattern: "scheduler\\.add_job"
    - from: "api/reports/generator.py"
      to: "api/reports/hub.py"
      via: "File persistence"
      pattern: "reports_hub\\.save"
    - from: "api/routes/admin.py"
      to: "api/reports/hub.py"
      via: "Service injection"
      pattern: "get_reports_hub"
---

<objective>
Implement the core job inventory (Sync, Cleanup, Metrics) and establish the Reports Hub for automated intelligence delivery and system monitoring.

Purpose: Automate repetitive maintenance tasks and provide visibility into system health and usage.
Output: Suite of background jobs (including external docs sync and GCS cleanup) and an admin interface for reports and job tracking.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-task-scheduling/25-CONTEXT.md
@.planning/phases/25-task-scheduling/25-RESEARCH.md
@api/main.py
@api/core/config.py
@api/jobs/base.py (created in 25-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Job Implementation (Sync & Cleanup Expansion)</name>
  <files>api/jobs/sync.py, api/jobs/cleanup.py, api/jobs/metrics.py</files>
  <action>
    - Create `api/jobs/sync.py`: Implement `sync_github_and_docs_job`.
      - Refactor `sync_issues` from `api/main.py` with `@job_audit`, `@with_retry`, and `@with_distributed_lock("sync_issues")`.
      - Add logic to fetch content from `EXTERNAL_DOC_URLS` (defined in settings/config) and update the local knowledge base (D-06).
    - Create `api/jobs/cleanup.py`: Implement `cleanup_job()`.
      - Scans `settings.repo_cache_dir` and deletes subdirectories not accessed in >7 days.
      - Scans `gs://{bucket}/temp` using the GCS SDK and deletes files older than 24 hours (D-06).
      - Apply `@job_audit` and `@with_distributed_lock("cache_cleanup")`.
    - Create `api/jobs/metrics.py`: Implement `collect_daily_metrics()`.
      - Query `questions` and `chat_messages` tables for last 24h counts.
      - Apply `@job_audit` and `@with_distributed_lock("metrics_collection")`.
  </action>
  <verify>
    <automated>pytest tests/test_jobs.py::test_sync_job_github_and_docs tests/test_jobs.py::test_cleanup_job_gcs_and_local tests/test_jobs.py::test_metrics_collection</automated>
  </verify>
  <done>Sync (GitHub+Docs), Cleanup (Local+GCS), and metrics jobs implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Reports Hub & Daily/Weekly AI Summaries</name>
  <files>api/reports/generator.py, api/reports/hub.py, docker-compose.yml</files>
  <action>
    - Create `api/reports/hub.py`: Implement `ReportsHub` class per D-05.
      - Methods to save/list/retrieve Markdown/JSON reports in `/app/reports`.
    - Update `docker-compose.yml`: Ensure `/app/reports` is a persistent volume (D-05).
    - Create `api/reports/generator.py`:
      - `generate_daily_intelligence_report()`: Identify "Intern Confusion" topics and summarize query logs using **Gemma-4 via Vertex AI** (FR-06, D-06).
      - `generate_weekly_report()`: Aggregate the last 7 daily reports/metrics into a trend summary (D-06).
    - Create a wrapper job in `api/jobs/reports.py` that schedules daily and weekly generations and saves them via `ReportsHub`.
  </action>
  <verify>
    <automated>pytest tests/test_jobs.py::test_reports_hub_storage tests/test_jobs.py::test_weekly_report_generation</automated>
  </verify>
  <done>Reports Hub and Daily/Weekly Intelligence generators (using Gemma-4) established.</done>
</task>

<task type="auto">
  <name>Task 3: Admin API, Registration & Graphify Update</name>
  <files>api/routes/admin.py, api/main.py</files>
  <action>
    - Create `api/routes/admin.py`: Implement FastAPI router.
      - `GET /admin/jobs/history`, `POST /admin/jobs/{job_id}/run`.
      - `GET /admin/reports`, `GET /admin/reports/{filename}`.
    - Update `api/main.py`:
      - Import `api/jobs` and register daily (sync, cleanup, metrics, daily_report) and weekly (weekly_report) jobs in `lifespan`.
      - Register the `admin` router: `app.include_router(admin.router, prefix="/admin", tags=["Admin"])`.
    - Run `graphify update .` to refresh the knowledge graph with the new scheduling and reporting structures.
  </action>
  <verify>
    <automated>pytest tests/test_jobs.py::test_admin_manual_trigger && graphify update .</automated>
  </verify>
  <done>Admin visibility interface and job scheduling fully wired; knowledge graph updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API → Reports | Access to internal file system (/app/reports). |
| Admin API → Scheduler | Permission to trigger jobs manually. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Information Disclosure | Admin API | mitigate | Restrict admin routes to `INTERNAL_AUTH_TOKEN` or specific User ID. |
| T-25-05 | Tampering | Report filenames | mitigate | Sanitize filenames in `GET /admin/reports/{filename}` to prevent path traversal. |
| T-25-06 | DoS | Manual Job Trigger | mitigate | Implement rate limiting or strict auth on manual triggers. |
</threat_model>

<verification>
E2E verification:
1. Trigger Daily Sync via Admin API.
2. Check `job_history` for success.
3. Check `repo_github_issues` for updated data.
4. Download the Daily Intelligence report from the Admin API.
</verification>

<success_criteria>
1. All core jobs scheduled and running (Sync, Cleanup, Metrics, Daily/Weekly Reports).
2. Reports Hub serving Markdown reports.
3. Admin dashboard endpoints functional and secured.
4. Graphify knowledge graph updated.
</success_criteria>

<output>
After completion, create `.planning/phases/25-task-scheduling/25-02-SUMMARY.md`
</output>
