---
phase: 27-model-migration-replace-qwen-with-google-ai-studio-gemini-an
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified: [docker-compose.yml, api/core/config.py, scripts/download_models.sh, Dockerfile, api/Dockerfile]
autonomous: true
requirements: [IR-01]

must_haves:
  truths:
    - "Local vLLM services (vllm-deep, vllm-fast) are completely removed from docker-compose.yml (D-05)"
    - "Model weights (Qwen, Gemma) are deleted from host filesystem to recover space (D-05)"
    - "Dockerfile is confirmed clean of ROCm/AMD-specific layers (D-05)"
    - "Legacy VRAM partitioning and port settings are removed from codebase (D-05)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Lean container configuration without local inference services"
    - path: "api/core/config.py"
      provides: "Configuration without legacy vLLM settings"
  key_links:
    - from: "docker-compose.yml"
      to: "host disk"
      via: "volume mounts (removed)"
---

<objective>
Decommission local AMD GPU inference infrastructure and clean up model artifacts (Qwen/Gemma weights) to recover disk space and simplify the deployment.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/27-model-migration-replace-qwen-with-google-ai-studio-gemini-an/27-CONTEXT.md
@docker-compose.yml
@api/core/config.py
@scripts/download_models.sh
@Dockerfile
@api/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker and Infra Decommissioning</name>
  <files>docker-compose.yml, api/core/config.py, Dockerfile, api/Dockerfile</files>
  <action>
    1. Remove `vllm-deep` and `vllm-fast` services from `docker-compose.yml`.
    2. Remove ROCm/AMD-specific lines from `docker-compose.yml` (e.g., `devices`, `group_add`, `ipc: host`).
    3. Audit `Dockerfile` and `api/Dockerfile` to ensure no ROCm/AMD base images or layers remain (per D-05).
    4. Update `api/core/config.py`:
       - Remove `BIG_MODEL_PORT`, `FAST_MODEL_PORT`, `FAST_MODEL_TIMEOUT`, and `BIG_MODEL_TIMEOUT` constants.
       - Clean up any unused VRAM partitioning logic or comments.
  </action>
  <verify>
    <automated>! (docker compose config --services | grep -q "vllm") && ! (grep -q "devices:" docker-compose.yml)</automated>
  </verify>
  <done>Local vLLM services and ROCm/AMD infra removed from container orchestration.</done>
</task>

<task type="auto">
  <name>Task 2: Weight and Script Purge</name>
  <files>scripts/download_models.sh</files>
  <action>
    1. Delete `scripts/download_models.sh` (per D-05).
    2. Delete the large model weights from the host's `/app/repo_cache/qwen` and `/app/repo_cache/gemma` directories (if accessible via the agent's environment, otherwise provide command for user to run).
    3. Note: If `/app/repo_cache` is a mounted volume, the deletion must be verified on the mount point.
  </action>
  <verify>
    <automated>ls scripts/download_models.sh 2>/dev/null || echo "Deleted script"; [ ! -d "/app/repo_cache/qwen" ] && [ ! -d "/app/repo_cache/gemma" ]</automated>
  </verify>
  <done>Local model weights and download scripts purged (D-05).</done>
</task>

<task type="auto">
  <name>Task 3: Final Infrastructure Review and Health Check</name>
  <files>docker-compose.yml, .env.example</files>
  <action>
    1. Ensure `.env.example` is updated to include `GEMINI_API_KEY` and remove any legacy vLLM port settings.
    2. Verify that `docker-compose.yml` only contains the essential `api` and `db` services.
    3. Run `npx --yes fallow --production` to ensure no unused configuration remains.
    4. Run `graphify update .` to finalise the graph state for the next phase.
  </action>
  <verify>
    <automated>grep "GEMINI_API_KEY" .env.example && npx --yes fallow --production</automated>
  </verify>
  <done>Infrastructure configuration cleaned, documented, and health-checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Host -> Docker | Removal of privileged device access (KFD/DRI) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Denial of Service | `docker-compose.yml` | mitigate | Ensure removal of legacy services doesn't break dependencies for the `api` service. |
</threat_model>

<verification>
Run `docker compose up --build` and verify only `api` and `db` start. Check disk space on host to confirm weight purge.
</verification>

<success_criteria>
- No local vLLM services exist in `docker-compose.yml`.
- `scripts/download_models.sh` is removed.
- Local model weight directories are cleared.
- VRAM partitioning config is removed from code.
- ROCm/AMD hardware access is removed from Docker configs.
</success_criteria>

<output>
After completion, create `.planning/phases/27-model-migration-replace-qwen-with-google-ai-studio-gemini-an/27-02-SUMMARY.md`
</output>
