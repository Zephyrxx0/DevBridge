---
phase: 27-model-migration-replace-qwen-with-google-ai-studio-gemini-an
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api/requirements.txt, api/core/config.py, api/agents/utils/llm.py, api/utils/tokenizer.py, tests/test_model_migration.py, api/agents/nodes/router.py, api/agents/nodes/big.py, api/agents/nodes/fast.py]
autonomous: true
requirements: [MR-01, MR-04]
user_setup:
  - service: Google AI Studio
    why: "Model inference via Gemini and Gemma API"
    env_vars:
      - name: GEMINI_API_KEY
        source: "https://aistudio.google.com/app/apikey"

must_haves:
  truths:
    - "Model inference uses Google AI Studio Gemini/Gemma models (D-01, D-02)"
    - "SDK is updated to google-genai Python SDK (D-03)"
    - "Thinking configuration is correctly applied (thinking_budget=-1 for Flash, thinking_level=HIGH for Gemma) (D-01, D-02)"
  artifacts:
    - path: "api/agents/utils/llm.py"
      provides: "Gemini SDK wrapper and model factory"
    - path: "tests/test_model_migration.py"
      provides: "Automated verification of migration"
  key_links:
    - from: "api/agents/nodes/big.py"
      to: "api/agents/utils/llm.py"
      via: "get_model call"
---

<objective>
Transition the application's model inference from local vLLM endpoints to Google AI Studio using the new `google-genai` SDK. This replaces the Qwen-72B and Gemma-9B local models with Gemini 2.5 Flash and Gemma 4 via Cloud API.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/27-model-migration-replace-qwen-with-google-ai-studio-gemini-an/27-CONTEXT.md
@api/agents/utils/llm.py
@api/core/config.py
@api/utils/tokenizer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffolding and Dependencies</name>
  <files>api/requirements.txt, tests/test_model_migration.py</files>
  <action>
    1. Add `google-genai` to `api/requirements.txt`.
    2. Create `tests/test_model_migration.py` with mock tests for `google.genai.Client` to verify the new integration logic without actual API calls initially.
    3. Ensure `pytest` is configured to run the new test file.
  </action>
  <verify>
    <automated>pip install -r api/requirements.txt && pytest tests/test_model_migration.py</automated>
  </verify>
  <done>Dependencies installed and test scaffold ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Config and SDK Integration</name>
  <files>api/core/config.py, api/agents/utils/llm.py</files>
  <behavior>
    - `settings.gemini_api_key` should load from `.env` or environment.
    - `get_model(is_fast=False)` should return a model configured with 'gemini-2.5-flash' and thinking_budget=-1.
    - `get_model(is_fast=True)` (or for analysis) should return a model configured with 'gemma-4-26b-a4b-it' and thinking_level='HIGH'.
  </behavior>
  <action>
    1. Add `gemini_api_key: str | None = Field(default=None, validation_alias="GEMINI_API_KEY")` to `api/core/config.py:Settings`.
    2. In `api/agents/utils/llm.py`, implement a `GeminiModel` class (or wrapper) that uses `google.genai.Client`.
    3. The wrapper must implement an `ainvoke` (or similar) method compatible with current agent nodes (`api/agents/nodes/big.py`, etc.).
    4. Implement the logic for `thinking_config` based on the model type per D-01 and D-02.
    5. Update `get_model` to return the new `GeminiModel` instances.
  </action>
  <verify>
    <automated>pytest tests/test_model_migration.py -k "test_get_model"</automated>
  </verify>
  <done>Google AI Studio SDK integrated and configured via environment variables.</done>
</task>

<task type="auto">
  <name>Task 3: Tokenizer, Node Alignment, and Quality Checks</name>
  <files>api/utils/tokenizer.py, api/agents/nodes/router.py, api/agents/nodes/big.py, api/agents/nodes/fast.py</files>
  <action>
    1. Update `api/utils/tokenizer.py`:
       - Remove `transformers` dependency and `AutoTokenizer` usage.
       - Implement `_count_tokens` using the `google-genai` SDK's `client.models.count_tokens` (SDK-native).
       - Ensure `enforce_cap` is updated to handle the new counting logic efficiently (e.g., caching or batching if appropriate, though single calls are likely fine for start).
    2. Verify and update `router.py`, `big.py`, and `fast.py` nodes to ensure they correctly process the response content from the new `GeminiModel` (e.g., ensuring they access `result.content` correctly).
    3. Run `npx --yes fallow --production` to check for dead code (like legacy vLLM helpers).
    4. Run `graphify update .` to refresh the knowledge graph with new SDK patterns.
  </action>
  <verify>
    <automated>pytest tests/test_model_migration.py && npx --yes fallow --production</automated>
  </verify>
  <done>Application logic aligned with Cloud API response formats, tokenizer refactored to SDK-native counting, and codebase quality verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> Google AI Studio | Outbound API calls with sensitive API Key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information Disclosure | `api/core/config.py` | mitigate | Use Pydantic `SecretStr` or simply ensure API Key is never logged. |
| T-27-02 | Denial of Service | `api/agents/utils/llm.py` | mitigate | Implement timeouts and retries for Google AI Studio API calls. |
</threat_model>

<verification>
Run `pytest tests/test_model_migration.py` and ensure the application starts without attempting to connect to local ports 8000/8001.
</verification>

<success_criteria>
- Gemini 2.5 Flash and Gemma 4 (API) are used for inference.
- `google-genai` SDK is used with correct `thinking_config`.
- Application remains functional (tests pass).
- Token counting uses SDK-native `count_tokens`.
</success_criteria>

<output>
After completion, create `.planning/phases/27-model-migration-replace-qwen-with-google-ai-studio-gemini-an/27-01-SUMMARY.md`
</output>
