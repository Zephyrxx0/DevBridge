---
phase: 34
plan: 04
type: execute
wave: 4
depends_on: ["34-03"]
files_modified:
  - web/src/components/chat/ChatShell.tsx
  - web/src/components/chat/__tests__/ChatShell.test.tsx
autonomous: true
gap_closure: true
requirements:
  - SHELL-04
must_haves:
  truths:
    - "Chat shell mounts and executes callbacks without runtime ReferenceError"
    - "Session-boundary shell behavior remains operational after callback fix"
    - "Regression test fails if undefined dependency is reintroduced"
  artifacts:
    - path: web/src/components/chat/ChatShell.tsx
      provides: "Runtime-safe callback dependencies for shell actions"
    - path: web/src/components/chat/__tests__/ChatShell.test.tsx
      provides: "Regression coverage for ChatShell mount and callback path"
  key_links:
    - from: web/src/components/chat/ChatShell.tsx
      to: web/src/components/chat/__tests__/ChatShell.test.tsx
      via: "test mount + callback invocation path"
---

<objective>
Close Phase 34 verification blocker by removing undefined callback dependency usage in `ChatShell` and adding regression coverage.

Purpose: Fix the runtime shell crash reported in `34-VERIFICATION.md` and restore must-have truth #5 (stable shell runtime).
Output: ChatShell dependency-safe callback code and deterministic regression test coverage.
</objective>

<execution_context>
@C:/Users/tronl/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/tronl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-chat-shell-session-boundaries/34-CONTEXT.md
@.planning/phases/34-chat-shell-session-boundaries/34-VERIFICATION.md
@.planning/phases/34-chat-shell-session-boundaries/34-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove undefined ChatShell callback dependency blocker</name>
  <files>web/src/components/chat/ChatShell.tsx</files>
  <read_first>
    - web/src/components/chat/ChatShell.tsx
    - .planning/phases/34-chat-shell-session-boundaries/34-VERIFICATION.md
  </read_first>
  <behavior>
    - Callback dependency arrays include only lexically defined identifiers.
    - ChatShell can mount and invoke remove-repository callback path without `ReferenceError: router is not defined`.
  </behavior>
  <action>
    Update `removeRepoFromWorkspace` callback in `web/src/components/chat/ChatShell.tsx` to eliminate the undefined `router` dependency cited in verification gap. Keep callback behavior unchanged (delete repo, clear localStorage keys, redirect to dashboard), but make dependency list strictly match identifiers used in callback scope. Implement per verification gap guidance in `34-VERIFICATION.md` while preserving D-19..D-23 route-shell ownership boundaries.
  </action>
  <acceptance_criteria>
    - `web/src/components/chat/ChatShell.tsx` contains `useCallback(..., [apiUrl, repoId])` for `removeRepoFromWorkspace` (no `router`).
    - `web/src/components/chat/ChatShell.tsx` does not contain the token pattern `, router]`.
    - `npm run lint --prefix web` exits 0 for updated file set.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint --prefix web -- --file src/components/chat/ChatShell.tsx</automated>
  </verify>
  <done>ChatShell no longer references undefined callback dependencies; runtime blocker root cause removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ChatShell regression test for mount + callback safety</name>
  <files>web/src/components/chat/__tests__/ChatShell.test.tsx</files>
  <read_first>
    - web/src/components/chat/ChatShell.tsx
    - web/src/components/chat/__tests__/ChatStream.test.tsx
    - web/jest.config.js
  </read_first>
  <behavior>
    - Test fails if ChatShell callback dependency list introduces undefined symbols causing mount/callback failures.
    - Test passes when ChatShell mounts and callback path executes without throwing.
  </behavior>
  <action>
    Create `web/src/components/chat/__tests__/ChatShell.test.tsx` using Jest + Testing Library patterns used in existing chat tests. Mock heavy child components (`ChatLayout`, `HistorySidebar`, Monaco editor, stream/input components) and isolate ChatShell runtime behavior. Add one regression test that mounts `ChatShell` with minimal repo props and triggers the remove-repo callback path via mocked `HistorySidebar` action to assert no throw/ReferenceError during callback execution path. Keep test deterministic and under 60 seconds runtime.
  </action>
  <acceptance_criteria>
    - Test file exists at `web/src/components/chat/__tests__/ChatShell.test.tsx`.
    - File contains a spec name matching `mounts ChatShell and executes removeRepo callback without ReferenceError`.
    - `npm run test --prefix web -- ChatShell.test.tsx --runInBand` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test --prefix web -- ChatShell.test.tsx --runInBand</automated>
  </verify>
  <done>Regression protection added for the exact runtime blocker path from verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UI -> destructive repo API | User-triggered remove action crosses into DELETE `/repo/{id}` network call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04-01 | D | `removeRepoFromWorkspace` callback path | mitigate | Ensure callback executes reliably (no runtime crash) so users can complete/remove flow deterministically and recover via existing API error handling. |
| T-34-04-02 | T | ChatShell callback dependencies | mitigate | Keep dependency arrays lexically scoped; add regression test to prevent undefined symbol regressions. |
</threat_model>

<verification>
- `npm run lint --prefix web -- --file src/components/chat/ChatShell.tsx`
- `npm run test --prefix web -- ChatShell.test.tsx --runInBand`
</verification>

<success_criteria>
- Phase 34 blocker in `34-VERIFICATION.md` is resolved: no undefined `router` dependency remains.
- ChatShell mount + callback regression test exists and passes.
- Phase can be re-verified without runtime ReferenceError in shell initialization path.
</success_criteria>

<output>
After completion, create `.planning/phases/34-chat-shell-session-boundaries/34-04-SUMMARY.md`
</output>
