---
phase: 34
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/routes/chats.py
  - web/src/hooks/useChatSessions.ts
autonomous: true
requirements:
  - SHELL-01
must_haves:
  truths:
    - "Session clear deletes messages without resetting the title"
    - "Session restore persists the last active session across reloads"
  artifacts:
    - path: api/routes/chats.py
      provides: "clear_chat_messages endpoint without title overwrite"
    - path: web/src/hooks/useChatSessions.ts
      provides: "Session management hook"
  key_links:
    - from: web/src/hooks/useChatSessions.ts
      to: api/routes/chats.py
      via: fetch calls
---

<objective>
Update backend clear endpoint and extract useChatSessions hook to manage session persistence and actions.

Purpose: Isolate session logic out of the route and fix the backend clear behavior (D-07).
Output: Backend clear endpoint updated and useChatSessions hook created.
</objective>

<execution_context>
@C:/Users/tronl/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/tronl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-chat-shell-session-boundaries/34-CONTEXT.md
@.planning/phases/34-chat-shell-session-boundaries/34-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Update backend clear endpoint</name>
  <files>api/routes/chats.py</files>
  <read_first>
    - api/routes/chats.py
  </read_first>
  <action>
    In `clear_chat_messages` inside `api/routes/chats.py`, update the `update_query` to remove the `title = 'New chat'` assignment per D-07. It should only set `updated_at = NOW()`. Leave the `DELETE FROM chat_messages` query as is.
  </action>
  <acceptance_criteria>
    - `api/routes/chats.py` contains `SET updated_at = NOW()` inside the `update_query` for `clear_chat_messages`.
    - `api/routes/chats.py` does NOT contain `title = 'New chat'` in the `clear_chat_messages` update query.
  </acceptance_criteria>
  <verify>
    <automated>grep -A 5 "UPDATE chat_sessions" api/routes/chats.py | grep -q "title" && exit 1 || exit 0</automated>
  </verify>
  <done>Backend endpoint updated to keep session title on clear.</done>
</task>

<task type="auto">
  <name>Task 2: Extract useChatSessions hook</name>
  <files>web/src/hooks/useChatSessions.ts</files>
  <read_first>
    - web/src/app/repo/[id]/page.tsx
  </read_first>
  <action>
    Create `web/src/hooks/useChatSessions.ts`. This hook must manage `sessions: ChatSession[]`, `activeSessionId: string | null`, and `loadingSessions: boolean`.
    Extract the following logic from `page.tsx`:
    - `createSession()` (POST `/repo/${repoId}/chats`)
    - `renameSession(sessionId, newTitle)` (PATCH `/repo/${repoId}/chats/${sessionId}`)
    - `deleteSession(sessionId)` (DELETE `/repo/${repoId}/chats/${sessionId}`) -> If deleting active, switch to nearest session by recency, or create new if none left (D-03, D-18).
    - `clearSession(sessionId)` (DELETE `/chats/${sessionId}/messages`)
    - `loadSessions()` (GET `/repo/${repoId}/chats`) -> On load, restore `activeSessionId` from `localStorage.getItem("repo:${repoId}:activeSessionId")`. If not found/invalid, pick newest. If empty, call `createSession` (D-01, D-02).
    - Persist `activeSessionId` to `localStorage` when it changes (D-22).
    The hook signature should be `export function useChatSessions(repoId: string, apiUrl: string)`. Return all state and handler functions.
  </action>
  <acceptance_criteria>
    - `web/src/hooks/useChatSessions.ts` exports `useChatSessions` hook.
    - Hook code contains `localStorage.getItem(\`repo:${repoId}:activeSessionId\`)`.
    - Hook code contains `localStorage.setItem(\`repo:${repoId}:activeSessionId\`, activeSessionId)`.
    - Hook exposes `createSession`, `renameSession`, `deleteSession`, `clearSession`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "localStorage.setItem" web/src/hooks/useChatSessions.ts</automated>
  </verify>
  <done>Session state and methods isolated into reusable hook.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Backend API | Chat operations must validate repo access (handled by existing routes) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | clear_chat_messages | accept | Session IDs are UUIDs; existing authorization logic applies. |
</threat_model>

<verification>
- Backend keeps title on clear.
- Hook is created and typescript compiles.
</verification>

<success_criteria>
- Hook successfully isolates localStorage and API calls for sessions.
</success_criteria>

<output>
After completion, create `.planning/phases/34-chat-shell-session-boundaries/34-01-SUMMARY.md`
</output>