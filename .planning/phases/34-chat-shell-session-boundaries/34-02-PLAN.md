---
phase: 34
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - web/src/components/chat/HistorySidebar.tsx
autonomous: true
requirements:
  - SHELL-02
must_haves:
  truths:
    - "User can rename chat sessions inline"
    - "User receives confirmation prompt when deleting a session"
    - "User can explicitly clear active chat from the utilities tab"
  artifacts:
    - path: web/src/components/chat/HistorySidebar.tsx
      provides: "UI implementation of sidebar actions without route orchestration"
  key_links:
    - from: web/src/components/chat/HistorySidebar.tsx
      to: web/src/components/chat/HistorySidebar.tsx
      via: props exposed by useChatSessions hook
---

<objective>
Update HistorySidebar UI to include app-owned dialogs/inline editing for session management, and add an explicit clear chat button to the Utilities tab.

Purpose: Move session actions UX out of native window prompt/confirm into proper components (D-14..D-17) and support the clear action (D-05, D-06, D-11).
Output: HistorySidebar handles rename, delete, and clear interactions.
</objective>

<execution_context>
@C:/Users/tronl/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/tronl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-chat-shell-session-boundaries/34-CONTEXT.md
@.planning/phases/34-chat-shell-session-boundaries/34-RESEARCH.md
</context>

<interfaces>
From web/src/hooks/useChatSessions.ts (created in 34-01):
```typescript
export function useChatSessions(repoId: string, apiUrl: string) {
  // returns { sessions, activeSessionId, loadingSessions, createSession, renameSession, deleteSession, clearSession, loadSessions }
}
```
</interfaces>

<tasks>
<task type="auto">
  <name>Task 1: Implement inline rename and delete dialog</name>
  <files>web/src/components/chat/HistorySidebar.tsx</files>
  <read_first>
    - web/src/components/chat/HistorySidebar.tsx
    - web/src/components/ui/dialog.tsx
  </read_first>
  <action>
    Update `HistorySidebarProps` to use `onRenameSession: (id: string, newTitle: string) => void` instead of `(id: string) => void`.
    Implement inline rename for chat sessions:
    - Add state for `editingSessionId` and `editingTitle`.
    - Render an `<input>` element instead of a button/link when `editingSessionId === session.id`.
    - On `<input>` `onKeyDown`: Enter saves (calls `onRenameSession(id, editingTitle)` if changed/non-empty), Escape cancels.
    - Empty or unchanged rename is a no-op and must not call `onRenameSession` (D-16).
    Implement custom delete dialog:
    - Add a `<Dialog>` to confirm deletion of a session (D-17). The dialog should display the session name.
    - On confirmation, call `onDeleteSession(id)`.
  </action>
  <acceptance_criteria>
    - `HistorySidebar.tsx` contains `onRenameSession: (id: string, newTitle: string) => void` in its props.
    - `HistorySidebar.tsx` contains `<input` logic for inline renaming (uses `Enter` to save).
    - `HistorySidebar.tsx` imports and uses `Dialog` or similar app-owned confirmation for deletion.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "onRenameSession.*newTitle" web/src/components/chat/HistorySidebar.tsx</automated>
  </verify>
  <done>Inline rename and delete dialog implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Clear active chat in Utilities</name>
  <files>web/src/components/chat/HistorySidebar.tsx</files>
  <read_first>
    - web/src/components/chat/HistorySidebar.tsx
  </read_first>
  <action>
    Update `HistorySidebarProps` to include `onClearSession: (id: string) => void`.
    In the Utilities tab rendering (`activeTab === 'utilities'`), add an explicit action: "Clear active chat" (D-06, D-11).
    This action MUST require confirmation using a dialog before calling `onClearSession(activeSessionId)` (D-08).
    Ensure the Chat tab (`activeTab === 'chat'`) only contains sessions, and does NOT contain repo utilities or the active clear action (D-10).
  </action>
  <acceptance_criteria>
    - `HistorySidebarProps` contains `onClearSession`.
    - `HistorySidebar.tsx` renders a clear action button in the Utilities tab logic block.
    - Clear action uses a confirmation dialog before invoking `onClearSession`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "onClearSession" web/src/components/chat/HistorySidebar.tsx</automated>
  </verify>
  <done>Clear session action added to utilities with confirmation.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User Input | Session titles provided by user |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02 | Spoofing | HistorySidebar | mitigate | Escape HTML entities in session titles; rely on React default XSS protection. |
</threat_model>

<verification>
- Inline rename works without calling window.prompt.
- Delete and Clear actions use app-owned confirmation dialogs.
</verification>

<success_criteria>
- User interactions for rename, delete, clear are implemented smoothly within HistorySidebar.
</success_criteria>

<output>
After completion, create `.planning/phases/34-chat-shell-session-boundaries/34-02-SUMMARY.md`
</output>