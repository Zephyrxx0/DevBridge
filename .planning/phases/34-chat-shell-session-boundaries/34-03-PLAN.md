---
phase: 34
plan: 03
type: execute
wave: 3
depends_on: ["34-01", "34-02"]
files_modified:
  - web/src/components/chat/ChatShell.tsx
  - web/src/app/repo/[id]/page.tsx
autonomous: true
requirements:
  - SHELL-04
must_haves:
  truths:
    - "Repo route page is a thin wrapper over a composed chat shell"
    - "Stream, messages, and session hooks compose inside the ChatShell module"
  artifacts:
    - path: web/src/components/chat/ChatShell.tsx
      provides: "Composed chat shell managing layout and session logic"
    - path: web/src/app/repo/[id]/page.tsx
      provides: "Thin repo page rendering ChatShell"
  key_links:
    - from: web/src/app/repo/[id]/page.tsx
      to: web/src/components/chat/ChatShell.tsx
      via: Component render
---

<objective>
Extract the heavy route orchestration into a `ChatShell` module and make the repo page a thin wrapper.

Purpose: Isolate route orchestration away from `page.tsx` into composed boundaries (D-19..D-21, D-23).
Output: A new `ChatShell` component and a slimmed down `page.tsx`.
</objective>

<execution_context>
@C:/Users/tronl/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/tronl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-chat-shell-session-boundaries/34-CONTEXT.md
@.planning/phases/34-chat-shell-session-boundaries/34-RESEARCH.md
</context>

<interfaces>
From web/src/hooks/useChatSessions.ts (created in 34-01):
```typescript
export function useChatSessions(repoId: string, apiUrl: string) {
  // returns { sessions, activeSessionId, loadingSessions, createSession, renameSession, deleteSession, clearSession, loadSessions }
}
```
</interfaces>

<tasks>
<task type="auto">
  <name>Task 1: Create ChatShell component</name>
  <files>web/src/components/chat/ChatShell.tsx</files>
  <read_first>
    - web/src/app/repo/[id]/page.tsx
    - web/src/components/chat/HistorySidebar.tsx
    - web/src/components/chat/ChatLayout.tsx
  </read_first>
  <action>
    Create `web/src/components/chat/ChatShell.tsx`.
    It should take props: `repoId: string`, `repo: Repo | null`, `apiUrl: string`.
    Extract the following from `page.tsx` and move into `ChatShell.tsx`:
    - `useChatSessions(repoId, apiUrl)` instantiation.
    - `messages`, `loadingMessages`, `input`, `isLoading`, `snippetChips`, `selectedSource` states.
    - Stream execution logic (`onSubmit` handling, `fetch` against `/chats/${activeSessionId}/messages`, SSE reader loop).
    - Logic for handling `/clear` (if `input === "/clear"`, call `clearSession(activeSessionId)`).
    - `ChatLayout` composition: pass `HistorySidebar` (wired with `useChatSessions` props), `ChatStream`, and `ChatInput`.
    Do NOT refactor the stream transport internals deeply—just move the existing logic over (D-20).
  </action>
  <acceptance_criteria>
    - `web/src/components/chat/ChatShell.tsx` is created and exports `ChatShell`.
    - `ChatShell` consumes `useChatSessions`.
    - `ChatShell` manages `messages` and streaming loop.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export function ChatShell" web/src/components/chat/ChatShell.tsx</automated>
  </verify>
  <done>ChatShell component encapsulates session and stream behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: Slim down route page.tsx</name>
  <files>web/src/app/repo/[id]/page.tsx</files>
  <read_first>
    - web/src/app/repo/[id]/page.tsx
    - web/src/components/chat/ChatShell.tsx
  </read_first>
  <action>
    Refactor `web/src/app/repo/[id]/page.tsx`:
    - Remove all inline session operations (`createSession`, `renameChat`, `deleteChat`, `loadSessions`, `loadMessages` effect, activeId localStorage).
    - Remove `messages` state and stream fetch loops.
    - The page should only handle params loading, repo fetching (`useRepo()`), error states, context bounds (like resolving Theme), and rendering `<ChatShell repoId={params.id as string} repo={repo} apiUrl={apiUrl} />`.
    - There should be no inline session logic remaining in `page.tsx` (D-21, D-23).
  </action>
  <acceptance_criteria>
    - `page.tsx` no longer contains `createSession` or `deleteChat` inline functions.
    - `page.tsx` renders `<ChatShell />`.
    - `page.tsx` is significantly smaller, focused purely on outer route mechanics.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "<ChatShell" web/src/app/repo/\[id\]/page.tsx</automated>
  </verify>
  <done>Route page is now a thin wrapper around ChatShell.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Component | ChatShell groups all related session context correctly |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Tampering | ChatShell | accept | Handled by downstream hooks and backend validation. |
</threat_model>

<verification>
- `ChatShell` exists and works as composed boundary.
- `page.tsx` has no inline session logic left.
</verification>

<success_criteria>
- The route shrinks considerably and delegates effectively to ChatShell.
</success_criteria>

<output>
After completion, create `.planning/phases/34-chat-shell-session-boundaries/34-03-SUMMARY.md`
</output>