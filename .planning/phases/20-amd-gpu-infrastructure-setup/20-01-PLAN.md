---
phase: 20-amd-gpu-infrastructure-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api/core/config.py, api/requirements.txt, api/utils/tokenizer.py, api/routes/chats.py, tests/test_phase20_truncation.py, tests/test_phase20_config.py, docker-compose.yml]
autonomous: true
requirements: [IR-01, IR-03]
must_haves:
  truths:
    - "D-01, D-04: Big Model and Fast Model instances are isolated via strict VRAM partitions in a single docker-compose."
    - "D-05, D-06, D-07, D-08: System enforces a maximum context limit of 48,000 tokens per request via FastAPI Gateway using exact HuggingFace tokenizers, providing Truncation (dropping older history) + Warning instead of 400 rejection."
    - "D-02: FastAPI Gateway routes requests via application logic by explicitly targeting separate ports."
    - "D-03: Embeddings continue to use Vertex AI text-embedding-004 to preserve VRAM."
  artifacts:
    - path: "api/utils/tokenizer.py"
      provides: "Context limit enforcement and token counting"
      min_lines: 10
    - path: "docker-compose.yml"
      provides: "Multi-container AMD GPU orchestration"
      contains: "vllm-deep"
  key_links:
    - from: "api/routes/chats.py"
      to: "api/utils/tokenizer.py"
      via: "enforce_cap before sending context to models"
      pattern: "enforce_cap"
---

<objective>
Configure VRAM partitioning for vLLM and implement context token cap enforcement in FastAPI.

Purpose: Prevent VRAM exhaustion (OOM) on the single MI300X GPU while efficiently orchestrating the Qwen and Gemma models.
Output: Tokenizer utility with test coverage, extended API configs, updated chat routes, and the base docker-compose infrastructure.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-amd-gpu-infrastructure-setup/20-CONTEXT.md
@.planning/phases/20-amd-gpu-infrastructure-setup/20-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API Configuration and Tokenizer Utility</name>
  <files>api/core/config.py, api/requirements.txt, api/utils/tokenizer.py, tests/test_phase20_config.py, tests/test_phase20_truncation.py</files>      
  <action>
    1. In `api/requirements.txt`, append `transformers` and `huggingface-hub`.
    2. In `api/core/config.py`, add the following to `Settings`:
       - `max_context_tokens: int = Field(default=48000, validation_alias="MAX_CONTEXT_TOKENS")`
       - `big_model_port: int = Field(default=8000, validation_alias="BIG_MODEL_PORT")` (per D-02)
       - `fast_model_port: int = Field(default=8001, validation_alias="FAST_MODEL_PORT")` (per D-02)
    3. Create `api/utils/tokenizer.py`. Use exact HuggingFace tokenizers (per D-08) for accurate counting: `transformers.AutoTokenizer.from_pretrained("Qwen/Qwen2.5-72B-Instruct-AWQ")` and `transformers.AutoTokenizer.from_pretrained("google/gemma-4-9b-it")`. Implement `def enforce_cap(messages: list, codebase_chunk: str, max_tokens: int = 48000, model_type: str = "qwen") -> tuple[list, bool]:`. If the combined token count > max_tokens, drop the oldest conversational history first (per D-07), always keep `codebase_chunk`, set the warning flag to True, and return `(truncated_messages, True)` (per D-05, D-06). Otherwise return `(messages, False)`. Note: Embeddings use Vertex AI (per D-03).
       Follow standard error handling patterns such as logging exceptions rather than failing silently if counting fails unexpectedly.
    4. Create `tests/test_phase20_config.py` to assert the default properties of the new `Settings` variables.
    5. Create `tests/test_phase20_truncation.py` to assert `enforce_cap` properly truncates history and preserves the codebase chunk for both tokenizers.
  </action>
  <verify>
    <automated>pytest tests/test_phase20_config.py tests/test_phase20_truncation.py -x</automated>
  </verify>
  <done>API is configured to support the infrastructure token constraints with validated token counting logic.</done>
</task>

<task type="auto">
  <name>Task 2: API Route Integration</name>
  <files>api/routes/chats.py</files>
  <action>
    In `api/routes/chats.py`, locate the endpoint where chat messages are constructed for inference. Import `enforce_cap` from `api/utils/tokenizer.py`.
    Before sending context to the model, process the messages via `messages, warning = enforce_cap(messages, codebase_chunk)`. If `warning` is True, inject a warning into the response payload explicitly stating "Context was trimmed to 48K tokens limit. Older history removed." (per D-06).
    Apply the existing error handling pattern from `api/routes/chats.py` analogs.
  </action>
  <verify>
    <automated>grep -v '^#' api/routes/chats.py | grep -c enforce_cap</automated>
  </verify>
  <done>FastAPI API Gateway actively enforces context truncation prior to forwarding to vLLM.</done>
</task>

<task type="auto">
  <name>Task 3: Docker Compose Base and VRAM Partitioning</name>
  <files>docker-compose.yml</files>
  <action>
    Create a single `docker-compose.yml` (per D-04) in the project root.
    Define two separate vLLM containers (per D-01): `vllm-deep` and `vllm-fast`.
    For `vllm-deep`: `image: rocm/vllm:latest`, `ipc: host`, `devices: ["/dev/kfd:/dev/kfd", "/dev/dri:/dev/dri"]`, `group_add: ["video"]`, `ports: ["8000:8000"]`, `command: "--model Qwen/Qwen2.5-72B-Instruct-AWQ --gpu-memory-utilization 0.60 --max-model-len 48000 --port 8000"`.
    For `vllm-fast`: `image: rocm/vllm:latest`, `ipc: host`, `devices: ["/dev/kfd:/dev/kfd", "/dev/dri:/dev/dri"]`, `group_add: ["video"]`, `ports: ["8001:8001"]`, `command: "--model Gemma-4-9B-it --gpu-memory-utilization 0.20 --max-model-len 48000 --port 8001"`.
  </action>
  <verify>
    <automated>docker-compose config</automated>
  </verify>
  <done>Docker orchestrator is defined with strict VRAM allocations per AMD GPU partitioning rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API Gateway | Untrusted client input enters the system here. |
| API Gateway -> vLLM | Internal request routing from FastAPI to local model containers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Denial of Service | `api/routes/chats.py` | mitigate | Truncate prompts mathematically via exact HuggingFace `AutoTokenizer` token limits prior to passing to models. |
| T-20-02 | Denial of Service | `vllm-deep` & `vllm-fast` | mitigate | Hardware isolation using strict `--gpu-memory-utilization` args to ensure memory boundaries. |
</threat_model>
