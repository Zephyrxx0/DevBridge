---
phase: 20-amd-gpu-infrastructure-setup
plan: 03
type: execute
wave: 3
depends_on: [20-01, 20-02]
files_modified: [api/requirements.txt, api/core/config.py, api/db/vector_store.py, api/ingestion/history.py, .env.example]
autonomous: true
requirements: [IR-01, IR-02, IR-03]
gap_closure: true

must_haves:
  truths:
    - "D-03: Embeddings use Vertex AI text-embedding-004 by default"
    - "Embedding service is configurable via EMBEDDING_MODEL setting"
    - "Vertex AI implementation exists and is imported when configured"
  artifacts:
    - path: "api/core/config.py"
      provides: "text-embedding-004 as default"
    - path: "api/db/vector_store.py"
      provides: "VertexAIEmbeddings integration"
  key_links:
    - from: "api/db/vector_store.py"
      to: "langchain_google_vertexai"
      via: "import and instantiation"
---

<objective>
Restore Vertex AI embedding continuity as required by decision D-03. This ensures that embedding tasks remain offloaded to the hosted API, preserving local AMD MI300X VRAM for large language model (LLM) inference and KV cache.

Purpose: Address verification failure where embeddings defaulted to a local placeholder instead of the specified Vertex AI model.
Output: Working Vertex AI embedding integration with fallback support for local development.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-amd-gpu-infrastructure-setup/20-CONTEXT.md
@.planning/phases/20-amd-gpu-infrastructure-setup/20-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dependencies and core configuration</name>
  <files>api/requirements.txt, api/core/config.py, .env.example</files>
  <action>
    - Add `langchain-google-vertexai` to `api/requirements.txt`.
    - Update `api/core/config.py`:
      - Set `embedding_model` default to "text-embedding-004".
      - Add `google_cloud_project` setting (alias `GOOGLE_CLOUD_PROJECT`) to support Vertex AI authentication.
    - Update `.env.example` to reflect the new default and required GCP variables.
  </action>
  <verify>
    <automated>pytest tests/test_phase20_config.py</automated>
  </verify>
  <done>Dependencies added and settings default to Vertex AI.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Vertex AI in Vector Store and History Ingestion</name>
  <files>api/db/vector_store.py, api/ingestion/history.py</files>
  <action>
    - Modify `api/db/vector_store.py`:
      - In `initialize()`, check `settings.embedding_model`.
      - If "text-embedding-004", instantiate `VertexAIEmbeddings` from `langchain_google_vertexai`.
      - Use `LocalEmbeddings` as fallback for other values.
    - Modify `api/ingestion/history.py`:
      - Refactor `_embed_text` to use the same logic (it should ideally use `vector_db.embedding_service` if initialized, or instantiate the correct one based on settings).
  </action>
  <verify>
    <automated>python -c "from api.db.vector_store import vector_db; from api.core.config import settings; print(f'Model: {settings.embedding_model}')"</automated>
  </verify>
  <done>Codebase supports Vertex AI embeddings based on configuration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify embedding service selection</name>
  <files>tests/test_vertex_embeddings.py</files>
  <behavior>
    - When `settings.embedding_model` is "text-embedding-004", `vector_db.initialize()` should use `VertexAIEmbeddings`.
    - When `settings.embedding_model` is "local-hash-embedding-v1", `vector_db.initialize()` should use `LocalEmbeddings`.
  </behavior>
  <action>
    Create a test that mocks `VertexAIEmbeddings` and verifies that the correct class is instantiated in `vector_db.initialize()` based on different setting values.
  </action>
  <verify>
    <automated>pytest tests/test_vertex_embeddings.py</automated>
  </verify>
  <done>Automated verification of embedding service selection logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Vertex AI | API transmits code snippets and query text to Google Cloud for embedding generation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | `api/db/vector_store.py` | mitigate | Ensure Vertex AI is used over encrypted HTTPS (default in SDK). |
| T-20-02 | Denial of Service | `api/db/vector_store.py` | mitigate | Implement fallback to `LocalEmbeddings` if Vertex AI is unreachable or credentials missing (log warning, don't crash). |
</threat_model>

<verification>
1. Run `pytest tests/test_vertex_embeddings.py` to confirm logic selection.
2. Verify `api/core/config.py` defaults to `text-embedding-004`.
</verification>

<success_criteria>
- `embedding_model` default is `text-embedding-004`.
- `VertexAIEmbeddings` is used when the model is set to the default.
- `LocalEmbeddings` fallback remains functional.
</success_criteria>

<output>
After completion, create `.planning/phases/20-amd-gpu-infrastructure-setup/20-03-SUMMARY.md`
</output>
