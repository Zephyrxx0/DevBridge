---
phase: 26
plan: 01
type: execute
wave: 1
depends_on: ["26-00"]
files_modified:
  - api/routes/admin.py
  - api/db/models.py
  - sql/migrations/0031_add_is_admin.sql
  - api/reports/generator.py
  - api/jobs/reports.py
  - tests/test_admin_auth.py
  - tests/test_report_generator.py
autonomous: true
requirements:
  - FR-06
must_haves:
  truths:
    - "Only admin users can access the reports API"
    - "Reports are generated per-repository"
  artifacts:
    - path: "api/routes/admin.py"
      provides: "Admin API routes"
    - path: "sql/migrations/0031_add_is_admin.sql"
      provides: "DB migration for admin role"
  key_links:
    - from: "api/routes/admin.py"
      to: "api/reports/generator.py"
      via: "ReportsHub"
      pattern: "ReportsHub"
---

<objective>
Implement the backend infrastructure for the Admin Dashboard, including authentication, repository-scoped report generation, and automated cron jobs.

Purpose: Provide secure access to "intern confusion" summaries scoped by repository.
Output: Admin API, migration, refactored generator, and cron job.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-admin-dashboard/26-CONTEXT.md
@.planning/phases/26-admin-dashboard/26-RESEARCH.md
@.planning/phases/26-admin-dashboard/26-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auth & API Route</name>
  <files>api/routes/admin.py, api/db/models.py, sql/migrations/0031_add_is_admin.sql, tests/test_admin_auth.py</files>
  <behavior>
    - Test 1: verify_admin dependency rejects non-admin users with 403
    - Test 2: verify_admin allows is_admin=true users
    - Test 3: GET /admin/repo/{id}/reports returns list of markdown reports
  </behavior>
  <action>
    1. Create `sql/migrations/0031_add_is_admin.sql` to add `is_admin BOOLEAN DEFAULT FALSE` to the `users` table.
    2. Update `api/db/models.py` (or appropriate model file) to include `is_admin` in the User record/dataclass.
    3. Create `api/routes/admin.py` implementing a `verify_admin` FastAPI dependency.
    4. Implement GET `/admin/repo/{repo_id}/reports` endpoint using `ReportsHub` to list and serve markdown files.
  </action>
  <verify>
    <automated>pytest tests/test_admin_auth.py</automated>
  </verify>
  <done>Admin auth middleware and API route are functional and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Report Generator & Cron Job</name>
  <files>api/reports/generator.py, api/jobs/reports.py, tests/test_report_generator.py</files>
  <behavior>
    - Test 1: generate_daily_intelligence_report(repo_id) filters by repo_id
    - Test 2: Cron job iterates over all active repositories and calls generator
  </behavior>
  <action>
    1. Refactor `api/reports/generator.py` to accept a `repo_id` parameter and filter Gemma/SQL queries accordingly.
    2. Update `api/jobs/reports.py` to fetch all active repositories and iterate over them, triggering a report generation for each.
    3. Ensure filenames in `ReportsHub` include the `repo_id` to prevent collisions.
  </action>
  <verify>
    <automated>pytest tests/test_report_generator.py</automated>
  </verify>
  <done>Report generation is scoped to repositories and automated for all repos.</done>
</task>

<task type="auto">
  <name>Task 3: Security Verification</name>
  <files>web/tests/admin.spec.ts</files>
  <action>
    Verify path traversal prevention in `ReportsHub` and admin access controls using Playwright.
    The test should attempt to access files outside the reports directory via the API and confirm rejection.
    Ensures `26-VALIDATION.md` requirement 26-01-03 is met.
  </action>
  <verify>
    <automated>npx playwright test web/tests/admin.spec.ts</automated>
  </verify>
  <done>Security controls are verified via E2E test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Admin access control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-1 | Elevation of Privilege | Admin API | mitigate | `verify_admin` dependency enforcement |
| T-26-01-2 | Information Disclosure | ReportsHub | mitigate | Path sanitization in ReportsHub |
</threat_model>

<verification>
Run `pytest tests/test_admin_auth.py tests/test_report_generator.py` and `npx playwright test web/tests/admin.spec.ts`.
</verification>

<success_criteria>
Backend supports per-repo reports with strict admin-only access.
</success_criteria>

<output>
After completion, create `.planning/phases/26-admin-dashboard/26-01-SUMMARY.md`
</output>
