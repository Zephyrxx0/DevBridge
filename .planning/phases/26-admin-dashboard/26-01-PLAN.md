---
phase: 26
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_admin_auth.py
  - tests/test_report_generator.py
  - web/tests/admin.spec.ts
  - sql/migrations/0031_add_is_admin.sql
  - api/routes/admin.py
  - api/reports/generator.py
  - web/src/app/repo/[id]/admin/page.tsx
autonomous: true
requirements:
  - FR-06
must_haves:
  truths:
    - "Maintainer can access admin dashboard for a specific repository"
    - "Non-admin users receive a 403 Forbidden error"
    - "Dashboard displays daily markdown reports of intern confusion topics"
    - "Reports are specific to the selected repository"
  artifacts:
    - path: "sql/migrations/0031_add_is_admin.sql"
      provides: "Boolean is_admin flag on users table"
    - path: "api/routes/admin.py"
      provides: "Admin API endpoints"
      exports: ["verify_admin", "get_repo_reports"]
    - path: "api/reports/generator.py"
      provides: "Per-repository report aggregation"
    - path: "web/src/app/repo/[id]/admin/page.tsx"
      provides: "Admin dashboard UI"
  key_links:
    - from: "web/src/app/repo/[id]/admin/page.tsx"
      to: "/api/admin/repo/{id}/reports"
      via: "fetch"
      pattern: "fetch.*api/admin/repo"
    - from: "api/routes/admin.py"
      to: "users"
      via: "database query"
      pattern: "SELECT is_admin FROM users"
---

<objective>
Implement the Admin Dashboard for viewing "intern confusion" topics, restricted to authorized users.

Purpose: Allow maintainers to identify areas where developers struggle, scoped per-repository (D-01), rendered simply as markdown feeds (D-02), and protected by Supabase roles (D-03).
Output: Working backend endpoints, report generator refactoring, and Next.js frontend dashboard.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-admin-dashboard/26-CONTEXT.md
@.planning/phases/26-admin-dashboard/26-RESEARCH.md
@.planning/phases/26-admin-dashboard/26-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Test Scaffolds (Wave 0)</name>
  <files>tests/test_admin_auth.py, tests/test_report_generator.py, web/tests/admin.spec.ts</files>
  <action>
    Create failing test scaffolds for the admin dashboard features to satisfy Nyquist verification rules.
    - tests/test_admin_auth.py: test verify_admin logic
    - tests/test_report_generator.py: test report generator repo filtering
    - web/tests/admin.spec.ts: E2E test for the admin dashboard and path traversal prevention
  </action>
  <verify>
    <automated>ls tests/test_admin_auth.py tests/test_report_generator.py web/tests/admin.spec.ts</automated>
  </verify>
  <done>Test files are created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Auth & API Endpoints</name>
  <files>sql/migrations/0031_add_is_admin.sql, api/routes/admin.py, tests/test_admin_auth.py</files>
  <behavior>
    - Test 1: verify_admin rejects request without user_id
    - Test 2: verify_admin rejects non-admin user
    - Test 3: verify_admin accepts admin user
    - Test 4: GET /admin/repo/{id}/reports returns list of report files
  </behavior>
  <action>
    Create migration 0031 to add `is_admin BOOLEAN DEFAULT FALSE` to `users`.
    Implement `api/routes/admin.py` with `verify_admin` FastAPI dependency checking `request.state.user_id` against the database.
    Implement GET `/admin/repo/{repo_id}/reports` returning markdown content from ReportsHub (using Phase 25's Hub pattern), protected by `verify_admin`.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create tests/test_admin_auth.py first</automated>
  </verify>
  <done>Migration created, API endpoint returns reports for admin, 403 for non-admin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-Repo Generator Refactor</name>
  <files>api/reports/generator.py, tests/test_report_generator.py</files>
  <behavior>
    - Test 1: Generator queries `questions` table with `repo_id` filter
    - Test 2: Saves report with repo_id in filename
  </behavior>
  <action>
    Modify `generate_daily_intelligence_report` (or equivalent) in `api/reports/generator.py` to accept a `repo_id`.
    Update the SQL query to include `WHERE repo_id = :repo_id`.
    Save the output using ReportsHub with format `daily-{repo_id}-{date}.md`.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create tests/test_report_generator.py first</automated>
  </verify>
  <done>Generator filters by repository and names files accordingly.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard Frontend</name>
  <files>web/src/app/repo/[id]/admin/page.tsx, web/tests/admin.spec.ts</files>
  <action>
    Create the Admin Dashboard page for a repository.
    Fetch reports from the new backend endpoint `/admin/repo/{id}/reports`.
    Render the markdown directly as a feed using `tiptap` or existing markdown components.
    Use shadcn (base-nova, card, skeleton, badge) per UI-SPEC.
    Handle empty state ("No confusion topics logged") and error states.
    Note: Do not build interactive charts or global aggregation per deferred ideas.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create web/tests/admin.spec.ts first</automated>
  </verify>
  <done>Page renders the markdown feed and handles 403 errors cleanly if unauthorized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | User requests admin reports from the backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Elevation of Privilege | `api/routes/admin.py` | mitigate | Strict `Depends(verify_admin)` on all admin endpoints |
| T-26-02 | Information Disclosure | `ReportsHub` | mitigate | `ReportsHub._safe_path` used to prevent path traversal when loading markdown |
</threat_model>

<verification>
1. Run backend tests: `pytest tests/test_admin_auth.py tests/test_report_generator.py`
2. Run frontend E2E tests: `cd web && npx playwright test tests/admin.spec.ts`
</verification>

<success_criteria>
1. Dashboard shows confusion topics per repository (D-01).
2. Gemma 4 generates summaries (maintained from previous phase, now per repo).
3. Access is restricted to `is_admin` users (D-03).
4. Reports displayed in Markdown format (D-02).
</success_criteria>

<output>
After completion, create `.planning/phases/26-admin-dashboard/26-01-SUMMARY.md`
</output>