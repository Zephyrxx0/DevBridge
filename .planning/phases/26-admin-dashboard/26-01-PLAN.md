---
phase: 26
plan: 01
type: execute
wave: 1
depends_on: ["26-00"]
files_modified:
  - sql/migrations/0031_add_is_admin.sql
  - api/routes/admin.py
  - api/reports/generator.py
  - api/jobs/reports.py
  - tests/test_admin_auth.py
  - tests/test_report_generator.py
autonomous: true
requirements:
  - FR-06
must_haves:
  truths:
    - "Non-admin users receive a 403 Forbidden error"
    - "Reports are specific to the selected repository"
  artifacts:
    - path: "sql/migrations/0031_add_is_admin.sql"
      provides: "Boolean is_admin flag on users table"
    - path: "api/routes/admin.py"
      provides: "Admin API endpoints"
      exports: ["verify_admin", "get_repo_reports"]
    - path: "api/reports/generator.py"
      provides: "Per-repository report aggregation"
  key_links:
    - from: "api/routes/admin.py"
      to: "users"
      via: "database query"
      pattern: "SELECT is_admin FROM users"
    - from: "api/routes/admin.py"
      to: "api/reports/generator.py"
      via: "ReportsHub"
      pattern: "ReportsHub"
---

<objective>
Implement the Backend endpoints and report generation for the Admin Dashboard.

Purpose: Allow maintainers to identify areas where developers struggle, scoped per-repository (D-01), rendered simply as markdown feeds (D-02), and protected by Supabase roles (D-03).
Output: Working backend endpoints and report generator refactoring.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-admin-dashboard/26-CONTEXT.md
@.planning/phases/26-admin-dashboard/26-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auth & API Endpoints</name>
  <files>sql/migrations/0031_add_is_admin.sql, api/routes/admin.py, tests/test_admin_auth.py</files>
  <behavior>
    - Test 1: verify_admin rejects request without user_id
    - Test 2: verify_admin rejects non-admin user
    - Test 3: verify_admin accepts admin user
    - Test 4: GET /admin/repo/{id}/reports returns list of report files
  </behavior>
  <action>
    Create migration 0031 to add `is_admin BOOLEAN DEFAULT FALSE` to `users`.
    Implement `api/routes/admin.py` with `verify_admin` FastAPI dependency checking `request.state.user_id` against the database.
    Implement GET `/admin/repo/{repo_id}/reports` returning markdown content from ReportsHub (using Phase 25's Hub pattern), protected by `verify_admin`.
  </action>
  <verify>
    <automated>pytest tests/test_admin_auth.py</automated>
  </verify>
  <done>Migration created, API endpoint returns reports for admin, 403 for non-admin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-Repo Generator Refactor</name>
  <files>api/reports/generator.py, api/jobs/reports.py, tests/test_report_generator.py</files>
  <behavior>
    - Test 1: Generator queries `questions` table with `repo_id` filter
    - Test 2: Saves report with repo_id in filename
    - Test 3: Cron job iterates active repos and calls generator per repo
  </behavior>
  <action>
    Modify `generate_daily_intelligence_report` (or equivalent) in `api/reports/generator.py` to accept a `repo_id`.
    Update the SQL query to include `WHERE repo_id = :repo_id`.
    Save the output using ReportsHub with format `daily-{repo_id}-{date}.md`.
    Update `api/jobs/reports.py` cron job to query all active repositories and call `generate_daily_intelligence_report` for each, passing the `repo_id`.
  </action>
  <verify>
    <automated>pytest tests/test_report_generator.py</automated>
  </verify>
  <done>Generator filters by repository and names files accordingly. Cron job processes all repos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | User requests admin reports from the backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Elevation of Privilege | `api/routes/admin.py` | mitigate | Strict `Depends(verify_admin)` on all admin endpoints |
| T-26-02 | Information Disclosure | `ReportsHub` | mitigate | `ReportsHub._safe_path` used to prevent path traversal when loading markdown |
</threat_model>

<verification>
1. Run backend tests: `pytest tests/test_admin_auth.py tests/test_report_generator.py`
</verification>

<success_criteria>
1. Backend correctly filters reports per repository (D-01).
2. Access is restricted to `is_admin` users (D-03).
3. Markdown reports generated correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/26-admin-dashboard/26-01-SUMMARY.md`
</output>