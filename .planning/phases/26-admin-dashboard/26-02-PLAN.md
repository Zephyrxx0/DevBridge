---
phase: 26
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - web/src/app/repo/[id]/admin/page.tsx
  - web/tests/admin.test.tsx
autonomous: true
requirements:
  - FR-06
must_haves:
  truths:
    - "Maintainer can access admin dashboard for a specific repository"
    - "Dashboard displays daily markdown reports of intern confusion topics"
  artifacts:
    - path: "web/src/app/repo/[id]/admin/page.tsx"
      provides: "Admin dashboard UI"
  key_links:
    - from: "web/src/app/repo/[id]/admin/page.tsx"
      to: "/api/admin/repo/{id}/reports"
      via: "fetch"
      pattern: "fetch.*api/admin/repo"
---

<objective>
Implement the Frontend Admin Dashboard.

Purpose: Render the markdown reports from the backend API directly as a feed using the UI spec.
Output: Next.js frontend dashboard.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-admin-dashboard/26-CONTEXT.md
@.planning/phases/26-admin-dashboard/26-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard Frontend</name>
  <files>web/src/app/repo/[id]/admin/page.tsx, web/tests/admin.test.tsx</files>
  <action>
    Create the Admin Dashboard page for a repository.
    Fetch reports from the backend endpoint `/admin/repo/{id}/reports`.
    Render the markdown directly as a feed using `tiptap` or existing markdown components.
    Use shadcn (base-nova, card, skeleton, badge) per UI-SPEC.
    Handle empty state ("No confusion topics logged") and error states.
    Note: Do not build interactive charts or global aggregation per deferred ideas.
  </action>
  <verify>
    <automated>cd web ; npx jest tests/admin.test.tsx</automated>
  </verify>
  <done>Page renders the markdown feed and handles 403 errors cleanly if unauthorized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Client | Maintainer viewing UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Spoofing | Frontend | mitigate | Backend handles auth, frontend just renders based on HTTP response status. |
</threat_model>

<verification>
1. Run frontend tests: `cd web ; npx jest tests/admin.test.tsx`
</verification>

<success_criteria>
1. Dashboard shows confusion topics per repository (D-01).
2. Reports displayed in Markdown format (D-02).
</success_criteria>

<output>
After completion, create `.planning/phases/26-admin-dashboard/26-02-SUMMARY.md`
</output>