---
phase: 26
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - web/src/app/repo/[id]/admin/page.tsx
autonomous: true
requirements:
  - FR-06
must_haves:
  truths:
    - "Maintainers can view the markdown feed of confusion topics"
  artifacts:
    - path: "web/src/app/repo/[id]/admin/page.tsx"
      provides: "Admin dashboard page"
  key_links:
    - from: "web/src/app/repo/[id]/admin/page.tsx"
      to: "/api/admin/repo/{id}/reports"
      via: "fetch"
      pattern: "fetch.*admin/repo"
---

<objective>
Implement the Admin Dashboard UI to render AI-generated "intern confusion" reports for a specific repository.

Purpose: Allow maintainers to browse and read summaries of developer struggles.
Output: Next.js admin page with markdown rendering.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-admin-dashboard/26-CONTEXT.md
@.planning/phases/26-admin-dashboard/26-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin Dashboard UI</name>
  <files>web/src/app/repo/[id]/admin/page.tsx</files>
  <action>
    Create the Admin Dashboard page for a repository.
    1. Implement fetching of reports from `/api/admin/repo/{id}/reports`.
    2. Render the markdown reports as a scrollable feed.
    3. Use shadcn components (Card, Badge, Skeleton) as defined in `26-UI-SPEC.md`.
    4. Handle 403 Forbidden errors by showing an "Access Denied" state.
    5. Handle empty states when no reports are available.
  </action>
  <verify>
    <automated>cd web ; npx playwright test tests/admin.spec.ts</automated>
  </verify>
  <done>Admin dashboard renders correctly and passes E2E tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> UI | Content rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-1 | Information Disclosure | Frontend | mitigate | Server-side auth enforcement |
</threat_model>

<verification>
Run `npx playwright test web/tests/admin.spec.ts` (from root) or `cd web ; npx playwright test tests/admin.spec.ts`.
</verification>

<success_criteria>
Dashboard displays markdown reports and respects access controls.
</success_criteria>

<output>
After completion, create `.planning/phases/26-admin-dashboard/26-02-SUMMARY.md`
</output>
