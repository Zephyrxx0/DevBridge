---
phase: 26
plan: 03
type: execute
wave: 3
depends_on: ["26-01", "26-02"]
files_modified:
  - api/routes/admin.py
  - tests/test_admin_auth.py
autonomous: true
requirements:
  - FR-06
gap_closure: true
must_haves:
  truths:
    - "Admin access strictly enforced via `is_admin` role"
  artifacts:
    - path: "api/routes/admin.py"
      provides: "Admin guard that requires database `users.is_admin=true` for all admin endpoints"
    - path: "tests/test_admin_auth.py"
      provides: "Regression tests proving internal token cannot bypass admin role check"
  key_links:
    - from: "api/routes/admin.py::verify_admin"
      to: "users.is_admin"
      via: "SELECT is_admin FROM users WHERE id = CAST(:uid AS uuid)"
      pattern: "SELECT is_admin FROM users"
---

<objective>
Close verification blocker by removing alternate auth success path and enforcing `is_admin` role checks on every admin route per D-03 and ROADMAP Phase 26 success criterion #2.

Purpose: eliminate privileged access path that bypasses role authorization.
Output: strict role-based `verify_admin` implementation plus regression tests.
</objective>

<execution_context>
@C:/Users/tronl/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/tronl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-admin-dashboard/26-CONTEXT.md
@.planning/phases/26-admin-dashboard/26-VERIFICATION.md
@api/routes/admin.py
@tests/test_admin_auth.py

<interfaces>
From api/routes/admin.py:
```python
async def verify_admin(request: Request, x_user_id: str | None = Header(default=None, alias="X-User-Id")) -> str
```

From tests/test_admin_auth.py:
```python
def _build_admin_app(tmp_path: Path, monkeypatch: pytest.MonkeyPatch, row: dict[str, object] | None) -> TestClient
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove internal-token bypass from admin guard</name>
  <files>api/routes/admin.py</files>
  <read_first>
    - api/routes/admin.py
    - .planning/phases/26-admin-dashboard/26-VERIFICATION.md
    - .planning/phases/26-admin-dashboard/26-CONTEXT.md
  </read_first>
  <behavior>
    - Request without `X-User-Id` (or `request.state.user_id`) returns 401.
    - Request with user id and DB row `is_admin=false` returns 403.
    - Request with user id and DB row `is_admin=true` returns success.
    - Presence of `X-Internal-Auth` header alone never grants access.
  </behavior>
  <action>
    Update `verify_admin` in `api/routes/admin.py` to remove the early return at current lines 24-25 (`if expected and provided == expected: return "internal"`). Keep only role-based flow: resolve user id from `X-User-Id` or `request.state.user_id`; require DB lookup `SELECT is_admin FROM users WHERE id = CAST(:uid AS uuid)`; reject non-admin with `HTTPException(403, "Forbidden")`; return concrete `user_id` string on success. Do not add any alternative success path (per D-03).
  </action>
  <acceptance_criteria>
    - `api/routes/admin.py` does not contain `return "internal"`.
    - `api/routes/admin.py` contains `SELECT is_admin FROM users`.
    - `api/routes/admin.py` contains exactly one success return in `verify_admin` and it returns `user_id`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_admin_auth.py -q</automated>
  </verify>
  <done>Admin guard enforces `is_admin` strictly and no header-only bypass remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression tests for bypass denial</name>
  <files>tests/test_admin_auth.py</files>
  <read_first>
    - tests/test_admin_auth.py
    - api/routes/admin.py
    - .planning/phases/26-admin-dashboard/26-VERIFICATION.md
  </read_first>
  <behavior>
    - Internal-token-only request to `/admin/repo/{repo_id}/reports` is denied (401/403).
    - Admin request with `X-User-Id` still allowed.
    - Non-admin request with `X-User-Id` remains denied.
  </behavior>
  <action>
    Extend `tests/test_admin_auth.py` with a dedicated regression test that sends `X-Internal-Auth` without `X-User-Id` and asserts denied status. Keep existing allow/deny tests and adjust fixtures only as needed for deterministic behavior. If settings token is needed, monkeypatch `api.routes.admin.settings.internal_auth_token` to a known value inside the test.
  </action>
  <acceptance_criteria>
    - `tests/test_admin_auth.py` contains a test name matching `internal` and `bypass` semantics.
    - Running `python -m pytest tests/test_admin_auth.py -q` exits 0.
    - Test asserts denial when only `X-Internal-Auth` is provided.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_admin_auth.py -q</automated>
  </verify>
  <done>Regression coverage prevents reintroducing internal-token bypass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → admin API | Untrusted caller attempting privileged report access |
| app auth context → DB role check | User identity must map to `users.is_admin` truth source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | E (Elevation of Privilege) | `api/routes/admin.py::verify_admin` | mitigate | Remove header-based early return; require DB `is_admin` for success on every request |
| T-26-03-02 | I (Information Disclosure) | `/admin/repo/{repo_id}/reports` | mitigate | Ensure dependency chain always executes strict `verify_admin`; regression test blocks bypass |
</threat_model>

<verification>
- `python -m pytest tests/test_admin_auth.py -q` passes.
- `rg "return \"internal\"" api/routes/admin.py` returns no matches.
</verification>

<success_criteria>
1. Verification gap "Admin access strictly enforced via `is_admin` role" flips from failed to verified.
2. No admin route can be accessed through `X-Internal-Auth` without a valid admin user record.
3. FR-06 remains satisfied with strict access semantics aligned to D-03.
</success_criteria>

<output>
After completion, create `.planning/phases/26-admin-dashboard/26-03-SUMMARY.md`
</output>
