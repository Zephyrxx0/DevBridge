---
phase: 33-behavior-pinning-prompt-helpers
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/hooks/useOnboarding.test.ts
  - web/src/components/chat/__tests__/ChatStream.test.tsx
autonomous: true
requirements: [SHELL-03]
must_haves:
  truths:
    - "User can enter first-run onboarding from an empty chat per D-14."
    - "Onboarding flow states IDLE, QUALIFYING, STREAMING, PLAN_READY, DONE remain covered by behavior pins per D-14."
    - "Cached onboarding plan reuse checks `/api/backend/repo/{repoId}/onboarding-plan` before EventSource per D-15."
    - "Cancel and try-again/error behavior remain pinned without moving onboarding ownership per D-16."
    - "ChatStream first-run, reopen, and completion behavior has render tests per D-17."
  artifacts:
    - path: "web/src/hooks/useOnboarding.test.ts"
      provides: "Cached plan, SSE, cancel, reset, error regression pins"
    - path: "web/src/components/chat/__tests__/ChatStream.test.tsx"
      provides: "First-run onboarding, reopen overlay/card, completion render regression pins"
  key_links:
    - from: "web/src/hooks/useOnboarding.test.ts"
      to: "web/src/hooks/useOnboarding.ts"
      via: "cached plan fetch before EventSource"
      pattern: "onboarding-plan"
    - from: "web/src/components/chat/__tests__/ChatStream.test.tsx"
      to: "web/src/components/chat/ChatStream.tsx"
      via: "OnboardingGuide mock props and reopen event"
      pattern: "devbridge:open-onboarding"
---

<objective>
Add onboarding behavior pins only; preserve current ownership and route behavior.

Purpose: SHELL-03 requires onboarding entry and completion behavior to remain stable before later shell/session boundary work.
Output: Extended `useOnboarding` hook tests and `ChatStream` render tests covering D-14 through D-17.
</objective>

<execution_context>
@C:/Users/tronl/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/tronl/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-behavior-pinning-prompt-helpers/33-CONTEXT.md
@.planning/phases/33-behavior-pinning-prompt-helpers/33-RESEARCH.md
@.planning/phases/33-behavior-pinning-prompt-helpers/33-VALIDATION.md
@web/src/hooks/useOnboarding.ts
@web/src/hooks/useOnboarding.test.ts
@web/src/components/chat/ChatStream.tsx
@web/src/components/chat/__tests__/ChatStream.test.tsx
@web/src/components/onboarding/OnboardingGuide.tsx

<interfaces>
`useOnboarding(repoId)` returns:
```ts
{ status, plan, loading, error, startGeneration, cancelGeneration, reset }
```

Cached plan contract:
```ts
fetch(`/api/backend/repo/${repoId}/onboarding-plan`)
new EventSource(`/api/backend/repo/${repoId}/start-here?focus=${encodedFocus}`)
```

ChatStream onboarding sentinel:
```ts
const onboardingSummaryToken = "__DEVBRIDGE_ONBOARDING_READY__";
window.dispatchEvent(new CustomEvent("devbridge:open-onboarding"));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend useOnboarding cached/cancel/try-again pins</name>
  <files>web/src/hooks/useOnboarding.test.ts</files>
  <read_first>
    - .planning/phases/33-behavior-pinning-prompt-helpers/33-CONTEXT.md
    - .planning/phases/33-behavior-pinning-prompt-helpers/33-VALIDATION.md
    - web/src/hooks/useOnboarding.ts
    - web/src/hooks/useOnboarding.test.ts
  </read_first>
  <action>Extend `MockEventSource` with `close = jest.fn()` or close-call tracking and optional `readyState` support. Add tests per D-14/D-15: initial render proves IDLE-equivalent hook state (`loading === false`, `plan === null`, `status === []`, `error === null`); `startGeneration("Backend")` proves STREAMING setup by calling `fetch("/api/backend/repo/123/onboarding-plan")` before EventSource and setting `loading === true`; cached plan success proves PLAN_READY-equivalent state by setting `plan`, setting `loading` false, and creating zero EventSource instances; cache miss opens `/api/backend/repo/123/start-here?focus=Backend`; EventSource `{ type: "plan" }` sets plan and `loading === false`; `cancelGeneration()` closes EventSource, sets `loading` false, clears `error`, and does not clear an existing plan; after an error message or connection error, calling `startGeneration("Frontend")` again resets status/error and opens a new EventSource, proving try-again behavior. DONE is owned by `OnboardingGuide`/`ChatStream`; this plan's Task 2 must assert completion callback path. Do not change `useOnboarding.ts` in this plan unless tests reveal missing behavior required by D-14/D-15.</action>
  <acceptance_criteria>
    - `web/src/hooks/useOnboarding.test.ts` contains `/api/backend/repo/123/onboarding-plan`.
    - `web/src/hooks/useOnboarding.test.ts` asserts initial IDLE-equivalent state: `loading` false, `plan` null, `status` empty, and `error` null.
    - `web/src/hooks/useOnboarding.test.ts` asserts STREAMING setup after `startGeneration("Backend")`: `loading` true and one EventSource on cache miss.
    - `web/src/hooks/useOnboarding.test.ts` asserts PLAN_READY-equivalent plan message handling sets `plan` and `loading` false.
    - `web/src/hooks/useOnboarding.test.ts` asserts cached reuse creates `0` EventSource instances.
    - `web/src/hooks/useOnboarding.test.ts` contains `cancelGeneration()` test with close assertion.
    - `web/src/hooks/useOnboarding.test.ts` contains try-again/restart assertion after error.
    - `npm run test --prefix web -- useOnboarding` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run test --prefix web -- useOnboarding</automated>
  </verify>
  <done>Hook tests pin cached reuse, cancel, retry/error, SSE URL, and cleanup behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ChatStream onboarding render pins</name>
  <files>web/src/components/chat/__tests__/ChatStream.test.tsx</files>
  <read_first>
    - .planning/phases/33-behavior-pinning-prompt-helpers/33-CONTEXT.md
    - .planning/phases/33-behavior-pinning-prompt-helpers/33-UI-SPEC.md
    - web/src/components/chat/ChatStream.tsx
    - web/src/components/chat/__tests__/ChatStream.test.tsx
    - web/src/components/onboarding/OnboardingGuide.tsx
  </read_first>
  <action>Replace shallow `OnboardingGuide` mock with a mock that renders `data-testid="onboarding-guide"`, exposes `resumeAtPlan` as `data-resume-at-plan`, renders `resumePlan?.summary`, and has a button labelled `Complete onboarding` that calls `onComplete(mockPlan)`. Add tests per D-14/D-17: empty `messages` with `isLoading={false}` renders first-run onboarding (IDLE entry); empty `messages` with `isInitializing={true}` renders skeleton and not onboarding; assistant message content `__DEVBRIDGE_ONBOARDING_READY__` renders `Onboarding guide ready.` and `Reopen onboarding`; clicking `Reopen onboarding` opens overlay; completing first-run by clicking `Complete onboarding` calls `onOnboardingComplete(mockPlan)` and proves DONE completion callback path; after completion stores latest plan, reopening must render mocked `OnboardingGuide` with `data-resume-at-plan="true"` and the mock plan summary. Keep existing escalation tests green by passing required `onOnboardingComplete={jest.fn()}` everywhere.</action>
  <acceptance_criteria>
    - `web/src/components/chat/__tests__/ChatStream.test.tsx` contains `__DEVBRIDGE_ONBOARDING_READY__`.
    - `web/src/components/chat/__tests__/ChatStream.test.tsx` contains `devbridge:open-onboarding` or clicks `Reopen onboarding`.
    - `web/src/components/chat/__tests__/ChatStream.test.tsx` asserts `onOnboardingComplete` was called with a mock plan.
    - `web/src/components/chat/__tests__/ChatStream.test.tsx` contains `Complete onboarding`.
    - `web/src/components/chat/__tests__/ChatStream.test.tsx` asserts `data-resume-at-plan="true"` after completion and reopen.
    - `web/src/components/chat/__tests__/ChatStream.test.tsx` asserts first-run onboarding renders when `messages={[]}` and `isLoading={false}`.
    - `web/src/components/chat/__tests__/ChatStream.test.tsx` uses the mocked `OnboardingGuide` to expose a `Start onboarding` or equivalent button and asserts clicking it renders QUALIFYING-equivalent choice UI before streaming.
    - `web/src/components/chat/__tests__/ChatStream.test.tsx` asserts skeleton path does not render onboarding when `isInitializing={true}`.
    - `npm run test --prefix web -- ChatStream` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run test --prefix web -- ChatStream</automated>
  </verify>
  <done>ChatStream tests pin first-run, reopen, cached resume, and completion callback behavior without moving onboarding ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser hook -> `/api/backend/repo/{repoId}/onboarding-plan` | Cached onboarding plan lookup crosses existing backend route. |
| browser hook -> EventSource `/start-here` | SSE plan generation crosses existing backend route. |
| ChatStream -> OnboardingGuide | Completion plan object crosses component boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-08 | Spoofing | cached onboarding fetch | accept | Phase 33 changes tests only; no auth/session endpoint or backend route behavior changes. |
| T-33-09 | Denial of Service | EventSource lifecycle | mitigate | Tests pin cancel/unmount close and retry resets to avoid duplicate active EventSource instances. |
| T-33-10 | Tampering | ChatStream completion plan | mitigate | Render tests assert `onOnboardingComplete(plan)` forwards current plan unchanged. |
</threat_model>

<verification>
Run `npm run test --prefix web -- useOnboarding ChatStream` after each task. Before summary, run `npm run test --prefix web -- prompt-context ChatInput ChatStream useOnboarding && npx --yes fallow --production && graphify update .`.
</verification>

<success_criteria>
SHELL-03 has automated evidence for cached plan reuse, onboarding first-run entry, reopen, completion, cancel, and try-again behavior without ownership extraction.
</success_criteria>

<output>
After completion, create `.planning/phases/33-behavior-pinning-prompt-helpers/33-03-SUMMARY.md`.
</output>
