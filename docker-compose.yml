services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - repo_cache_data:/app/repo_cache
      - reports_data:/app/reports

volumes:
  repo_cache_data:
  reports_data:
